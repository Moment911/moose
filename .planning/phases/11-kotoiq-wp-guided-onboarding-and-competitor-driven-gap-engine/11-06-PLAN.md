---
phase: 11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine
plan: 06
type: execute
wave: 5
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-05"]
files_modified:
  - src/views/kotoiq/KotoIQShellPage.jsx
  - src/components/kotoiq-wp/GuidedSpine.jsx
  - src/components/kotoiq-wp/steps/StepConnected.jsx
  - src/components/kotoiq-wp/steps/StepSiteToday.jsx
  - src/components/kotoiq-wp/steps/StepCompetitors.jsx
  - src/components/kotoiq-wp/steps/StepGaps.jsx
  - src/components/kotoiq-wp/steps/StepPlan.jsx
  - src/components/kotoiq-wp/steps/StepLiveCited.jsx
autonomous: false
requirements: [ONBOARD-08]
must_haves:
  truths:
    - "A linear 6-step guided spine renders at /kotoiq-wp (Connected → Your site today → Who you're up against → Your gaps → Your plan → Live + cited) without disturbing FleetView or power-user tabs"
    - "Every step has a plain-English 'what this does' subtitle (no bare acronyms), exactly one primary action, and a visible status (done/running/waiting)"
    - "Long background scans show live status from run_all_status polling, so they never look broken"
    - "All new UI uses DESIGN.md koto/* primitives + tokens, not the old theme"
  artifacts:
    - path: "src/components/kotoiq-wp/GuidedSpine.jsx"
      provides: "6-step WorkflowStepper rail assembling steps 1-6"
      min_lines: 60
    - path: "src/views/kotoiq/KotoIQShellPage.jsx"
      provides: "New ?shell=guided mode routing to GuidedSpine"
      contains: "guided"
  key_links:
    - from: "src/views/kotoiq/KotoIQShellPage.jsx"
      to: "src/components/kotoiq-wp/GuidedSpine.jsx"
      via: "shell==='guided' renders GuidedSpine"
      pattern: "guided"
    - from: "src/components/kotoiq-wp/steps/StepConnected.jsx (and StepSiteToday)"
      to: "run_all_status polling"
      via: "fetch run_all_status for live scan status"
      pattern: "run_all_status"
---

<objective>
Build WS7: the guided 6-step spine that assembles everything from 11-01..11-05 into a linear,
self-explanatory onboarding at `/kotoiq-wp` — Connected → Your site today → Who you're up against →
Your gaps → Your plan → Live + cited. Add a new `?shell=guided` mode (default for first-run/client
view) that renders a WorkflowStepper rail; FleetView and existing power-user tabs stay reachable.

Purpose: Turns the wired engines into a guided experience a non-expert can follow, with plain-English
subtitles, one primary action per step, and visible status so long scans never look broken.
Output: GuidedSpine + 6 step components + shell routing. Ends in a human-verify checkpoint (visual/UX).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-CONTEXT.md
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-RESEARCH.md
@DESIGN.md
@_knowledge/data-integrity-standard.md

# Shell to extend (VERIFIED): shell from ?shell @56 (default 'intel'); sub @66; useAuth agencyId @51; useClient clientId @52; intel short-circuits to <KotoIQPage/>
@src/views/kotoiq/KotoIQShellPage.jsx
@src/components/kotoiq-wp/ClientView.jsx
@src/components/kotoiq-wp/FleetView.jsx
# Reference implementation for new DESIGN.md tokens/primitives:
@src/components/kotoiq/AEOVisibilityTab.jsx

<interfaces>
<!-- DESIGN.md primitives (src/components/ui/koto/*) — MANDATORY, NOT the old src/lib/theme tokens (Pitfall 6):
     Pattern 7 WorkflowStepper (the 6-step rail), Pattern 1 SectionHeader + Pattern 3 EducationalNote
     (gloss AEO/GEO acronyms), Pattern 4 ActionCallout (one primary action), Pattern 10 LiveTicker
     (scan-running status), Pattern 5 EmptyState, Pattern 8 FlagChip, Pattern 12 Skeleton.
     Palette: navy #201b51, pink #cb1c6b, cream #faf9f6. -->

<!-- Step → data/action mapping (RESEARCH WS7):
  1 Connected      → koto_wp_sites.shim_version==='v4' + connected
  2 Your site today→ 11-02 kotoiq_site_baseline (page count/types) + run_all_status live scan
  3 Competitors    → analyze_competitors / grid-scan scoped by 11-04 CityPicker cities
  4 Your gaps      → 11-05 score_grid bucketed report ("38 opportunities — 9 quick wins, 21 net-new, 8 big bets")
  5 Your plan      → ranked build order → Page Factory build (11-05 auto-link; approval gate)
  6 Live + cited   → published pages + aeoVisibilityEngine citation status (surface only; no auto-remediate — deferred) -->

<!-- run_all_status — /api/kotoiq action @5831, input {run_id} → {status, wave, total_waves, completed_actions[], failed_actions[], completed_at}. Poll for step-2 live status. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GuidedSpine rail + shell routing + steps 1-3</name>
  <files>src/views/kotoiq/KotoIQShellPage.jsx, src/components/kotoiq-wp/GuidedSpine.jsx, src/components/kotoiq-wp/steps/StepConnected.jsx, src/components/kotoiq-wp/steps/StepSiteToday.jsx, src/components/kotoiq-wp/steps/StepCompetitors.jsx</files>
  <action>
    In `KotoIQShellPage.jsx`, add a new shell value `guided` (make it the default for the first-run/client view
    without breaking the existing `intel`/FleetView paths — keep those reachable as power-user tabs). When
    `shell==='guided'`, render `<GuidedSpine clientId agencyId/>`. Read `node_modules/next/dist/docs/` for any
    new framework pattern first (AGENTS.md). Build `GuidedSpine.jsx` ("use client") using DESIGN.md Pattern 7
    WorkflowStepper for the 6-step rail with done/running/waiting status per step. Build steps 1-3:
    - StepConnected: pair status from koto_wp_sites (shim_version==='v4' + connected); EducationalNote subtitle; one primary action ("Re-scan" or "Continue").
    - StepSiteToday: 11-02 baseline page count/types; LiveTicker driven by run_all_status polling while the scan runs (so it never looks broken).
    - StepCompetitors: 11-04 CityPicker to choose cities → triggers analyze_competitors/grid-scan; EducationalNote glossing "competitor rank data".
    Every step: plain-English subtitle (no bare acronyms — gloss AEO/GEO), exactly one primary action, visible
    status. DESIGN.md koto/* primitives + tokens ONLY (never old #111/FH/FB). Read DESIGN.md before any visual choice.
  </action>
  <verify>
    <automated>test -f src/components/kotoiq-wp/GuidedSpine.jsx && grep -q "guided" src/views/kotoiq/KotoIQShellPage.jsx && npx tsc --noEmit 2>&1 | grep -E "GuidedSpine|KotoIQShellPage|Step(Connected|SiteToday|Competitors)" || echo "spine + steps 1-3: no type errors"</automated>
  </verify>
  <done>?shell=guided renders the WorkflowStepper spine; steps 1-3 work with subtitles, one action, live status; FleetView/intel untouched; DESIGN.md primitives used.</done>
</task>

<task type="auto">
  <name>Task 2: Steps 4-6 (gaps, plan, live+cited)</name>
  <files>src/components/kotoiq-wp/steps/StepGaps.jsx, src/components/kotoiq-wp/steps/StepPlan.jsx, src/components/kotoiq-wp/steps/StepLiveCited.jsx</files>
  <action>
    Build the final three steps, wired to GuidedSpine:
    - StepGaps: calls 11-05 `score_grid`, renders the bucketed report as outcomes not metrics
      ("38 opportunities, ranked by what'll move traffic fastest — 9 quick wins, 21 net-new, 8 big bets" —
      CONTEXT specific). Show no-competitor cities with their reason. Embed the 11-03 ServiceChips so the user
      confirms services before scoring drives builds. FlagChip for AI-inferred.
    - StepPlan: ranked build order → Page Factory build (existing flow, restyled to DESIGN.md). One primary
      action ("Build N pages"). KEEP the approval gate (no auto-publish). Auto-linking happens via 11-05.
    - StepLiveCited: published pages + aeoVisibilityEngine citation status — SURFACE ONLY (no auto-remediation —
      deferred). EducationalNote glossing "AEO = whether AI assistants recommend you".
    All steps: DESIGN.md koto/* primitives + tokens, plain-English subtitles, one primary action, visible status.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "Step(Gaps|Plan|LiveCited)" || echo "steps 4-6: no type errors"</automated>
  </verify>
  <done>Steps 4-6 render the bucketed gap report, ranked build order → factory build (approval gate), and citation status surface; outcomes-style copy; DESIGN.md primitives.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — guided 6-step spine</name>
  <action>Operator visually + functionally verifies the assembled guided spine before the phase gate. No code change in this task.</action>
  <what-built>The full guided 6-step spine at /kotoiq-wp (?shell=guided), assembling pairing status, baseline inventory, competitor discovery, the bucketed gap report, the build plan, and citation status — all on DESIGN.md primitives, with live scan status.</what-built>
  <how-to-verify>
    1. Run `next dev --no-turbo` (STATE.md: Turbopack/React19 Sidebar bug).
    2. Visit `/kotoiq-wp?shell=guided` for a paired client (use a Phase 7 UAT seed client if needed).
    3. Walk the 6 steps. Confirm each has: a plain-English subtitle (no bare acronyms — AEO/GEO glossed), exactly ONE primary action, and a visible status (done/running/waiting).
    4. Trigger a scan and confirm step 2 shows live status (not a frozen/blank spinner).
    5. Confirm step 4 reads like outcomes ("N opportunities — X quick wins...") and shows AI-inferred service chips + any no-competitor city reasons.
    6. Confirm FleetView and the old Intel/Publish/Tune tabs are still reachable (power-user paths intact).
    7. Confirm visuals match DESIGN.md (navy/pink/cream, koto primitives) — flag any old-theme leakage.
  </how-to-verify>
  <verify>Operator walks all 6 steps in a `next dev --no-turbo` session per how-to-verify; FleetView + power tabs confirmed intact.</verify>
  <done>Operator approves the guided spine (6 steps, subtitles, one action each, live status, DESIGN.md compliance, FleetView intact).</done>
  <resume-signal>Type "approved" or describe issues (per step).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/kotoiq | All step data/actions carry agency_id; gated server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Info disclosure | guided shell rendering another agency's client | mitigate | Steps read clientId/agencyId from useClient/useAuth; every action server-side agency-gated (no client-only trust) |
| T-11-18 | Tampering | UI presenting unverified AI services/ranks as fact | mitigate | FlagChip on AI-inferred services; DataSourceBadge freshness on ranks (data-integrity standard) |
| T-11-19 | Tampering | accidental publish without review | mitigate | Approval gate preserved in StepPlan (no auto-publish) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for touched files
- Human-verify checkpoint approved (6 steps, subtitles, one action each, live status, DESIGN.md compliance, FleetView intact)
- `npm run test` full suite green at phase gate
</verification>

<success_criteria>
- Linear 6-step guided spine at /kotoiq-wp; FleetView/power tabs untouched.
- Each step: plain-English subtitle, one primary action, visible status; live scan status via run_all_status.
- Outcomes-style gap copy; AI-inferred flagged; approval gate retained; citation status surfaced (no auto-remediate).
- All new UI on DESIGN.md primitives + tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-06-SUMMARY.md`
</output>
