---
phase: 11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/kotoiq/CityPicker.jsx
  - src/components/kotoiq/TopicCampaignPanel.jsx
  - src/lib/builder/pageGapEngine.ts
  - tests/kotoiq/pageGapEngineCities.test.ts
autonomous: true
requirements: [ONBOARD-05]
must_haves:
  truths:
    - "A reusable Census-backed city multi-select component exists and is shared by both the guided shell and the campaign panel"
    - "Chosen cities scope competitor discovery — analyzePageGaps accepts an explicit cities[] and filters to it"
    - "Cities load from Census (geoLookup / existing geo route), carrying source provenance"
  artifacts:
    - path: "src/components/kotoiq/CityPicker.jsx"
      provides: "Extracted shared Census city multi-select (state select + filter + select-all + cap)"
      min_lines: 60
    - path: "src/lib/builder/pageGapEngine.ts"
      provides: "analyzePageGaps accepts cities[] and filters citiesToCheck to it"
      contains: "cities"
  key_links:
    - from: "src/components/kotoiq/CityPicker.jsx"
      to: "Census geo source (geoLookup / /api/geo)"
      via: "loadCities → VerifiedDataSource"
      pattern: "loadCities|getPlacesForState|/api/geo"
    - from: "src/lib/builder/pageGapEngine.ts (analyzePageGaps)"
      to: "citiesToCheck"
      via: "filter to explicit cities[] when provided"
      pattern: "cities"
---

<objective>
Build WS4: extract the existing Census-backed city multi-select out of `TopicCampaignPanel` into a
shared `CityPicker` component, and extend `analyzePageGaps` to accept an explicit `cities[]` so the
chosen cities scope competitor discovery. This is independent of WS1-3 and can land in parallel.

Purpose: A single targeting control (cities) replaces State+Counties, reused by the guided shell and
the campaign panel, and drives the gap grid (WS5).
Output: shared `CityPicker.jsx` (TopicCampaignPanel refactored to consume it) + `analyzePageGaps` cities[] support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-CONTEXT.md
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-RESEARCH.md
@_knowledge/data-integrity-standard.md
@DESIGN.md

# Reuse target — the picker to extract (VERIFIED this session):
@src/components/kotoiq/TopicCampaignPanel.jsx  # selectedCities Set @237; loadCities() @1151; toggleCity @1169; selectAllFiltered @1177; picker JSX @2480-2536; cityChip @3693
# Gap engine to extend (VERIFIED): analyzePageGaps @86; citiesToCheck = cities.slice(0,cityLimit) @168; loop @171
@src/lib/builder/pageGapEngine.ts
@src/lib/geoLookup.ts   # getPlacesForState → VerifiedDataSource (Census)

<interfaces>
<!-- analyzePageGaps input (VERIFIED @41-72) -->
```typescript
export interface GapAnalysisInput {
  agencyId: string; clientId: string; services: string[];
  state: string; counties?: string[]; cityLimit?: number; // default 100
  // ADD: cities?: string[]  — when present, filter citiesToCheck to these names instead of slice(0, cityLimit)
}
```
<!-- CityPicker contract (extract from TopicCampaignPanel; controlled component): -->
```jsx
<CityPicker state={state} onStateChange={fn} selectedCities={Set<string>} onToggle={fn}
            onSelectAllFiltered={fn} onClear={fn} cap={100} />
// Cities load via Census (geoLookup / existing /api/geo route loadCities hits) — keep provenance.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract CityPicker shared component</name>
  <files>src/components/kotoiq/CityPicker.jsx, src/components/kotoiq/TopicCampaignPanel.jsx</files>
  <action>
    Extract the city multi-select from `TopicCampaignPanel.jsx` (@2480-2536 picker JSX, plus loadCities @1151,
    toggleCity @1169, selectAllFiltered @1177, cityChip @3693, selectedCities Set @237) into a new controlled
    component `src/components/kotoiq/CityPicker.jsx` ("use client"). Keep the SAME Census load path
    (loadCities → whatever geo route/`geoLookup.getPlacesForState` it currently hits — confirm the exact route
    @1155-1162 and preserve provenance), the 500-render cap, and the per-deploy cap. Make it a controlled
    component (props per the interface). Then refactor TopicCampaignPanel to consume `<CityPicker .../>` —
    behavior must be unchanged (same selectedCities Set, same deploy flow). Do NOT rebuild the picker
    (RESEARCH Don't-Hand-Roll). Use DESIGN.md koto/* primitives for the new component shell; read DESIGN.md first.
  </action>
  <verify>
    <automated>test -f src/components/kotoiq/CityPicker.jsx && npx tsc --noEmit 2>&1 | grep -E "CityPicker|TopicCampaignPanel" || echo "CityPicker + panel: no type errors"</automated>
  </verify>
  <done>CityPicker exists as a shared controlled Census city multi-select; TopicCampaignPanel consumes it with unchanged behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: analyzePageGaps accepts explicit cities[]</name>
  <files>src/lib/builder/pageGapEngine.ts, tests/kotoiq/pageGapEngineCities.test.ts</files>
  <behavior>
    - When input.cities is provided (non-empty), analyzePageGaps filters citiesToCheck to only those city names (case-insensitive) instead of cities.slice(0, cityLimit).
    - When input.cities is absent, behavior is unchanged (back-compat: slice(0, cityLimit)).
    - The filter matches on city name; unknown names are dropped (not invented — data-integrity: cities still come from the Census-loaded set, never fabricated).
  </behavior>
  <action>
    Add `cities?: string[]` to `GapAnalysisInput` (@41) and, at the citiesToCheck computation (@168), when
    `cities?.length`, filter the loaded census cities to those whose name matches (case-insensitive) instead of
    slicing. Keep the existing slice path as the default. Do NOT change any downstream scoring in this plan —
    only the city scoping. Extract the city-filter step into a small pure helper so it's unit-testable.

    Tests (in tests/kotoiq/pageGapEngineCities.test.ts): feed a fixture census-city list + the pure filter
    helper: (1) cities:['Austin','Dallas'] → only those two retained; (2) no cities → slice(0,cityLimit) shape
    unchanged; (3) a name not in the census list is dropped, never added.
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/pageGapEngineCities.test.ts</automated>
  </verify>
  <done>analyzePageGaps scopes to explicit cities[] when given, falls back to slice otherwise, never fabricates a city.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → geo source | City list load for the picker (Census-backed) |
| city input → gap engine | User-chosen city names scope competitor discovery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering | fabricated/non-Census city names | mitigate | Filter only against Census-loaded set; drop unknown names (data-integrity standard) |
| T-11-12 | Info disclosure | city list leakage across agencies | accept | City lists are public Census data, not agency-private |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/pageGapEngineCities.test.ts` green
- `npx tsc --noEmit` clean for touched files
- Manual: TopicCampaignPanel city picker still selects + deploys exactly as before
</verification>

<success_criteria>
- Shared Census CityPicker extracted; campaign panel behavior unchanged.
- analyzePageGaps scopes competitor discovery to explicit cities[].
- Cities always sourced from Census; never fabricated.
</success_criteria>

<output>
After completion, create `.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-04-SUMMARY.md`
</output>
