---
phase: 11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/lib/kotoiq/serviceInference.ts
  - src/app/api/kotoiq/route.ts
  - src/components/kotoiq/ServiceChips.jsx
  - tests/kotoiq/serviceInference.test.ts
autonomous: true
requirements: [ONBOARD-04]
must_haves:
  truths:
    - "After the baseline scan, a clean services list is inferred FROM the client's own scanned pages"
    - "Inferred services appear as editable add/remove chips, pre-seeded from the real site, flagged AI-inferred"
    - "User-confirmed services persist with provenance and feed the WS5 gap grid"
    - "Optional per-service target phrases auto-derive from kotoiq_keywords and accept manual pins"
  artifacts:
    - path: "src/lib/kotoiq/serviceInference.ts"
      provides: "inferServices() over baseline page text (Haiku or heuristic), provenance-wrapped"
      exports: ["inferServices"]
    - path: "src/components/kotoiq/ServiceChips.jsx"
      provides: "Editable service chips with AI-inferred badge"
      min_lines: 40
    - path: "src/app/api/kotoiq/route.ts"
      provides: "infer_services + save_services actions"
      contains: "infer_services"
  key_links:
    - from: "src/components/kotoiq/ServiceChips.jsx"
      to: "/api/kotoiq (infer_services / save_services)"
      via: "fetch with agency_id"
      pattern: "infer_services|save_services"
    - from: "src/lib/kotoiq/serviceInference.ts"
      to: "logTokenUsage"
      via: "log every Claude call"
      pattern: "logTokenUsage\\("
---

<objective>
Build WS3: infer the client's services FROM the baseline-scanned pages (not the keyword scan),
present them as editable add/remove chips pre-seeded from the real site and flagged AI-inferred,
and persist confirmed services with provenance so they feed the WS5 gap grid. Add optional
per-service target phrases auto-derived from `kotoiq_keywords` plus manual pins.

Purpose: Replaces the manual comma-separated services box in `PageSuggestionsTab` with a
self-explanatory, real-site-seeded, user-verifiable input — the services half of the gap grid.
Output: `inferServices()` engine + `infer_services`/`save_services` actions + `ServiceChips` component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-CONTEXT.md
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-RESEARCH.md
@_knowledge/data-integrity-standard.md
@DESIGN.md

# Reuse / mirror (read before implementing):
@src/components/kotoiq/PageSuggestionsTab.jsx   # current manual services box @47-51,333-338 (to replace); uniqueServices @271
# Provenance + storage target (RESEARCH WS3 A3): kotoiq_client_profile.fields, source_type:'ai_inferred'
# Chip UI references: ConversationalBot.jsx ChoiceChips @918; AEOVisibilityTab.jsx chip() @789, AI badge @618
# DESIGN.md Pattern 8 (FlagChip) + AI-inferred badge (violet/info variant). Use new koto/* primitives, NOT old theme tokens.

<interfaces>
<!-- ExtractedPage (from 11-02 baseline / pageContentExtractor) provides h1, meta_title, url, word_count.
     inferServices reads these. inferPageType already isolates /services/... paths. -->
<!-- logTokenUsage — MANDATORY on every Claude call. Use Haiku for cheap inference:
     model 'claude-haiku-4-5-20251001', feature:'kotoiq_service_inference'. -->
<!-- Canonical kotoiq route shape (STATE.md Phase 7): verifySession → 401 if !verified||!agencyId →
     ALLOWED_ACTIONS validation → action chain → typed db helpers. Mirror verbatim. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: serviceInference engine (Haiku or heuristic, provenance-wrapped)</name>
  <files>src/lib/kotoiq/serviceInference.ts, tests/kotoiq/serviceInference.test.ts</files>
  <behavior>
    - inferServices({agencyId, clientId, pages}): given baseline pages (h1 + meta_title + url path segments), returns a deduped clean services list, each item carrying provenance {source_type:'ai_inferred', confidence, source_url|page} so the UI can flag it.
    - Prefer the cheapest-accurate approach (CONTEXT discretion): a heuristic over /services/ URL paths + H1 patterns first, escalating to a single Haiku pass over page text only when the heuristic is thin.
    - If a Claude pass runs, it logs via logTokenUsage({feature:'kotoiq_service_inference', model:'claude-haiku-4-5-20251001'}).
    - Output is deterministic-shaped (sorted, deduped) so it's unit-testable on fixtures without network.
  </behavior>
  <action>
    Implement `inferServices` in `src/lib/kotoiq/serviceInference.ts`. Keep the parsing/dedup/normalization as
    pure helpers. Wrap each inferred service in a ProvenanceRecord consistent with Phase 7
    `kotoiq_client_profile.fields` (source_type:'ai_inferred'). Persisting confirmed services goes to
    `kotoiq_client_profile.fields` (RESEARCH WS3 A3) — expose a `saveConfirmedServices()` helper the route calls.
    Any Claude call uses Haiku + logTokenUsage. Per data-integrity standard, AI-inferred services MUST be
    flagged for the UI (carry the flag in the returned shape).

    Tests: (1) heuristic extracts services from fixture pages with /services/{x} paths + service-y H1s, deduped
    + sorted; (2) each returned service carries source_type:'ai_inferred' + a confidence; (3) when a Claude pass
    is exercised (mock the client), logTokenUsage is called once with the service-inference feature.
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/serviceInference.test.ts</automated>
  </verify>
  <done>inferServices returns a clean, deduped, provenance-flagged services list from baseline pages; Claude path logs tokens; pure helpers tested.</done>
</task>

<task type="auto">
  <name>Task 2: infer_services + save_services actions</name>
  <files>src/app/api/kotoiq/route.ts</files>
  <action>
    Add two actions to the `/api/kotoiq` dispatcher in the existing if-chain style (append; do not refactor
    the monolith). `infer_services` {client_id, agency_id}: loads the client's latest baseline pages
    (kotoiq_site_baseline from 11-02), calls inferServices, returns the flagged list (does NOT auto-confirm).
    `save_services` {client_id, agency_id, services[]}: persists the user-confirmed services to
    kotoiq_client_profile.fields via saveConfirmedServices (provenance: user-confirmed where edited,
    ai_inferred where untouched). Add an optional `derive_phrases` {client_id, agency_id, service}: returns
    target phrases for a service from `kotoiq_keywords` (GSC + DataForSEO already populate it) filtered to the
    service, each carrying source/fetched_at (data-integrity). Add all three to ALLOWED_ACTIONS. Follow the
    canonical route shape (verifySession → agency gate → action) and read `node_modules/next/dist/docs/`
    route-handlers guide before editing the handler (AGENTS.md — this Next.js differs from training data).
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "api/kotoiq/route" || echo "no type errors in kotoiq route"</automated>
  </verify>
  <done>infer_services returns the flagged list, save_services persists confirmed services with provenance, derive_phrases returns timestamped phrases; all gated + in ALLOWED_ACTIONS.</done>
</task>

<task type="auto">
  <name>Task 3: ServiceChips editable component</name>
  <files>src/components/kotoiq/ServiceChips.jsx</files>
  <action>
    Build `ServiceChips` ("use client") using DESIGN.md koto/* primitives (Pattern 8 FlagChip) — NOT the old
    `src/lib/theme` #111/FH/FB tokens (Pitfall 6). On mount it calls `infer_services` and pre-seeds editable
    add/remove chips from the real site; AI-inferred chips render an "AI-inferred" badge (violet/info variant,
    mirror AEOVisibilityTab AI chip @618) so the user knows to verify (data-integrity). Add: remove a chip,
    add a chip (text input → new chip, marked user-added), and a single primary "Confirm services" action that
    POSTs `save_services`. Optionally show per-service phrase pins via `derive_phrases` with manual add. Read
    DESIGN.md before any visual decision; one primary action per the WS7 spine convention. This component is
    consumed by the 11-06 guided shell (step "Your site today"/"Your gaps" handoff) and replaces the manual
    services box in PageSuggestionsTab's flow.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ServiceChips" || echo "ServiceChips: no type errors"</automated>
  </verify>
  <done>ServiceChips renders AI-flagged, add/removable chips seeded from infer_services, supports manual phrase pins, and confirms via save_services — built on DESIGN.md primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/kotoiq | infer_services/save_services with agency_id |
| dashboard → Claude API | service inference over client page text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Info disclosure | cross-agency service read/write | mitigate | verifySession + agency gate on both actions; profile writes auto-scope agency_id (Phase 7 helper) |
| T-11-09 | Tampering | unverified AI services driving builds | mitigate | Flag ai_inferred in chips; user must confirm before services feed WS5 (data-integrity standard) |
| T-11-10 | Spoofing | save_services with arbitrary client_id | mitigate | AUTH-gate client ownership check (kotoiq route @455-463 pattern) |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/serviceInference.test.ts` green
- `npx tsc --noEmit` clean for touched files
- Manual: ServiceChips loads inferred services from a baselined client, AI badge visible, confirm persists
</verification>

<success_criteria>
- Services inferred from the client's own pages, provenance-flagged AI-inferred.
- Editable chips pre-seeded from the real site; confirmed services persist and feed WS5.
- Optional phrases auto-derive from kotoiq_keywords + manual pins, timestamped.
- Every Claude call logs via logTokenUsage (Haiku).
</success_criteria>

<output>
After completion, create `.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-03-SUMMARY.md`
</output>
