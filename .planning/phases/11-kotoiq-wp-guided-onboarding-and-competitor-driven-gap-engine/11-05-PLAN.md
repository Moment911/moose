---
phase: 11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine
plan: 05
type: execute
wave: 4
depends_on: ["11-03", "11-04"]
files_modified:
  - supabase/migrations/20260608_kotoiq_page_suggestions_gap_score.sql
  - src/lib/builder/scoreServiceCityGrid.ts
  - src/lib/wp-shim/computeInternalLinks.ts
  - src/app/api/kotoiq/route.ts
  - src/app/api/kotoiq/topic-campaign/route.ts
  - tests/kotoiq/scoreServiceCityGrid.test.ts
  - tests/kotoiq/computeInternalLinks.test.ts
autonomous: true
requirements: [ONBOARD-06, ONBOARD-07]
must_haves:
  truths:
    - "scoreServiceCityGrid produces a ranked build order with an explicit (demand + competition_strength) × (1 − our_coverage) ÷ difficulty score per service×city cell"
    - "Each cell is bucketed quick-win / net-new / big-bet; a client city with no competitor target is surfaced (quick win or low-demand deprioritize) with a reason"
    - "Competitor ranks + difficulty carry source + fetched_at (data-integrity)"
    - "Internal-link computation is extracted into a shared helper and applied on Page Factory builds (approval gate retained)"
  artifacts:
    - path: "src/lib/builder/scoreServiceCityGrid.ts"
      provides: "Gap-scoring wrapper over analyzePageGaps signals + bucketing; exported weight constants"
      exports: ["scoreServiceCityGrid", "SCORE_WEIGHTS"]
    - path: "src/lib/wp-shim/computeInternalLinks.ts"
      provides: "Extracted sibling/cross/hub link computation reusable by Page Factory + deployCampaign"
      exports: ["computeInternalLinks"]
    - path: "supabase/migrations/20260608_kotoiq_page_suggestions_gap_score.sql"
      provides: "score/bucket/our_coverage/competition_strength columns on kotoiq_page_suggestions (manual-apply)"
      contains: "ALTER TABLE kotoiq_page_suggestions"
  key_links:
    - from: "src/lib/builder/scoreServiceCityGrid.ts"
      to: "analyzePageGaps signals (volume, competitor rank, coverage, difficulty)"
      via: "calls analyzePageGaps / its loaders"
      pattern: "analyzePageGaps"
    - from: "src/app/api/kotoiq/topic-campaign/route.ts (deployCampaign)"
      to: "computeInternalLinks"
      via: "shared helper replaces inline sibling/cross computation"
      pattern: "computeInternalLinks\\("
---

<objective>
Build WS5 + WS6. WS5: a new `scoreServiceCityGrid()` wrapper that re-expresses the signals
`analyzePageGaps` already computes as the explicit formula
`score = (demand + competition_strength) × (1 − our_coverage) ÷ difficulty`, with quick-win /
net-new / big-bet bucketing and the "client city, no competitor" surfacing. WS6: extract the
sibling/cross/hub internal-link computation that already runs in `deployCampaign` into a shared
helper and apply it on Page Factory builds — approval gate retained, machinery reused not rebuilt.

Purpose: Replaces generic best-practice strategy with a competitor-rank-driven ranked build order,
and auto-links what gets built.
Output: gap-score columns migration + `scoreServiceCityGrid` + `computeInternalLinks` + new action + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-CONTEXT.md
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-RESEARCH.md
@_knowledge/data-integrity-standard.md

# WS5 signal sources (read before implementing):
@src/lib/builder/pageGapEngine.ts   # analyzePageGaps @86; PageSuggestion @25; additive priority @197-255; existingPageIndex @117-118; DataForSEO local-volume re-score @294-355
# Competitor rank (authoritative — RESEARCH WS5 A5): analyze_competitors @1398 (serpUrls rank=rank_group @1430), dfs_compare @5256, grid-scan / kotoiq_grid_scans_pro
# Coverage degree inputs: kotoiq_content_inventory.sc_position (ranking), kotoiq_semantic_analysis.thin_content_pages (thin)

# WS6 link machinery (VERIFIED — reuse, do NOT rebuild):
@src/app/api/kotoiq/topic-campaign/route.ts  # deployCampaign @1698; siblingLinks @1770; crossByCity @1779; redeploy siblings @2060
@src/lib/wp-shim/tokenResolver.ts             # ctx.siblingLinks @45/589 (self-filter); ctx.hub @1120 (BreadcrumbList JSON-LD ONLY — schema, never post body)
@src/lib/wp-shim/hubBuilder.ts                # buildHubPage() pure fn

<interfaces>
<!-- The formula (CONTEXT WS5). Compute from RAW signals — do NOT bolt onto the additive priority (Pitfall 3). -->
<!-- demand ← search_volume (DataForSEO local when enriched, else kotoiq_keywords) + client-targets-it + derived phrase volume
     competition_strength ← real competitor rank set (analyze_competitors/dfs_compare rank_group + grid-scan), NOT coarse snapshot count
     our_coverage ← 0..1 degree: no page=0, strong ranking page=1, thin/mid page=partial (sc_position in [8,25] = quick-win partial)
     difficulty ← keyword_difficulty (100 - opportunity_score, or DataForSEO KD) -->
<!-- buckets: quick_win (existing page ranks ~8-25), net_new (competitors cover, our_coverage≈0), big_bet (high demand×difficulty, our_coverage≈0).
     Special: client-listed city w/ no competitor target → quick_win OR low_demand_deprioritize, with reason string. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — gap-score columns on kotoiq_page_suggestions (manual-apply)</name>
  <files>supabase/migrations/20260608_kotoiq_page_suggestions_gap_score.sql</files>
  <action>
    Write a migration ALTERing `kotoiq_page_suggestions` to ADD COLUMN IF NOT EXISTS: `score numeric`,
    `bucket text`, `our_coverage numeric`, `competition_strength numeric`, and a `score_sources jsonb`
    (to hold the source_url/fetched_at for competitor-rank + difficulty inputs per data-integrity). Keep the
    existing additive `priority` column for back-compat (RESEARCH A4 — PageSuggestionsTab reads it). Re-runnable
    (IF NOT EXISTS). Top-of-file comment: "APPLY MANUALLY via Supabase SQL editor — never `supabase db push`."
    Append to the pending-bundle aggregate if the repo keeps one (mirror 11-02 Task 1). Do NOT run any push.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260608_kotoiq_page_suggestions_gap_score.sql && grep -q "ALTER TABLE kotoiq_page_suggestions" supabase/migrations/20260608_kotoiq_page_suggestions_gap_score.sql && grep -qi "APPLY MANUALLY" supabase/migrations/20260608_kotoiq_page_suggestions_gap_score.sql && echo OK</automated>
  </verify>
  <done>Migration adds score/bucket/our_coverage/competition_strength/score_sources, keeps priority, manual-apply notice present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scoreServiceCityGrid wrapper + bucketing</name>
  <files>src/lib/builder/scoreServiceCityGrid.ts, tests/kotoiq/scoreServiceCityGrid.test.ts</files>
  <behavior>
    - scoreServiceCityGrid({agencyId, clientId, services, state, cities}) gathers signals via analyzePageGaps (reusing 11-04's cities[] scoping) plus the authoritative competitor-rank source (analyze_competitors/dfs_compare/grid-scan), then computes per cell: score = (demand + competition_strength) × (1 − our_coverage) ÷ max(difficulty, MIN_DIFFICULTY).
    - our_coverage is a 0..1 degree: no page = 0; strong ranking page = 1; thin or sc_position-in-[8,25] page = partial (e.g. 0.4) → quick-win.
    - Buckets each cell: quick_win | net_new | big_bet. A client-listed city with zero competitor target → quick_win OR low_demand_deprioritize, each carrying a human-readable reason string.
    - Competitor-rank + difficulty inputs carry {source, fetched_at} (wrapped per createVerifiedData) in score_sources.
    - SCORE_WEIGHTS + MIN_DIFFICULTY exported as tunable constants. The scoring + bucketing is a PURE function over a signals object (no IO) so it unit-tests on fixtures.
  </behavior>
  <action>
    Implement `scoreServiceCityGrid` in `src/lib/builder/scoreServiceCityGrid.ts` as a NEW wrapper (RESEARCH A4)
    — it calls analyzePageGaps (or its loaders) for signals, then a PURE `computeCellScore(signals)` +
    `bucketCell(scored)` do the math. Do NOT re-blend the additive priority (Pitfall 3) — compute from raw
    volume / competitor-rank set / coverage-degree / KD. Pull the competitor rank set from the authoritative
    sources (RESEARCH A5), not the coarse snapshot count. Wrap rank/difficulty facts with createVerifiedData
    (source_url + fetched_at) per the standard. Persist results to the new kotoiq_page_suggestions columns
    (Task 1) while leaving priority intact. Export SCORE_WEIGHTS + MIN_DIFFICULTY. Add a new
    `score_grid` action to `/api/kotoiq/route.ts` (append to the if-chain + ALLOWED_ACTIONS; canonical route
    shape) returning the bucketed report ("N opportunities — X quick wins, Y net-new, Z big bets").

    Tests (pure-fn, fixtures): (1) formula correctness on known signals; (2) quick_win when sc_position∈[8,25];
    (3) net_new when competition_strength>0 and our_coverage≈0; (4) big_bet on high demand×difficulty,
    coverage≈0; (5) client city, no competitor → low_demand_deprioritize with reason; (6) difficulty=0 does not
    divide-by-zero (MIN_DIFFICULTY floor).
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/scoreServiceCityGrid.test.ts</automated>
  </verify>
  <done>scoreServiceCityGrid emits the explicit formula + buckets per cell, surfaces no-competitor cities with reasons, wraps rank/difficulty facts with provenance; pure math unit-tested; score_grid action exposed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract computeInternalLinks + apply on Page Factory build</name>
  <files>src/lib/wp-shim/computeInternalLinks.ts, src/app/api/kotoiq/topic-campaign/route.ts, tests/kotoiq/computeInternalLinks.test.ts</files>
  <behavior>
    - computeInternalLinks(supabase, {siteId, campaignId|serviceKey, locations}) returns {siblingLinks, crossByCity, hub?} — the same shape deployCampaign currently builds inline (@1745-1785): siblingLinks deduped by city, cross-campaign related-services by city, and hub context when a hub exists.
    - It is a pure-ish helper (DB reads only, no writes) so it can be unit-tested against a mocked supabase.
    - deployCampaign is refactored to CALL computeInternalLinks instead of its inline block — output must be byte-identical (no behavior change to existing deploys).
  </behavior>
  <action>
    Extract the sibling/cross/hub computation from `deployCampaign` (@1745-1785, and the redeploy block @2060)
    into `src/lib/wp-shim/computeInternalLinks.ts`. Refactor deployCampaign + redeploy to call it — behavior
    MUST be unchanged (same siblingLinks/crossByCity passed into tokenResolver ctx). Then make it callable from
    the Page Factory build/publish path so factory pages get the same sibling + cross + hub links via the
    existing tokenResolver (reuse buildHubPage/deploy_hub for the pillar). BreadcrumbList stays schema-only —
    never inject into post body (Pitfall: KSES). KEEP the approval gate before publish (CONTEXT deferred: no
    auto-publish). Do NOT rebuild the link injector (RESEARCH Don't-Hand-Roll — this is the least new work).

    Tests: (1) given fixture published deploys, computeInternalLinks returns sibling links deduped by city +
    self-excluded; (2) cross-campaign related-services grouped by city; (3) hub context present only when a hub
    row exists. Plus a smoke assertion that deployCampaign's call produces the same shape as the old inline path
    (snapshot on a fixture).
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/computeInternalLinks.test.ts && npx tsc --noEmit 2>&1 | grep -E "topic-campaign/route|computeInternalLinks" || echo "topic-campaign + helper: no type errors"</automated>
  </verify>
  <done>computeInternalLinks extracted + consumed by deployCampaign unchanged, and applied on Page Factory builds; breadcrumbs schema-only; approval gate retained.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard → DataForSEO / Google Places | Live competitor rank + difficulty fetch |
| browser → /api/kotoiq score_grid | Gap report request with agency_id |
| dashboard → WP (publish) | Page Factory build with injected links (approval-gated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering | stale/fabricated competitor ranks | mitigate | createVerifiedData wrap (source_url + fetched_at); live DataForSEO/Places only, never recalled |
| T-11-14 | Info disclosure | cross-agency gap data | mitigate | verifySession + agency gate on score_grid; suggestions table RLS via agency_members |
| T-11-15 | Tampering | auto-publish bypassing review | mitigate | Approval gate retained before any factory publish (CONTEXT deferred) |
| T-11-16 | Tampering | divide-by-zero / score blow-up on difficulty=0 | mitigate | MIN_DIFFICULTY floor in computeCellScore |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/scoreServiceCityGrid.test.ts tests/kotoiq/computeInternalLinks.test.ts` green
- `npx tsc --noEmit` clean for touched files
- Both migrations applied manually by operator before pilot
- Manual: deployCampaign still deploys with identical sibling/cross links after the extraction
</verification>

<success_criteria>
- Ranked build order via the explicit formula + quick-win/net-new/big-bet buckets; no-competitor cities surfaced with reasons.
- Competitor ranks/difficulty carry source + fetched_at.
- Internal-link computation shared; Page Factory builds auto-link; approval gate retained.
- priority kept for back-compat; new columns added via manual-apply migration.
</success_criteria>

<output>
After completion, create `.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-05-SUMMARY.md`
</output>
