---
phase: 11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - supabase/migrations/20260608_kotoiq_site_baseline.sql
  - src/lib/kotoiq/baselineSnapshot.ts
  - tests/kotoiq/baselineSnapshot.test.ts
autonomous: true
requirements: [ONBOARD-03]
must_haves:
  truths:
    - "On first scan, a day-1 immutable inventory of the client's OWN pages is captured (url, title, h1, type, word count, content hash)"
    - "The baseline table is insert-only — later scans diff against the latest baseline row per URL, never mutate it"
    - "Snapshot capture reuses pageDiscovery + pageContentExtractor (no new crawler)"
  artifacts:
    - path: "supabase/migrations/20260608_kotoiq_site_baseline.sql"
      provides: "kotoiq_site_baseline table + indexes + RLS (manual-apply)"
      contains: "CREATE TABLE IF NOT EXISTS kotoiq_site_baseline"
    - path: "src/lib/kotoiq/baselineSnapshot.ts"
      provides: "captureBaseline() + diffAgainstBaseline() pure-ish engine over existing discovery/extract"
      exports: ["captureBaseline", "diffAgainstBaseline"]
  key_links:
    - from: "src/lib/kotoiq/baselineSnapshot.ts"
      to: "src/lib/kotoiq/pageDiscovery.ts + pageContentExtractor.ts"
      via: "discoverPages / fetchAndExtract reuse"
      pattern: "discoverPages|fetchAndExtract"
    - from: "src/lib/kotoiq/orchestrateOnboarding.ts (11-01)"
      to: "captureBaseline"
      via: "dynamic import in the onboarding chain"
      pattern: "captureBaseline\\("
---

<objective>
Build the WS2 day-1 baseline snapshot: an immutable inventory of the client's OWN pages, captured
once at onboarding, that later scans diff against. Ship a manual-apply migration for a new
`kotoiq_site_baseline` table and a snapshot engine that reuses `pageDiscovery` + `pageContentExtractor`.

Purpose: Gives "Your site today" (step 2) real numbers and feeds WS3 service inference + WS5 coverage.
Output: migration file + `captureBaseline()`/`diffAgainstBaseline()` engine + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-CONTEXT.md
@.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-RESEARCH.md
@_knowledge/data-integrity-standard.md

# Reuse targets (read before implementing):
@src/lib/kotoiq/pageDiscovery.ts        # discoverPages(domain) → DiscoveredPage[] (default cap ~5 — raise/bypass for full baseline; see RESEARCH WS2)
@src/lib/kotoiq/pageContentExtractor.ts # fetchAndExtract(url) → ExtractedPage {url, h1, meta_title, word_count, content_hash, schema_orgs...}
# RLS pattern to mirror (VERIFIED):
@supabase/migrations/20260608_page_factory.sql  # agency_isolation policy via agency_members @37-45

<interfaces>
<!-- Proposed table shape (RESEARCH WS2; column names = Claude's discretion). Insert-only. -->
```sql
kotoiq_site_baseline (
  id uuid PK default gen_random_uuid(),
  agency_id uuid NOT NULL REFERENCES agencies(id) ON DELETE CASCADE,
  client_id uuid NOT NULL REFERENCES clients(id) ON DELETE CASCADE,
  site_id uuid,                  -- koto_wp_sites.id (nullable; pre-pair scans)
  url text NOT NULL,
  page_type text,                -- inferPageType() output
  title text,                    -- meta_title
  h1 text,
  word_count int,
  content_hash text NOT NULL,    -- ExtractedPage.content_hash (SHA-256)
  captured_at timestamptz DEFAULT now(),
  UNIQUE (client_id, url, captured_at)
)
-- indexes: (client_id, captured_at desc), (client_id, url)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — kotoiq_site_baseline (manual-apply)</name>
  <files>supabase/migrations/20260608_kotoiq_site_baseline.sql</files>
  <action>
    Write the migration creating `kotoiq_site_baseline` per the interface shape above. Mirror the
    `agency_isolation` RLS policy from `20260608_page_factory.sql` lines 37-45 (agency_members → auth.uid()).
    Add the two indexes. Use `CREATE TABLE IF NOT EXISTS` + `DROP POLICY IF EXISTS` guards so the file is
    re-runnable. Add a top-of-file comment block: "APPLY MANUALLY via Supabase SQL editor — do NOT run
    `supabase db push` (prod has tracking drift). Paste this file's contents into the SQL editor." Check whether
    the repo keeps a pending-bundle aggregate (`_pending_bundle.sql` / `_paste_all_pending.sql`) and, if present,
    append a pointer there too (RESEARCH WS2). Do NOT run any push command.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260608_kotoiq_site_baseline.sql && grep -q "kotoiq_site_baseline" supabase/migrations/20260608_kotoiq_site_baseline.sql && grep -qi "APPLY MANUALLY" supabase/migrations/20260608_kotoiq_site_baseline.sql && echo OK</automated>
  </verify>
  <done>Migration file exists with the table, indexes, agency_members RLS, and a manual-apply notice. No db push run.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: baselineSnapshot engine — capture + diff</name>
  <files>src/lib/kotoiq/baselineSnapshot.ts, tests/kotoiq/baselineSnapshot.test.ts</files>
  <behavior>
    - captureBaseline({agencyId, clientId, siteId, domain}): discovers the client's own pages, extracts each (h1/title/word_count/content_hash/page_type), and INSERTs one row per URL into kotoiq_site_baseline. Insert-only — never UPDATE/DELETE existing baseline rows.
    - For a full baseline it must NOT silently cap at the competitor-quick-look default (~5); raise/bypass the cap so the real site inventory is captured (RESEARCH WS2).
    - diffAgainstBaseline({clientId, url, currentHash}): compares a current content_hash against the latest baseline row for that (client_id, url) and returns {changed: boolean, baseline_hash, captured_at} — pure comparison, no writes.
    - The page-extraction/diff logic is structured as pure helpers (parse/compare) testable without DB/network (mirror pageContentExtractor.extractFromHtml + hubBuilder pure-fn style).
  </behavior>
  <action>
    Implement `captureBaseline` + `diffAgainstBaseline` in `src/lib/kotoiq/baselineSnapshot.ts`. Reuse
    `discoverPages` (pageDiscovery) and `fetchAndExtract` (pageContentExtractor) — do NOT hand-roll a crawler.
    Constrain fetches to the client's own paired domain / sitemap origin (ASVS — SSRF guard, RESEARCH security:
    Phase 8 precedent). Persist via the agency-scoped DB pattern: `kotoiq_site_baseline` is NOT in
    DIRECT_AGENCY_TABLES, so pass agency_id explicitly on insert (mirror `pageGapEngine.saveSuggestions`).
    Keep the hash-compare + row-shaping as pure exported helpers.

    Tests: (1) given fixture ExtractedPage objects, the engine produces correctly-shaped insert rows
    (url/title/h1/word_count/content_hash/page_type); (2) diffAgainstBaseline returns changed:true when
    hashes differ and changed:false when equal; (3) immutability — capture never issues an UPDATE/DELETE
    (assert the DB mock only saw inserts).
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/baselineSnapshot.test.ts</automated>
  </verify>
  <done>captureBaseline inserts an immutable per-URL inventory reusing existing discovery/extract; diffAgainstBaseline compares against the latest baseline; pure helpers unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard → client website | Outbound fetch of the client's own pages for discovery/extraction |
| dashboard → Supabase | Insert into a new agency-scoped table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05 | Tampering (SSRF) | captureBaseline fetching arbitrary URLs | mitigate | Constrain fetches to clients.website / sitemap origin (Phase 8 SSRF precedent) |
| T-11-06 | Info disclosure | cross-agency baseline rows | mitigate | RLS via agency_members + explicit agency_id on every insert |
| T-11-07 | Tampering | baseline row mutation breaking immutability | mitigate | Engine only INSERTs; unique(client_id,url,captured_at); diff reads latest, never updates |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/baselineSnapshot.test.ts` green
- Migration file present + manual-apply notice; operator applies it via SQL editor before pilot
- `npx tsc --noEmit` clean for touched files
</verification>

<success_criteria>
- Immutable day-1 baseline captured via reused discovery/extract engines.
- Insert-only; diff compares current hashes against the latest baseline per URL.
- Migration shipped as a file with a manual-apply notice (no db push).
</success_criteria>

<output>
After completion, create `.planning/phases/11-kotoiq-wp-guided-onboarding-and-competitor-driven-gap-engine/11-02-SUMMARY.md`
</output>
