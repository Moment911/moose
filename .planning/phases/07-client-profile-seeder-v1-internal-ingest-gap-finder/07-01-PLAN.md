---
phase: 07-client-profile-seeder-v1-internal-ingest-gap-finder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260507_kotoiq_client_profile.sql
  - src/lib/kotoiqDb.ts
  - src/lib/theme.ts
  - src/lib/kotoiq/profileTypes.ts
  - src/lib/kotoClientPick.ts
  - src/views/KotoProposalBuilderPage.jsx
  - package.json
  - vitest.config.ts
  - tests/fixtures/profiles.ts
  - tests/fixtures/anthropicMock.ts
  - tests/profileTypes.test.ts
autonomous: false
risk_level: medium
requirements: [PROF-03, PROF-04]
must_haves:
  truths:
    - "Operator can run supabase db push and the new migration applies cleanly"
    - "Every new kotoiq_* table enforces agency_id via getKotoIQDb(agencyId) helper"
    - "Every field in kotoiq_client_profile can carry the full VerifiedDataSource provenance quintet"
    - "npm test runs at least one real test file to green"
    - "ESLint still flags direct supabase.from('kotoiq_*') on the two new tables"
    - "kotoiq_client_profile has a margin_notes jsonb column + ClientProfile type exposes margin_notes array for D-10 proactive Claude observations"
  artifacts:
    - path: "supabase/migrations/20260507_kotoiq_client_profile.sql"
      provides: "kotoiq_client_profile + kotoiq_clarifications tables with RLS + realtime publication"
      contains: "CREATE TABLE kotoiq_client_profile"
    - path: "src/lib/kotoiqDb.ts"
      provides: "Extended with clientProfile + clarifications typed helpers"
      contains: "clientProfile"
    - path: "src/lib/theme.ts"
      provides: "DST = '#DC2626' destructive token"
      contains: "export const DST"
    - path: "src/lib/kotoiq/profileTypes.ts"
      provides: "ClientProfile, Clarification, ProvenanceRecord, EntityGraphSeed types"
      contains: "export type ProvenanceRecord"
    - path: "src/lib/kotoClientPick.ts"
      provides: "Shared pick(client, ...keys) helper extracted from KotoProposalBuilderPage"
      contains: "export function pick"
    - path: "vitest.config.ts"
      provides: "Vitest test runner configuration"
      contains: "defineConfig"
    - path: "tests/fixtures/profiles.ts"
      provides: "Golden-file profile fixtures"
      contains: "COMPLETE_PROFILE"
  key_links:
    - from: "src/lib/kotoiqDb.ts DIRECT_AGENCY_TABLES set"
      to: "kotoiq_client_profile + kotoiq_clarifications"
      via: "table name added to Set"
      pattern: "kotoiq_client_profile.*kotoiq_clarifications"
    - from: "src/views/KotoProposalBuilderPage.jsx"
      to: "src/lib/kotoClientPick.ts"
      via: "import { pick } from ...kotoClientPick"
      pattern: "from.*kotoClientPick"
---

<objective>
Land the data layer + test framework + shared helpers every downstream plan depends on.

Purpose:
- Phase 7 needs a new authoritative profile table with per-field provenance (D-01..D-05, PROF-03).
- Phase 7 introduces the first Vitest suite in the Koto repo — research §14 recommended installing Vitest because the seeder has many pure-logic libraries worth unit-testing.
- Phase 7 adds a shared pick() helper (currently inside KotoProposalBuilderPage.jsx:47) so Plan 2's ingest pullers can use it without duplication.

Output:
- Migration 20260507_kotoiq_client_profile.sql applied to live Supabase.
- src/lib/kotoiqDb.ts gains typed helpers db.clientProfile.* and db.clarifications.*.
- src/lib/theme.ts gains DST = '#DC2626' (UI-SPEC §3 destructive token).
- src/lib/kotoiq/profileTypes.ts exports every type the phase consumes.
- src/lib/kotoClientPick.ts becomes the canonical home of pick().
- Vitest installed + one real passing test exercising the type contracts + fixtures ready for Plans 2-5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-client-profile-seeder-v1-internal-ingest-gap-finder/07-CONTEXT.md
@.planning/phases/07-client-profile-seeder-v1-internal-ingest-gap-finder/07-RESEARCH.md
@.planning/phases/07-client-profile-seeder-v1-internal-ingest-gap-finder/07-UI-SPEC.md
@.planning/phases/07-client-profile-seeder-v1-internal-ingest-gap-finder/07-VALIDATION.md
@CLAUDE.md
@AGENTS.md
@_knowledge/data-integrity-standard.md
@src/lib/kotoiqDb.ts
@src/lib/theme.ts
@supabase/migrations/20260505_kotoiq_builder.sql
@src/lib/dataIntegrity.ts
@src/views/KotoProposalBuilderPage.jsx

<interfaces>
Extracted from src/lib/kotoiqDb.ts — preserve these shapes when extending:

```
export function getKotoIQDb(agencyId: string): {
  raw(): SupabaseClient,
  templates: { list, get, create, update, delete, slots: { ... } },
  campaigns: { list, get, create, update },
  // Phase 7 will add:
  //   clientProfile: { get, upsert, updateField, addField, deleteField, list, markLaunched }
  //   clarifications: { list, get, create, update, markAnswered, markForwarded, markSkipped }
}
```

DIRECT_AGENCY_TABLES set (around line 39) — add kotoiq_client_profile + kotoiq_clarifications.

Extracted from src/lib/dataIntegrity.ts (line 100):

```
export function createVerifiedData<T>(
  data: T,
  meta: {
    source_url: string
    source_name: string
    source_type: 'api' | 'scraped' | 'user-provided' | 'ai-inferred' | 'computed' | 'static'
    fetched_at: string
    expires_at?: string
    cross_referenced?: boolean
    ai_generated?: boolean
    confidence: 'authoritative' | 'cross-referenced' | 'single-source' | 'ai-inferred'
  }
): VerifiedDataSource<T>
```

Pattern from supabase/migrations/20260505_kotoiq_builder.sql — copy this RLS style:

```
ALTER TABLE kotoiq_templates ENABLE ROW LEVEL SECURITY;
CREATE POLICY "kotoiq_templates_all" ON kotoiq_templates
  FOR ALL USING (true) WITH CHECK (true);
```

pick() signature (from KotoProposalBuilderPage.jsx:47 — relocate verbatim):

```
function pick(client, ...keys): string | null
```

Resolves across client[key], client.onboarding_answers[key], client.onboarding_data[key]. First non-empty wins. Arrays joined with ', '.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — kotoiq_client_profile + kotoiq_clarifications</name>
  <files>supabase/migrations/20260507_kotoiq_client_profile.sql</files>
  <read_first>
    - 07-RESEARCH.md §2 (lines ~366-496) — full DDL sketch
    - supabase/migrations/20260505_kotoiq_builder.sql — RLS policy + trigger pattern to mirror (DO NOT improvise)
    - AGENTS.md — the general prohibition on improvising framework patterns applies to SQL too: copy 20260505 style verbatim
  </read_first>
  <action>
Create supabase/migrations/20260507_kotoiq_client_profile.sql with the exact DDL from RESEARCH §2. The migration MUST contain, in this order:

1. Comment header referencing Phase 7 + D-01..D-05, D-11, D-16, D-22.

2. CREATE TABLE IF NOT EXISTS kotoiq_client_profile with columns:
   - id uuid PRIMARY KEY DEFAULT gen_random_uuid()
   - agency_id uuid NOT NULL
   - client_id uuid NOT NULL REFERENCES clients(id) ON DELETE CASCADE
   - Hot columns (D-02): business_name text, website text, primary_service text, target_customer text, service_area text, phone text, founding_year int, unique_selling_prop text, industry text, city text, state text
   - fields jsonb NOT NULL DEFAULT '{}'::jsonb — with a SQL comment block documenting the shape {[field_name]: ProvenanceRecord[]} where ProvenanceRecord has value, source_type, source_url, source_ref, source_snippet, char_offset_start?, char_offset_end?, captured_at, confidence, edit_history?
   - entity_graph_seed jsonb NOT NULL DEFAULT '{}'::jsonb (D-22 contract)
   - completeness_score numeric(4,3), completeness_reasoning text, soft_gaps jsonb DEFAULT '[]'::jsonb
   - margin_notes jsonb NOT NULL DEFAULT '[]'::jsonb — D-10 proactive Claude observations; shape per margin note: { id, field_path, question, suggested_value?, source_ref, created_at, status: 'pending' | 'accepted' | 'rejected' | 'edited' }
   - sources jsonb DEFAULT '[]'::jsonb
   - last_seeded_at timestamptz, last_edited_at timestamptz, launched_at timestamptz, last_pipeline_run_id text
   - created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()
   - CONSTRAINT uq_profile_per_client_agency UNIQUE (agency_id, client_id)

3. Three indexes on kotoiq_client_profile:
   - idx_kotoiq_profile_agency_client (agency_id, client_id)
   - idx_kotoiq_profile_agency_launched (agency_id, launched_at DESC)
   - idx_kotoiq_profile_business_name (agency_id, business_name)

4. RLS: ALTER TABLE kotoiq_client_profile ENABLE ROW LEVEL SECURITY; plus policy "kotoiq_client_profile_all" FOR ALL USING (true) WITH CHECK (true) — service-role only; app-layer scoping via getKotoIQDb — mirrors 20260505 pattern.

5. CREATE TABLE IF NOT EXISTS kotoiq_clarifications with columns:
   - id uuid PRIMARY KEY DEFAULT gen_random_uuid()
   - agency_id uuid NOT NULL
   - client_id uuid NOT NULL REFERENCES clients(id) ON DELETE CASCADE
   - profile_id uuid REFERENCES kotoiq_client_profile(id) ON DELETE CASCADE
   - question text NOT NULL
   - reason text
   - target_field_path text
   - severity text NOT NULL CHECK (severity IN ('low','medium','high'))
   - status text NOT NULL DEFAULT 'open' CHECK (status IN ('open','asked_client','answered','skipped'))
   - asked_channel text CHECK (asked_channel IN ('sms','email','portal','operator'))
   - asked_at timestamptz, answered_at timestamptz, answer_text text, answered_by text
   - impact_hint text, impact_unlocks jsonb DEFAULT '[]'::jsonb
   - created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now()

6. Three indexes on kotoiq_clarifications: (agency_id, client_id, status), (agency_id, severity, created_at DESC), (profile_id).

7. RLS: same pattern as above.

8. Realtime publication (idempotent — wrap in DO block ignoring "already member" if 20260505 shows this idiom): ALTER PUBLICATION supabase_realtime ADD TABLE kotoiq_clarifications; and ALTER PUBLICATION supabase_realtime ADD TABLE kotoiq_pipeline_runs; (D-23 ribbon).

9. updated_at triggers on both tables — use the repo's existing trigger function. Grep 20260505_kotoiq_builder.sql for the function name (likely set_updated_at or update_updated_at_column) and reuse it. Do NOT create a new trigger function.

Do NOT add foreign keys from fields.provenance.source_ref — provenance is jsonb only. Do NOT add computed columns. Do NOT seed any data.
  </action>
  <verify>
    <automated>cd /Users/adamsegall/Desktop/moose &amp;&amp; test -f supabase/migrations/20260507_kotoiq_client_profile.sql &amp;&amp; grep -q 'CREATE TABLE IF NOT EXISTS kotoiq_client_profile' supabase/migrations/20260507_kotoiq_client_profile.sql &amp;&amp; grep -q 'CREATE TABLE IF NOT EXISTS kotoiq_clarifications' supabase/migrations/20260507_kotoiq_client_profile.sql &amp;&amp; grep -q 'uq_profile_per_client_agency' supabase/migrations/20260507_kotoiq_client_profile.sql &amp;&amp; grep -q 'ALTER PUBLICATION supabase_realtime ADD TABLE kotoiq_clarifications' supabase/migrations/20260507_kotoiq_client_profile.sql &amp;&amp; grep -q 'margin_notes' supabase/migrations/20260507_kotoiq_client_profile.sql</automated>
  </verify>
  <acceptance_criteria>
    - File supabase/migrations/20260507_kotoiq_client_profile.sql exists
    - Contains `CREATE TABLE IF NOT EXISTS kotoiq_client_profile` exactly once
    - Contains `CREATE TABLE IF NOT EXISTS kotoiq_clarifications` exactly once
    - Contains `REFERENCES clients(id) ON DELETE CASCADE` on both tables
    - Contains `UNIQUE (agency_id, client_id)` on kotoiq_client_profile
    - Contains CHECK constraints for severity, status, asked_channel
    - Contains `ENABLE ROW LEVEL SECURITY` for both tables
    - Contains `ALTER PUBLICATION supabase_realtime ADD TABLE kotoiq_clarifications`
    - Contains `ALTER PUBLICATION supabase_realtime ADD TABLE kotoiq_pipeline_runs` (idempotent acceptable)
    - Contains exactly 6 index creation statements (3 per table)
    - kotoiq_client_profile column list includes `margin_notes jsonb NOT NULL DEFAULT '[]'::jsonb` (D-10 support)
    - Contains 2 CREATE TRIGGER statements reusing the repo's existing trigger function
  </acceptance_criteria>
  <done>Migration file exists with both tables, RLS, realtime, indexes, and triggers. Eyeball against §2 DDL passes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] supabase db push — apply migration to live DB</name>
  <action>Human operator runs `supabase db push` to apply migration 20260507_kotoiq_client_profile.sql to the live Supabase database. Confirms both tables + realtime publication membership exist before downstream plans proceed.</action>
  <what-built>Migration file from Task 1.</what-built>
  <how-to-verify>
This is a BLOCKING human-action task. Build + type checks pass without this step because Supabase types come from config, not the live DB. The migration must actually hit the live database before downstream plans read from the new tables.

Run in a terminal with SUPABASE_ACCESS_TOKEN exported (or interactively logged in):

```
cd /Users/adamsegall/Desktop/moose
supabase db push
```

If prompted about destructive changes: this migration is purely additive (`CREATE TABLE IF NOT EXISTS`, `ALTER PUBLICATION`) so no destructive operations should appear. If any do, STOP.

After push completes, verify both tables and realtime membership via Supabase Studio SQL editor:

```
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
  AND table_name IN ('kotoiq_client_profile','kotoiq_clarifications');

SELECT pubname, tablename
FROM pg_publication_tables
WHERE pubname = 'supabase_realtime'
  AND tablename IN ('kotoiq_clarifications','kotoiq_pipeline_runs');
```

Both queries must return 2 rows.
  </how-to-verify>
  <resume-signal>Type "applied" after confirming both tables + both publication rows are present in the live Supabase. If the push fails, paste the error and STOP.</resume-signal>
  <done>Both kotoiq_client_profile and kotoiq_clarifications exist in the live DB. Realtime publication includes kotoiq_clarifications and kotoiq_pipeline_runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: profileTypes.ts — shared types for the entire phase</name>
  <files>src/lib/kotoiq/profileTypes.ts, tests/profileTypes.test.ts</files>
  <read_first>
    - 07-CONTEXT.md — D-04 provenance quintet, D-22 entity graph seed contract
    - 07-RESEARCH.md §2 (table shape) + §1 Stage 0 contract + Pattern 3 (tool-use schema)
    - src/lib/dataIntegrity.ts — createVerifiedData signature, VerifiedDataSource shape
  </read_first>
  <behavior>
    - ProvenanceRecord type has the 9 fields from RESEARCH §2
    - SOURCE_TYPES exports exactly 7 values: onboarding_form, voice_call, discovery_doc, operator_edit, claude_inference, uploaded_doc, deferred_v2
    - ClientProfile mirrors the migration table
    - Clarification mirrors the clarifications table
    - EntityGraphSeed has exactly the 8 keys from D-22
    - CANONICAL_FIELD_NAMES includes every hot column from the migration + spillover/voice fields
    - Tests: 4 Vitest cases verifying the above invariants
  </behavior>
  <action>
Create src/lib/kotoiq/profileTypes.ts (new file). Start with `import 'server-only'` (matches src/lib/tokenTracker.ts pattern — RESEARCH §State of the Art).

Export:

```
import 'server-only'

export const SOURCE_TYPES = [
  'onboarding_form',
  'voice_call',
  'discovery_doc',
  'operator_edit',
  'claude_inference',
  'uploaded_doc',
  'deferred_v2',
] as const
export type SourceType = typeof SOURCE_TYPES[number]

export type ProvenanceRecord = {
  value: string | number | string[] | null
  source_type: SourceType
  source_url?: string
  source_ref?: string
  source_snippet?: string
  char_offset_start?: number
  char_offset_end?: number
  captured_at: string
  confidence: number
  edit_history?: Array<{ at: string; by: string; prev_value: any }>
}

export const CANONICAL_FIELD_NAMES = [
  'business_name','website','primary_service','target_customer','service_area',
  'phone','founding_year','unique_selling_prop','industry','city','state',
  'marketing_budget','welcome_statement','customer_pain_points','current_channels',
  'competitors','differentiators','pain_points','service_areas','trust_anchors',
  'expansion_signals','competitor_mentions','objections','pain_point_emphasis',
  'caller_sentiment','follow_up_flag',
] as const
export type CanonicalFieldName = typeof CANONICAL_FIELD_NAMES[number]

export type ClientProfile = {
  id: string
  agency_id: string
  client_id: string
  business_name: string | null
  website: string | null
  primary_service: string | null
  target_customer: string | null
  service_area: string | null
  phone: string | null
  founding_year: number | null
  unique_selling_prop: string | null
  industry: string | null
  city: string | null
  state: string | null
  fields: Record<string, ProvenanceRecord[]>
  entity_graph_seed: EntityGraphSeed | Record<string, never>
  completeness_score: number | null
  completeness_reasoning: string | null
  soft_gaps: Array<{ field: string; reason: string }>
  margin_notes: Array<{
    id: string
    field_path: string
    question: string
    suggested_value?: string | null
    source_ref: string
    created_at: string
    status: 'pending' | 'accepted' | 'rejected' | 'edited'
  }>
  sources: Array<{
    source_type: SourceType
    source_url?: string
    source_ref?: string
    added_at: string
    added_by: string
    metadata?: Record<string, any>
  }>
  last_seeded_at: string | null
  last_edited_at: string | null
  launched_at: string | null
  last_pipeline_run_id: string | null
  created_at: string
  updated_at: string
}

export type EntityGraphNode = {
  id: string
  label: string
  confidence: number
  source_refs: string[]
  metadata?: Record<string, any>
}

export type EntityGraphEdge = {
  from: string
  to: string
  kind: string
  confidence: number
  source_refs: string[]
}

export type EntityGraphSeed = {
  client_node: EntityGraphNode & { url?: string }
  service_nodes: EntityGraphNode[]
  audience_nodes: EntityGraphNode[]
  competitor_nodes: EntityGraphNode[]
  service_area_nodes: EntityGraphNode[]
  differentiator_edges: EntityGraphEdge[]
  trust_anchor_nodes: EntityGraphNode[]
  confidence_by_node: Record<string, number>
}

export type ClarificationSeverity = 'low' | 'medium' | 'high'
export type ClarificationStatus = 'open' | 'asked_client' | 'answered' | 'skipped'
export type ClarificationChannel = 'sms' | 'email' | 'portal' | 'operator'

export type Clarification = {
  id: string
  agency_id: string
  client_id: string
  profile_id: string | null
  question: string
  reason: string | null
  target_field_path: string | null
  severity: ClarificationSeverity
  status: ClarificationStatus
  asked_channel: ClarificationChannel | null
  asked_at: string | null
  answered_at: string | null
  answer_text: string | null
  answered_by: string | null
  impact_hint: string | null
  impact_unlocks: Array<{ stage: string; unit: string }>
  created_at: string
  updated_at: string
}

export type NarrationEvent =
  | { kind: 'line'; text: string }
  | { kind: 'error'; text: string }
  | { kind: 'done' }
```

Create tests/profileTypes.test.ts:

```
import { describe, it, expect } from 'vitest'
import {
  SOURCE_TYPES,
  CANONICAL_FIELD_NAMES,
  type ProvenanceRecord,
  type EntityGraphSeed,
} from '../src/lib/kotoiq/profileTypes'

describe('profileTypes', () => {
  it('SOURCE_TYPES has exactly 7 members', () => {
    expect(SOURCE_TYPES).toHaveLength(7)
    expect(SOURCE_TYPES).toContain('onboarding_form')
    expect(SOURCE_TYPES).toContain('voice_call')
    expect(SOURCE_TYPES).toContain('deferred_v2')
  })

  it('CANONICAL_FIELD_NAMES includes every hot column', () => {
    const HOT = ['business_name','website','primary_service','target_customer','service_area','phone','founding_year','unique_selling_prop','industry','city','state']
    for (const hc of HOT) expect(CANONICAL_FIELD_NAMES).toContain(hc)
  })

  it('ProvenanceRecord accepts the 9 documented fields', () => {
    const rec: ProvenanceRecord = {
      value: 'Google Ads management',
      source_type: 'onboarding_form',
      source_url: 'https://hellokoto.com/clients/abc',
      source_snippet: 'We do Google Ads for local service businesses.',
      char_offset_start: 0,
      char_offset_end: 50,
      captured_at: '2026-04-17T00:00:00Z',
      confidence: 0.95,
    }
    expect(rec.confidence).toBe(0.95)
  })

  it('EntityGraphSeed has exactly 8 D-22 keys', () => {
    const seed: EntityGraphSeed = {
      client_node: { id: 'c1', label: 'Unified', confidence: 1.0, source_refs: [] },
      service_nodes: [], audience_nodes: [], competitor_nodes: [],
      service_area_nodes: [], differentiator_edges: [], trust_anchor_nodes: [],
      confidence_by_node: {},
    }
    expect(Object.keys(seed).sort()).toEqual([
      'audience_nodes','client_node','competitor_nodes','confidence_by_node',
      'differentiator_edges','service_area_nodes','service_nodes','trust_anchor_nodes',
    ])
  })
})
```
  </action>
  <verify>
    <automated>cd /Users/adamsegall/Desktop/moose &amp;&amp; test -f src/lib/kotoiq/profileTypes.ts &amp;&amp; test -f tests/profileTypes.test.ts &amp;&amp; grep -q "SOURCE_TYPES" src/lib/kotoiq/profileTypes.ts &amp;&amp; grep -q "EntityGraphSeed" src/lib/kotoiq/profileTypes.ts &amp;&amp; grep -q "CANONICAL_FIELD_NAMES" src/lib/kotoiq/profileTypes.ts &amp;&amp; npx tsc --noEmit -p tsconfig.json 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/kotoiq/profileTypes.ts exists and exports SOURCE_TYPES, CANONICAL_FIELD_NAMES, ProvenanceRecord, ClientProfile, EntityGraphSeed, EntityGraphNode, EntityGraphEdge, Clarification, ClarificationSeverity, ClarificationStatus, ClarificationChannel, NarrationEvent
    - File starts with `import 'server-only'`
    - tests/profileTypes.test.ts exists with the 4 tests above
    - `npx tsc --noEmit` exits with no NEW errors (executor compares to baseline main branch tsc output)
  </acceptance_criteria>
  <done>Types file compiles, test file exists (runs after Task 6 installs Vitest), exports are complete.</done>
</task>

<task type="auto">
  <name>Task 4: Extract pick() helper into src/lib/kotoClientPick.ts</name>
  <files>src/lib/kotoClientPick.ts, src/views/KotoProposalBuilderPage.jsx</files>
  <read_first>
    - src/views/KotoProposalBuilderPage.jsx lines 47-100 — the current pick() function verbatim (DO NOT modify semantics — relocate only)
    - _knowledge/modules/onboarding.md — dual-storage pattern (dedicated columns + onboarding_answers jsonb)
    - 07-RESEARCH.md §3.1 mapping table — the alias keys pick() resolves across
  </read_first>
  <action>
1. Open src/views/KotoProposalBuilderPage.jsx at line 47. Copy the complete pick(client, ...keys) function body (including every fallback and the array-join semantics).

2. Create src/lib/kotoClientPick.ts:

```
/**
 * Extracted from src/views/KotoProposalBuilderPage.jsx:47
 *
 * Resolves a client field across:
 *   - client[key] (dedicated column — voice agent writes here)
 *   - client.onboarding_answers[key] (jsonb — web form writes here)
 *   - client.onboarding_data[key] (legacy jsonb name some routes still use)
 *
 * Returns: joined string for arrays (', '), trimmed string for strings,
 * null if all keys empty/missing. First non-empty match wins.
 */
export function pick(
  client: Record<string, any> | null | undefined,
  ...keys: string[]
): string | null {
  // EXACT body from KotoProposalBuilderPage.jsx:47 — preserve semantics
}
```

The body MUST preserve:
- Array joining with ', '
- Whitespace-collapse + null-return for empty strings
- The jsonb fallback chain (onboarding_answers first, then onboarding_data)

3. Open src/views/KotoProposalBuilderPage.jsx. REPLACE the local function pick definition (lines ~47-100) with an import at top:
```
import { pick } from '../lib/kotoClientPick'
```
(Use the path convention matching other imports in this file — probably ../lib/kotoClientPick.) Leave the rest of the page untouched. All pick() call sites continue to work because signature is unchanged.

4. If the file has other helpers declared alongside pick (arr, safeText, etc.), leave those in place — only move pick.
  </action>
  <verify>
    <automated>cd /Users/adamsegall/Desktop/moose &amp;&amp; test -f src/lib/kotoClientPick.ts &amp;&amp; grep -q "export function pick" src/lib/kotoClientPick.ts &amp;&amp; grep -q "from.*kotoClientPick" src/views/KotoProposalBuilderPage.jsx &amp;&amp; ! grep -qE "^function pick\(" src/views/KotoProposalBuilderPage.jsx &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/kotoClientPick.ts exists and exports `pick`
    - src/views/KotoProposalBuilderPage.jsx imports pick from the new file
    - src/views/KotoProposalBuilderPage.jsx no longer declares `function pick(` at top level
    - `npm run build` exits 0 (KotoProposalBuilderPage still renders)
  </acceptance_criteria>
  <done>pick() available from src/lib/kotoClientPick.ts. KotoProposalBuilderPage still builds and renders identically.</done>
</task>

<task type="auto">
  <name>Task 5: Extend src/lib/kotoiqDb.ts with clientProfile + clarifications helpers</name>
  <files>src/lib/kotoiqDb.ts</files>
  <read_first>
    - src/lib/kotoiqDb.ts — full file (read lines 1-250 for DIRECT_AGENCY_TABLES set + existing templates/campaigns helper patterns)
    - 07-RESEARCH.md §2 (lines ~488-513) — typed helper method list
    - src/lib/kotoiq/profileTypes.ts (from Task 3) — types this helper surfaces
  </read_first>
  <action>
1. Open src/lib/kotoiqDb.ts. Locate DIRECT_AGENCY_TABLES Set (around line 39). Add `kotoiq_client_profile` and `kotoiq_clarifications` to the Set:

```
const DIRECT_AGENCY_TABLES = new Set([
  'kotoiq_builder_sites',
  'kotoiq_templates',
  'kotoiq_campaigns',
  'kotoiq_variants',
  'kotoiq_publishes',
  'kotoiq_cwv_readings',
  'kotoiq_indexnow_submissions',
  'kotoiq_call_attribution',
  'kotoiq_elementor_schema_versions',
  'kotoiq_client_profile',      // NEW — Phase 7
  'kotoiq_clarifications',      // NEW — Phase 7
])
```

(Read the actual current list first — executor preserves existing entries exactly.)

2. After the existing `templates: { ... }` and `campaigns: { ... }` helper objects, add two new typed helpers inside the returned object from getKotoIQDb:

```
clientProfile: {
  async get(clientId: string) {
    return await scopedFrom('kotoiq_client_profile')
      .select('*')
      .eq('client_id', clientId)
      .maybeSingle()
  },
  async upsert(data: Record<string, any>) {
    // data MUST include client_id; agency_id is auto-injected by scopedInsert
    return await scopedInsert('kotoiq_client_profile', data, {
      onConflict: 'agency_id,client_id',
    })
  },
  async updateField(profileId: string, fieldName: string, newRecord: Record<string, any>) {
    // Reads current fields jsonb, appends newRecord into fields[fieldName] array,
    // recomputes the winning value + confidence, updates hot column if fieldName is a hot column.
    // Executor: implement as a single UPDATE using jsonb_set or read-modify-write in JS.
    // Hot columns list must come from CANONICAL_FIELD_NAMES first 11 entries.
    // Return: { data, error }
  },
  async addField(profileId: string, fieldName: string, value: any, sourceMeta: Record<string, any>) {
    // Operator custom field (D-05) — source_type='operator_edit', confidence=1.0
  },
  async deleteField(profileId: string, fieldName: string) {
    // Removes fields[fieldName] entirely; preserves sources registry (D-05)
  },
  async list(filters?: { client_id?: string; launched?: boolean }) {
    let q = scopedFrom('kotoiq_client_profile').select('*')
    if (filters?.client_id) q = q.eq('client_id', filters.client_id)
    if (filters?.launched === true) q = q.not('launched_at', 'is', null)
    if (filters?.launched === false) q = q.is('launched_at', null)
    return await q.order('updated_at', { ascending: false })
  },
  async markLaunched(profileId: string, runId: string) {
    return await scopedFrom('kotoiq_client_profile')
      .update({ launched_at: new Date().toISOString(), last_pipeline_run_id: runId })
      .eq('id', profileId)
  },
},
clarifications: {
  async list(filters?: { client_id?: string; status?: string; severity?: string; limit?: number }) {
    let q = scopedFrom('kotoiq_clarifications').select('*')
    if (filters?.client_id) q = q.eq('client_id', filters.client_id)
    if (filters?.status) q = q.eq('status', filters.status)
    if (filters?.severity) q = q.eq('severity', filters.severity)
    q = q.order('severity', { ascending: false }).order('created_at', { ascending: true })
    if (filters?.limit) q = q.limit(filters.limit)
    return await q
  },
  async get(id: string) {
    return await scopedFrom('kotoiq_clarifications').select('*').eq('id', id).single()
  },
  async create(data: Record<string, any>) {
    // data: { client_id, profile_id, question, reason?, target_field_path?, severity, impact_hint?, impact_unlocks? }
    return await scopedInsert('kotoiq_clarifications', data)
  },
  async update(id: string, patch: Record<string, any>) {
    return await scopedFrom('kotoiq_clarifications').update(patch).eq('id', id)
  },
  async markAnswered(id: string, answerText: string, answeredBy: string) {
    return await scopedFrom('kotoiq_clarifications').update({
      status: 'answered',
      answer_text: answerText,
      answered_by: answeredBy,
      answered_at: new Date().toISOString(),
    }).eq('id', id)
  },
  async markForwarded(id: string, channel: 'sms' | 'email' | 'portal') {
    return await scopedFrom('kotoiq_clarifications').update({
      status: 'asked_client',
      asked_channel: channel,
      asked_at: new Date().toISOString(),
    }).eq('id', id)
  },
  async markSkipped(id: string) {
    return await scopedFrom('kotoiq_clarifications').update({ status: 'skipped' }).eq('id', id)
  },
},
```

IMPORTANT: implement updateField, addField, deleteField as real working code (not stubs). For updateField:

```
async updateField(profileId, fieldName, newRecord) {
  const HOT_COLUMNS = new Set([
    'business_name','website','primary_service','target_customer','service_area',
    'phone','founding_year','unique_selling_prop','industry','city','state',
  ])
  // 1. Read current row
  const { data: current, error: readErr } = await scopedFrom('kotoiq_client_profile')
    .select('fields').eq('id', profileId).single()
  if (readErr || !current) return { data: null, error: readErr || new Error('not found') }
  const fields = (current.fields || {}) as Record<string, any[]>
  const arr = fields[fieldName] || []
  arr.push(newRecord)
  // Sort descending by confidence, operator_edit wins ties (RESEARCH Open Q #4)
  arr.sort((a, b) => {
    if (a.source_type === 'operator_edit' && b.source_type !== 'operator_edit') return -1
    if (b.source_type === 'operator_edit' && a.source_type !== 'operator_edit') return 1
    return (b.confidence || 0) - (a.confidence || 0)
  })
  fields[fieldName] = arr
  const patch: any = { fields, last_edited_at: new Date().toISOString(), completeness_score: null }
  if (HOT_COLUMNS.has(fieldName)) {
    patch[fieldName] = arr[0]?.value ?? null
  }
  return await scopedFrom('kotoiq_client_profile').update(patch).eq('id', profileId)
}
```

addField is similar but creates a fresh array. deleteField does `delete fields[fieldName]` + sets hot column to null if applicable.

3. Verify imports at top of kotoiqDb.ts already include the Supabase client and any existing helpers. Add no new imports unless needed.

4. Do NOT remove or modify any existing helper methods. Append only.
  </action>
  <verify>
    <automated>cd /Users/adamsegall/Desktop/moose &amp;&amp; grep -q "'kotoiq_client_profile'" src/lib/kotoiqDb.ts &amp;&amp; grep -q "'kotoiq_clarifications'" src/lib/kotoiqDb.ts &amp;&amp; grep -q "clientProfile:" src/lib/kotoiqDb.ts &amp;&amp; grep -q "clarifications:" src/lib/kotoiqDb.ts &amp;&amp; grep -q "markAnswered" src/lib/kotoiqDb.ts &amp;&amp; grep -q "updateField" src/lib/kotoiqDb.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - DIRECT_AGENCY_TABLES contains 'kotoiq_client_profile' and 'kotoiq_clarifications'
    - getKotoIQDb return value has `clientProfile` and `clarifications` typed methods
    - clientProfile has 7 methods: get, upsert, updateField, addField, deleteField, list, markLaunched
    - clarifications has 7 methods: list, get, create, update, markAnswered, markForwarded, markSkipped
    - `npx tsc --noEmit` exits 0 with no new errors
    - ESLint rule `kotoiq/no-unscoped-kotoiq` still passes (no direct supabase.from('kotoiq_*') introduced)
    - All existing helper methods (templates, campaigns) unchanged
  </acceptance_criteria>
  <done>getKotoIQDb(agencyId).clientProfile and .clarifications are callable and type-safe.</done>
</task>

<task type="auto">
  <name>Task 6: Install Vitest + configure + land fixtures + DST theme token</name>
  <files>package.json, vitest.config.ts, tests/fixtures/profiles.ts, tests/fixtures/anthropicMock.ts, src/lib/theme.ts</files>
  <read_first>
    - 07-RESEARCH.md §14 (Validation Architecture) — framework decision rationale
    - 07-VALIDATION.md — Wave 0 checklist
    - src/lib/theme.ts — full file (understand existing export style before adding DST)
    - 07-UI-SPEC.md §3 — DST = '#DC2626' rationale
    - package.json — existing scripts and devDependencies
  </read_first>
  <action>
1. Install Vitest:
```
npm install --save-dev vitest@^1 @vitest/ui@^1 jsdom@^24
```
(If the repo is pnpm, use `pnpm add -D`; executor confirms from lockfile presence.)

2. Add to package.json scripts:
```
"test": "vitest run",
"test:watch": "vitest",
"test:ui": "vitest --ui"
```

3. Create vitest.config.ts at repo root:
```
import { defineConfig } from 'vitest/config'
import path from 'path'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['tests/**/*.test.ts'],
    globals: false,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
})
```

4. Create tests/fixtures/profiles.ts with three fixtures (used by Plans 2-5):

```
import type { ClientProfile, ProvenanceRecord } from '../../src/lib/kotoiq/profileTypes'

const pr = (overrides: Partial<ProvenanceRecord>): ProvenanceRecord => ({
  value: overrides.value ?? 'unset',
  source_type: overrides.source_type ?? 'onboarding_form',
  captured_at: '2026-04-17T00:00:00Z',
  confidence: overrides.confidence ?? 0.9,
  ...overrides,
})

export const COMPLETE_PROFILE: Partial<ClientProfile> = {
  business_name: 'Unified Marketing',
  website: 'https://unifiedmktg.com',
  primary_service: 'Google Ads management for local service businesses',
  target_customer: 'small businesses in South Florida',
  service_area: 'South Florida',
  phone: '+15615551234',
  founding_year: 2019,
  unique_selling_prop: 'Same-day response and fixed-price retainers',
  industry: 'Marketing Agency',
  city: 'Boca Raton',
  state: 'FL',
  fields: {
    business_name: [pr({ value: 'Unified Marketing', confidence: 1.0 })],
    primary_service: [pr({ value: 'Google Ads management for local service businesses', confidence: 0.95 })],
    // ...add one ProvenanceRecord per hot column — executor fills in
  },
}

export const PARTIAL_PROFILE: Partial<ClientProfile> = {
  business_name: 'RDC Contracting',
  website: null,
  primary_service: null,
  target_customer: null,
  service_area: null,
  phone: '+15615559999',
  founding_year: null,
  unique_selling_prop: null,
  industry: null,
  city: null,
  state: null,
  fields: {
    business_name: [pr({ value: 'RDC Contracting', confidence: 1.0 })],
    phone: [pr({ value: '+15615559999', source_type: 'onboarding_form', confidence: 0.9 })],
  },
}

export const DISCREPANCY_PROFILE: Partial<ClientProfile> = {
  business_name: 'Pangea Plumbing',
  founding_year: 2019,
  fields: {
    business_name: [pr({ value: 'Pangea Plumbing', confidence: 1.0 })],
    founding_year: [
      pr({ value: 2019, source_type: 'onboarding_form', confidence: 0.9, source_url: 'https://hellokoto.com/clients/xyz' }),
      pr({ value: 2020, source_type: 'voice_call', confidence: 0.85, source_ref: 'retell_call:abc', source_snippet: "We've been serving South Florida for about six years." }),
      pr({ value: 2011, source_type: 'claude_inference', confidence: 0.6, source_snippet: 'Website says 15 years in business' }),
    ],
  },
}
```

5. Create tests/fixtures/anthropicMock.ts:

```
import { vi } from 'vitest'

export function mockAnthropicFetch(responseBody: any) {
  return vi.fn().mockResolvedValue({
    ok: true,
    status: 200,
    json: async () => responseBody,
    body: null,
  })
}

export function mockAnthropicToolUse(extractedFields: Array<Record<string, any>>) {
  return mockAnthropicFetch({
    content: [{ type: 'tool_use', name: 'extract_profile_fields', input: { fields: extractedFields } }],
    usage: { input_tokens: 100, output_tokens: 50 },
  })
}

export function mockAnthropicStreaming(chunks: string[]) {
  const encoder = new TextEncoder()
  const stream = new ReadableStream({
    start(controller) {
      for (const c of chunks) {
        const sse = `event: content_block_delta\ndata: ${JSON.stringify({
          type: 'content_block_delta',
          delta: { type: 'text_delta', text: c },
        })}\n\n`
        controller.enqueue(encoder.encode(sse))
      }
      controller.close()
    },
  })
  return vi.fn().mockResolvedValue({ ok: true, status: 200, body: stream })
}
```

6. Extend src/lib/theme.ts — APPEND (do not modify existing exports):

```
// ── Phase 7 destructive action token (UI-SPEC §3) ────────────────────────
// Used ONLY for field-rejection confirmation. Koto Pink (R) remains reserved
// for accent + discrepancy; using it for destructive would collide visually.
export const DST = '#DC2626'
```

7. Run `npm test` to verify the test runner is wired. The Task 3 test file should pass at this point.
  </action>
  <verify>
    <automated>cd /Users/adamsegall/Desktop/moose &amp;&amp; grep -q '"vitest"' package.json &amp;&amp; grep -q '"test": "vitest run"' package.json &amp;&amp; test -f vitest.config.ts &amp;&amp; test -f tests/fixtures/profiles.ts &amp;&amp; test -f tests/fixtures/anthropicMock.ts &amp;&amp; grep -q "COMPLETE_PROFILE" tests/fixtures/profiles.ts &amp;&amp; grep -q "DISCREPANCY_PROFILE" tests/fixtures/profiles.ts &amp;&amp; grep -q "mockAnthropicToolUse" tests/fixtures/anthropicMock.ts &amp;&amp; grep -qE "^export const DST = '#DC2626'" src/lib/theme.ts &amp;&amp; npm test 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - package.json has `vitest` in devDependencies
    - package.json has `"test": "vitest run"` script
    - vitest.config.ts exists at repo root with `include: ['tests/**/*.test.ts']`
    - tests/fixtures/profiles.ts exports COMPLETE_PROFILE, PARTIAL_PROFILE, DISCREPANCY_PROFILE
    - tests/fixtures/anthropicMock.ts exports mockAnthropicFetch, mockAnthropicToolUse, mockAnthropicStreaming
    - src/lib/theme.ts exports DST = '#DC2626' (appended, existing exports unchanged)
    - `npm test` exits 0 and passes the 4 tests in tests/profileTypes.test.ts
  </acceptance_criteria>
  <done>Vitest runs, 4/4 profileTypes tests pass, fixtures ready for downstream plans, DST token available for UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agency A operator → kotoiq_client_profile rows of Agency B | MUST be blocked by getKotoIQDb(agencyId) agency_id injection + ESLint rule |
| Migration application → live Supabase | Operator hand holds the secret (SUPABASE_ACCESS_TOKEN). Migration contents reviewed before push. |
| tests/fixtures/ → production code paths | Fixtures are dev-only. Vitest include is limited to tests/**/*.test.ts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01a | Elevation of Privilege | kotoiq_client_profile / kotoiq_clarifications cross-agency read | high | mitigate | DIRECT_AGENCY_TABLES set includes both tables in Task 5; getKotoIQDb auto-injects .eq('agency_id', agencyId). ESLint rule kotoiq/no-unscoped-kotoiq blocks raw supabase.from('kotoiq_*') calls at build time. |
| T-07-01b | Information Disclosure | Migration accidentally grants broader permissions | medium | mitigate | RLS policy copies 20260505 pattern verbatim — FOR ALL USING (true) WITH CHECK (true) is service-role-only; anon/authenticated clients cannot hit the tables directly (Koto never uses anon client for writes). Task 1 acceptance criteria enforces ENABLE ROW LEVEL SECURITY on both tables. |
| T-07-06 | Denial of Service | Migration applied out of order or with incompatible schema | medium | mitigate | `CREATE TABLE IF NOT EXISTS` + `DO` block for realtime ADD prevents idempotency breakage. Task 2 human verification confirms the tables exist before downstream plans proceed. |
| T-07-07 | Tampering | Vitest config accidentally runs tests against production DB | low | mitigate | vitest.config.ts uses `environment: 'node'` with no Supabase client init. Mocks via tests/fixtures/anthropicMock.ts. No real API keys read in test context. |
</threat_model>

<verification>
## Plan-Level Verification

After all tasks complete:

1. Migration file exists and passes grep assertions (Task 1)
2. Live DB contains both tables + realtime publication (Task 2 human-verified)
3. `npx tsc --noEmit` reports no new errors
4. `npm run build` exits 0 (KotoProposalBuilderPage still builds after pick() extraction)
5. `npm test` exits 0 with 4 tests passing from tests/profileTypes.test.ts
6. `npm run lint` exits 0 (ESLint rule kotoiq/no-unscoped-kotoiq not triggered by new helpers)
7. src/lib/theme.ts unchanged except for the DST append — diff against main should show only an added export line
</verification>

<success_criteria>
- [ ] Migration 20260507_kotoiq_client_profile.sql exists, passes grep acceptance
- [ ] Migration applied to live Supabase (human-verified via Studio query)
- [ ] src/lib/kotoiq/profileTypes.ts exports all 12 required types/consts
- [ ] src/lib/kotoClientPick.ts exports pick(); KotoProposalBuilderPage imports from there
- [ ] src/lib/kotoiqDb.ts has clientProfile + clarifications helpers with full method surface
- [ ] Vitest installed, `npm test` runs tests/profileTypes.test.ts to 4/4 green
- [ ] tests/fixtures/profiles.ts + anthropicMock.ts exist with required exports
- [ ] src/lib/theme.ts exports DST = '#DC2626'
- [ ] No regressions: `npm run build` + `npm run lint` + `npx tsc --noEmit` all exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/07-client-profile-seeder-v1-internal-ingest-gap-finder/07-01-SUMMARY.md` documenting:
- Exact migration file name and tables created
- DIRECT_AGENCY_TABLES final list
- Model IDs pinned for downstream plans (record `claude-sonnet-4-5-20250929` and `claude-haiku-4-5-20251001` as the canonical IDs — RESEARCH §17 Risk #1 resolution; CONTEXT.md "Sonnet 4.6 / Haiku 4.5" is aspirational and will upgrade via profileConfig.ts when the newer IDs ship)
- Vitest version installed + whether the project uses npm or pnpm
- Any deviation from the DDL in RESEARCH §2 + rationale
</output>
