---
phase: 12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kotoiq/competitorIntel.ts
  - src/app/api/kotoiq/route.ts
  - tests/kotoiq/competitorIntel.test.ts
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]
must_haves:
  truths:
    - "For a chosen service×city set, the aggregator gathers organic (top 3-5), GEO (map-pack), and AEO (5-engine SoV) competitor signals"
    - "Identities are reconciled across lenses (organic domains ↔ GEO business names ↔ AEO brand rows) into one per-competitor × per-lens set"
    - "Every fetched lens fact is wrapped in createVerifiedData (source_url + fetched_at), with staleness buildExpiresAt('rankings')=24h"
    - "AEO roster is seeded before the scan; a failed lens is marked 'unavailable', never presented as 'no competitors'"
    - "Spend is bounded: a representative service×city subset by default; what was capped is logged"
  artifacts:
    - path: "src/lib/kotoiq/competitorIntel.ts"
      provides: "Three-lens aggregator + pure identity-reconciliation (domain↔brand) merge"
      exports: ["aggregateCompetitorIntel", "reconcileCompetitorIdentities"]
    - path: "tests/kotoiq/competitorIntel.test.ts"
      provides: "Pure reconciliation/dedup unit tests"
  key_links:
    - from: "src/app/api/kotoiq/route.ts (competitor_intel)"
      to: "competitorIntel.aggregateCompetitorIntel"
      via: "action handler"
      pattern: "competitor_intel"
    - from: "competitorIntel.ts"
      to: "kotoiq_aeo_competitors (seed roster) + runAEOVisibilityScan + getCompetitorCompare"
      via: "AEO lens"
      pattern: "runAEOVisibilityScan"
---

<objective>
Build the competitor-intel aggregator (WS5): ONE lib (`competitorIntel.ts`) + ONE action
(`competitor_intel`) that, for a chosen service×city set, gathers THREE lenses with
provenance — ORGANIC (`analyze_competitors`/`dfs_compare`), GEO (`grid-scan` map-pack),
AEO (`setupClientForAEO` → `runAEOVisibilityScan` → `getCompetitorCompare`) — reconciles
identities across the lenses, wraps every fetched fact in `createVerifiedData`, and bounds
spend with a representative subset.

Purpose: This is the phase's only substantial NEW orchestration. It feeds 12-05
(opportunity list) and 12-06 (strategy). Independent of 12-01/02/03 — runs in Wave 1.

Output: `competitorIntel.ts` (new), `competitor_intel` action, pure reconciliation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-CONTEXT.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-RESEARCH.md
@_knowledge/data-integrity-standard.md

# Engines to reuse (read signatures before coding):
@src/lib/kotoiq/aeoVisibilityEngine.ts
@src/lib/dataIntegrity.ts
@src/lib/geoLookup.ts

<interfaces>
<!-- Three lenses, three identity models (verified this session): -->

// ORGANIC — analyze_competitors action (route.ts:1401), market mode:
//   input {market:{service,city,state}} → builds "{service} {city} {state}" → getSERPResults
//   → top {url, domain, title, rank: rank_group}. Authoritative organic rank = rank_group.
//   dfs_compare (route.ts:5259) → getDomainIntersection(domain1,domain2,'United States',100) (dataforseo.ts:505)
//   = keyword gaps between two domains (use once a top competitor domain is known).
//   FALLBACK: analyze_competitors falls back to koto_intel_reports competitors on DataForSEO failure (route.ts:1450).

// GEO — /api/seo/grid-scan: input {keyword, location, target_business, grid_size(≤7), spacing_km, search_radius_km}
//   → grid_results[] per-cell {lat,lng,row,col,rank,total,top3[]} + summary{avg_rank,best_rank,coverage_pct}.
//   top3[] per cell = local-pack competitor names (the GEO competitor signal). Returns [] on failure.
//   Cost: each cell = a Places call. 5×5 = 25 calls per keyword per business — CAP grid_size + subset.

// AEO — aeoVisibilityEngine.ts:
//   setupClientForAEO(s, {client_id, agency_id?, seed_prompts=true, seed_self_competitor=true}) (:35) — MUST run first; idempotent upserts.
//   runAEOVisibilityScan(s, {client_id, agency_id?, engines?, prompt_limit?}) (:117) — active prompts × 5 engines (ChatGPT/Claude/Gemini/Perplexity/Google AIO), ~120s/40 prompts. Real $ per engine call.
//   getCompetitorCompare(s, {client_id, days}) (:431) → per-brand {brand, is_self, mentions, share, avg_position}.
//   getCitedSources(s, {client_id, days, limit}) (:396) → cited URLs.
//   kotoiq_aeo_competitors: client_id-scoped (NO agency_id col), UNIQUE(client_id, brand_name), has is_self/aliases[]/domain.
//   Existing actions: aeo_add_competitor (route.ts:4306), aeo_list_competitors (:4294).
//   ⚠ Competitors must be ADDED to kotoiq_aeo_competitors BEFORE the scan or AEO SoV is empty.

// Provenance — dataIntegrity.ts:100:
//   createVerifiedData(data, {source_url, source_name, source_type, fetched_at, expires_at})
//   buildExpiresAt('rankings') = 24h. scoreServiceCityGrid.ts:30-34 shows the exact wrapping to copy.

// Cities — geoLookup / selectCitiesToCheck (11-04) Census-validated; drops unknown cities (V5 input validation).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reconcileCompetitorIdentities — pure domain↔brand merge</name>
  <files>src/lib/kotoiq/competitorIntel.ts, tests/kotoiq/competitorIntel.test.ts</files>
  <behavior>
    - reconcileCompetitorIdentities({ organic:[{name?,domain,rank_group}], geo:[{business_name, local_pack_rank, cells_present}], aeo:[{brand, share, avg_position, mentions, domain?, aliases?}] }) → unified competitors[] = [{ name, domain?, organic?:{rank}, geo?:{local_pack_rank, cells_present}, aeo?:{share, avg_position, mentions} }]. Match rules: organic.domain ↔ aeo.domain (exact host, www-stripped, lowercased); geo.business_name ↔ aeo.brand (normalized name equality + aeo.aliases[] membership); unmatched entries appear as single-lens rows (NOT dropped). Dedup so the same competitor never double-counts across lenses. Pure, IO-free.
    - normalizeBrand(s) / hostOf(url) helpers, pure.
    Tests:
    - same competitor present in organic (domain) + aeo (same domain) merges into ONE row with both lenses.
    - geo business_name matching an aeo alias merges; non-matching stays separate.
    - empty/missing lens does not crash; single-lens rows preserved; no double-count.
  </behavior>
  <action>
    Create src/lib/kotoiq/competitorIntel.ts with reconcileCompetitorIdentities + normalizeBrand + hostOf as PURE exported functions (mirror scoreServiceCityGrid's pure computeCellScore/bucketCell so they test on fixtures without DB/network). This is the correctness anchor of WS5 — get the merge + dedup right. No Next APIs, no IO in these functions.
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/competitorIntel.test.ts</automated>
  </verify>
  <done>Reconciliation merges cross-lens identities correctly, preserves single-lens rows, never double-counts; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: aggregateCompetitorIntel — orchestrate 3 lenses with provenance + spend caps</name>
  <files>src/lib/kotoiq/competitorIntel.ts</files>
  <action>
    Add async aggregateCompetitorIntel({ agencyId, clientId, services[], cities[], state, fullScan=false }):
    1. Census-validate cities via geoLookup/selectCitiesToCheck (drop unknowns — V5).
    2. Pick a REPRESENTATIVE service×city subset by default (e.g. top-N by some deterministic order); only the full grid when fullScan===true. log() the cap (which combos were scanned vs skipped) — spend control.
    3. ORGANIC: per representative service×city, call the analyze_competitors market path → collect top 3-5 {name, domain, rank_group}. Wrap each in createVerifiedData(source_url=the SERP query URL, source_type, fetched_at=now, expires_at=buildExpiresAt('rankings')). If DataForSEO fails, mark lens 'unavailable' (do NOT present empty as "no competitors").
    4. GEO: per representative service×city, call grid-scan (grid_size capped ≤5 by default) → collect top3[] local-pack names + client avg_rank. Wrap in createVerifiedData. Empty result → mark lens 'unavailable'.
    5. RECONCILE roster: union organic domains + GEO business names → upsert each into kotoiq_aeo_competitors via aeo_add_competitor; ensure self seeded via setupClientForAEO (idempotent) BEFORE the AEO scan.
    6. AEO: runAEOVisibilityScan (gated — only when explicitly requested or fullScan, to bound $); read getCompetitorCompare → per-brand {share, avg_position, mentions}. Wrap in createVerifiedData. If the scan can't run (no key / not seeded), mark AEO lens 'unavailable' + ai_available:false.
    7. Merge via reconcileCompetitorIdentities → unified set. Return { competitors[], lenses:{organic:'ok'|'unavailable', geo:..., aeo:...}, capped:{scanned, skipped}, ai_available }.
    Persist to kotoiq_client_profile.fields.competitor_intel (jsonb — extend, no new table per CONTEXT). NOTE in the SUMMARY if payload volume looks large enough to warrant a future kotoiq_competitor_intel table.
    NEVER bare catch{}: a failed lens → status 'unavailable' + a logged reason.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Aggregator orchestrates 3 lenses, seeds AEO roster before scan, wraps every fact in createVerifiedData(rankings 24h), caps spend with a logged subset, marks failed lenses 'unavailable', persists to fields.competitor_intel; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: competitor_intel action in /api/kotoiq</name>
  <files>src/app/api/kotoiq/route.ts</files>
  <action>
    Append `competitor_intel` to the if-chain: validate {services[], cities[], state, fullScan?} (grid_size ≤7, cities Census-filtered — V5), confirm client ownership via the existing route gate BEFORE any AEO write (V4 — kotoiq_aeo_* are client_id-scoped), call aggregateCompetitorIntel. Return the unified set + lens statuses + capped info + ai_available. Rides verifySession. Route exports only handlers — read node_modules/next/dist/docs/ if unsure.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Action wired with input validation + ownership check; returns unified competitor set + lens statuses; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/kotoiq | services/cities/state/fullScan untrusted → validate + Census-filter |
| server→DataForSEO/Places/AEO engines | paid external IO; failures must degrade visibly, not present empty as truth |
| server→kotoiq_aeo_* | client_id-scoped writes → ownership check before write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering | stale/fabricated competitor ranks | mitigate | createVerifiedData + buildExpiresAt('rankings') 24h on every lens fact |
| T-12-14 | Spoofing | identity collision across lenses double-counts | mitigate | reconcileCompetitorIdentities domain↔brand match + dedup (pure, tested) |
| T-12-15 | DoS (cost) | AEO 5-engine + N×N Places per service×city | mitigate | representative subset default, grid_size≤5, AEO gated behind explicit/fullScan; capped logged |
| T-12-16 | Info disclosure | cross-agency / cross-client AEO leak | mitigate | route ownership check before AEO writes; getKotoIQDb agency-scoped for fields |
| T-12-17 | Tampering | silent fallback presents empty as "no competitors" | mitigate | failed lens → status 'unavailable' + logged reason, never bare catch{} |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/competitorIntel.test.ts` passes (reconciliation correctness); `npx tsc --noEmit` clean.
- Manual (gated on funded ANTHROPIC_API_KEY for the AEO lens + live DataForSEO/Places): run competitor_intel for a paired client → per-competitor × per-lens set with provenance badges; lens marked 'unavailable' when an engine fails. Organic + GEO work without a Claude key; AEO shows ai_available:false until a funded key is set. Build/ship independent of the key.
</verification>

<success_criteria>
- Three-lens aggregation with correct cross-lens identity reconciliation (tested pure).
- Every fetched fact provenance-wrapped (rankings 24h); failed lenses 'unavailable'.
- Spend bounded by a logged representative subset; AEO roster seeded before scan.
- Persists to fields.competitor_intel (no new table).
</success_criteria>

<output>
After completion, create `.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-04-SUMMARY.md`. Note whether payload volume warrants a future dedicated table.
</output>
