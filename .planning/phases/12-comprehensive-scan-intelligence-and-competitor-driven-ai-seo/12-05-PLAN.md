---
phase: 12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo
plan: 05
type: execute
wave: 2
depends_on: ["12-04"]
files_modified:
  - src/lib/kotoiq/opportunityList.ts
  - src/app/api/kotoiq/route.ts
  - tests/kotoiq/opportunityList.test.ts
autonomous: true
requirements: [OPP-01, OPP-02]
must_haves:
  truths:
    - "Aggregated competitor keywords/pages from WS5 are fed into analyzePageGaps/scoreServiceCityGrid → ONE extensive ranked keyword + page opportunity list"
    - "The existing bucketing (quick_win/net_new/big_bet/low_demand_deprioritize) and provenance are preserved"
    - "The list is genuinely larger/competitor-driven, not just the client's own GSC keywords"
  artifacts:
    - path: "src/lib/kotoiq/opportunityList.ts"
      provides: "Glue: merge WS5 competitor keywords into the gap grid → one extensive ranked list"
      exports: ["buildOpportunityList", "competitorKeywordsFromIntel"]
    - path: "tests/kotoiq/opportunityList.test.ts"
      provides: "Pure merge/dedup unit tests for competitor-keyword injection"
  key_links:
    - from: "src/lib/kotoiq/opportunityList.ts"
      to: "scoreServiceCityGrid / analyzePageGaps"
      via: "competitor keywords passed as extra seed phrases"
      pattern: "scoreServiceCityGrid|analyzePageGaps"
    - from: "src/app/api/kotoiq/route.ts (opportunity_list)"
      to: "opportunityList.buildOpportunityList"
      via: "action handler"
      pattern: "opportunity_list"
---

<objective>
Build the extensive opportunity list (WS6): feed WS5's aggregated competitor
keywords/pages into the existing `analyzePageGaps`/`scoreServiceCityGrid` so the ranked
output becomes EXTENSIVE and competitor-driven — keeping the proven bucketing and
provenance, producing ONE list (not a parallel one). Per research recommendation A5,
extend the grid rather than augment the Sonnet `content-gap`/`keyword-gap` routes (which
lack `logTokenUsage` and throw on a missing key).

Purpose: Turns competitor intel (12-04) + confirmed inputs (12-01) into the build target
the strategy step (12-06) consumes. Thin glue over reused engines.

Output: `opportunityList.ts` (new), `opportunity_list` action, pure merge tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-CONTEXT.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-RESEARCH.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-04-PLAN.md

# Reuse targets (read signatures before coding):
@src/lib/builder/scoreServiceCityGrid.ts
@src/lib/builder/pageGapEngine.ts

<interfaces>
<!-- pageGapEngine.analyzePageGaps → PageSuggestion[] with
//   {service, city, state, priority, reason, search_volume, keyword_difficulty,
//    competitor_count, competitor_urls[], competitor_fingerprints?}; accepts explicit cities[] (11-04 selectCitiesToCheck).
// scoreServiceCityGrid() → formula + bucketing quick_win/net_new/big_bet/low_demand_deprioritize over analyzePageGaps.
//   score_grid action (route.ts:6272) already reads confirmed fields.services[].
// WS5 output (12-04) persisted at kotoiq_client_profile.fields.competitor_intel:
//   competitors[] = [{ name, domain?, organic?:{rank}, geo?:{...}, aeo?:{...} }] + lens statuses.
//   dfs_compare intersection keywords + organic SERP titles + AEO cited URLs are the competitor-keyword sources. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: opportunityList.ts — competitor-keyword extraction + merge into the grid</name>
  <files>src/lib/kotoiq/opportunityList.ts, tests/kotoiq/opportunityList.test.ts</files>
  <behavior>
    - competitorKeywordsFromIntel(intel): pure — extracts a deduped competitor keyword/phrase set from the WS5 competitor_intel payload (dfs_compare intersection keywords if present + organic SERP titles tokenized + AEO cited-URL slugs). Returns string[] normalized + deduped.
    - buildOpportunityList({ agencyId, clientId, services[], cities[], state, intel }): derive extra seed phrases via competitorKeywordsFromIntel(intel), pass them as additional services/phrases into analyzePageGaps so scoreServiceCityGrid buckets them alongside the client's own grid → ONE merged ranked list. Preserve existing bucketing + provenance. Return { items: ScoredCell[], buckets:{quick_win, net_new, big_bet, low_demand_deprioritize}, source_counts:{own, competitor_derived} } so the UI can show the list grew.
    - Keep competitorKeywordsFromIntel + the merge/dedup step PURE for testing.
    Tests:
    - competitorKeywordsFromIntel on a fixture intel payload returns deduped competitor keywords.
    - merging competitor phrases into a service set increases the candidate count without duplicating existing services (dedup correctness).
  </behavior>
  <action>
    Create src/lib/kotoiq/opportunityList.ts. REUSE analyzePageGaps + scoreServiceCityGrid — do NOT reimplement scoring/bucketing. Inject WS5 competitor keywords as extra seed phrases (research A5 preferred path) so there is ONE ranked output with the existing provenance + UI. Do NOT touch the Sonnet content-gap/keyword-gap routes in this plan (they lack logTokenUsage + throw on missing key — out of scope here). Keep the extraction + dedup pure. No Next APIs.
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/opportunityList.test.ts</automated>
  </verify>
  <done>buildOpportunityList produces ONE extensive ranked list with preserved buckets/provenance and a competitor-derived count; pure extraction tested; no Sonnet-route changes.</done>
</task>

<task type="auto">
  <name>Task 2: opportunity_list action in /api/kotoiq</name>
  <files>src/app/api/kotoiq/route.ts</files>
  <action>
    Append `opportunity_list` to the if-chain: read confirmed fields.services[] + fields.competitor_intel (WS5) for the client, validate {cities[], state} (Census-filtered — V5), call buildOpportunityList. Return { items, buckets, source_counts }. If fields.competitor_intel is absent, return the own-only grid + a flag indicating competitor data wasn't run yet (graceful — don't fail). Rides verifySession. Route exports only handlers.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Action wired; returns extensive list + buckets + source_counts; degrades gracefully when WS5 intel absent; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/kotoiq | cities/state untrusted → Census-filter |
| fields.competitor_intel | server-read WS5 data; already provenance-wrapped at write time |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-18 | Tampering | competitor keywords from unverified source | mitigate | derived only from WS5 facts already createVerifiedData-wrapped in 12-04 |
| T-12-19 | Tampering | duplicate/inflated opportunity list | mitigate | pure dedup in competitorKeywordsFromIntel + the merge step (tested) |
| T-12-20 | V5 input | unknown cities | mitigate | selectCitiesToCheck / Census filter drops unknowns |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/opportunityList.test.ts` passes; `npx tsc --noEmit` clean.
- Manual (live DataForSEO for volume/KD; no Claude key needed for the grid path): run opportunity_list after competitor_intel → list is larger than the own-only grid (source_counts.competitor_derived > 0), buckets + provenance intact. No Sonnet-route changes, so no key-throw risk introduced.
</verification>

<success_criteria>
- ONE extensive ranked competitor-driven list via the reused grid (no parallel list, no Sonnet-route edits).
- Buckets + provenance preserved; competitor-derived count exposed.
- Graceful when WS5 intel absent.
</success_criteria>

<output>
After completion, create `.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-05-SUMMARY.md`.
</output>
