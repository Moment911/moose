---
phase: 12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/kotoiq/comprehensiveExtractor.ts
  - src/lib/kotoiq/serviceInference.ts
  - src/app/api/kotoiq/route.ts
  - tests/kotoiq/comprehensiveExtractor.test.ts
autonomous: true
requirements: [SCAN-01, SCAN-02, SCAN-03]
must_haves:
  truths:
    - "On scan, a unified Haiku pass returns four categories (keywords, phrases, services, offerings) from real baseline page content"
    - "When ANTHROPIC_API_KEY is absent/unfunded, each category falls back to a heuristic (no throw, no silent catch)"
    - "Confirmed items persist to kotoiq_client_profile.fields[category] in the {value, source_type, confidence, source_url, captured_at} shape"
    - "score_grid still reads fields.services[] unchanged"
  artifacts:
    - path: "src/lib/kotoiq/comprehensiveExtractor.ts"
      provides: "Unified four-category extractor (Haiku + per-category heuristic fallback), pure-fn-first"
      exports: ["extractComprehensive", "phrasesFromHeuristic", "offeringsFromHeuristic", "keywordsFromHeuristic"]
    - path: "src/lib/kotoiq/serviceInference.ts"
      provides: "saveConfirmedField(category, items) generalization; saveConfirmedServices kept as wrapper"
      contains: "saveConfirmedField"
    - path: "tests/kotoiq/comprehensiveExtractor.test.ts"
      provides: "Pure-fn unit tests for heuristics + JSON parse + graceful-degrade"
  key_links:
    - from: "src/app/api/kotoiq/route.ts (extract_comprehensive)"
      to: "comprehensiveExtractor.extractComprehensive"
      via: "action handler in /api/kotoiq if-chain"
      pattern: "extract_comprehensive"
    - from: "src/app/api/kotoiq/route.ts (save_field)"
      to: "serviceInference.saveConfirmedField"
      via: "action handler"
      pattern: "save_field"
---

<objective>
Build the unified four-category extractor (WS1) and generalize the confirmed-field
persistence so all four categories (keywords / phrases / services / offerings) are
extracted in ONE Haiku pass over the client's already-captured baseline pages and
persisted to `kotoiq_client_profile.fields[category]`.

Purpose: This is the data foundation. Plans 12-02 (chips), 12-03 (synergy), 12-05
(opportunity list), and 12-06 (strategy) all read the four `fields[category]` lists this
plan writes. `score_grid` already reads `fields.services[]` — keep that intact.

Output: `comprehensiveExtractor.ts` (new), a `saveConfirmedField` generalization in
`serviceInference.ts`, two new `/api/kotoiq` actions (`extract_comprehensive`,
`save_field`), and pure-function unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-CONTEXT.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-RESEARCH.md

# Engines/patterns to reuse (read before coding):
@src/lib/kotoiq/serviceInference.ts
@src/lib/kotoiq/pageContentExtractor.ts
@src/lib/kotoiq/pageDiscovery.ts
@_knowledge/data-integrity-standard.md

<interfaces>
<!-- Exact shapes the executor must reuse, extracted from serviceInference.ts this session. -->

const HAIKU_MODEL = 'claude-haiku-4-5-20251001'   // serviceInference.ts:34 — use for extraction

// The GOOD graceful-degrade pattern to COPY (serviceInference.ts:216):
//   if (!process.env.ANTHROPIC_API_KEY) return { ...empty, ok: false }
// NEVER use `process.env.ANTHROPIC_API_KEY!` (the throw pattern in localStrategistEngine:188).
// NEVER swallow the $0-credit error in a bare `catch {}` — return {ok:false, reason:'ai_unavailable'}.

// The persistence record shape already used by saveConfirmedServices (serviceInference.ts:328):
export interface StoredServiceRecord {
    value: string
    source_type: 'ai_inferred' | 'user_confirmed' | 'user_added'
    confidence: number
    source_url?: string
    captured_at: string
}

// Existing function to GENERALIZE (serviceInference.ts:353), full body read this session:
//   saveConfirmedServices({ agencyId, clientId, services }) writes db.clientProfile.upsert
//   with fields.services = records (dedup-by-lowercase, sorted, never throws).
//   It uses getKotoIQDb(agencyId), db.clientProfile.get(clientId) → fields jsonb,
//   then upsert({ client_id, fields }). agency_id is auto-injected.

// Reusable heuristic + helpers already exported:
//   servicesFromHeuristic(pages: BaselinePageInput[]): string[]   // serviceInference.ts:132
//   normalizeServiceName(raw): string                              // serviceInference.ts:94
//   pageSignalsForClaude(pages): string                            // serviceInference.ts:181 (slices to 60 pages / 8000 chars)

// Existing baseline loader for the action (route.ts:6793, infer_services):
//   loads latest captured_at batch from kotoiq_site_baseline → {url, h1, title, page_type, word_count}
//   extract_comprehensive should reuse this SAME loader.

// Richer per-page signals (pageContentExtractor.ts:62, fetchAndExtract → ExtractedPage):
//   h1, h2_list[] (≤20), cta_list[], hero_copy (≤500c), body_text (≤50000c),
//   meta_title, meta_description, schema_orgs[], word_count, content_hash
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: comprehensiveExtractor.ts — heuristics (pure) + unified Haiku pass</name>
  <files>src/lib/kotoiq/comprehensiveExtractor.ts, tests/kotoiq/comprehensiveExtractor.test.ts</files>
  <behavior>
    - keywordsFromHeuristic(pages): reuses voiceOnboardingAutoSetup quick_scan keyword logic OR derives from h1/meta_title/h2 tokens (lowercased, stopword-stripped, dedup, frequency-sorted). Pure, IO-free.
    - phrasesFromHeuristic(pages): n-grams (2-4 words) from h2_list + hero_copy, dedup, frequency-sorted. Pure.
    - offeringsFromHeuristic(pages): noun-phrase candidates from cta_list + h2_list (product/offer language), dedup. Pure.
    - servicesFromHeuristic: REUSE the existing export from serviceInference (do not re-implement).
    - extractComprehensive({ agencyId, clientId, pages }): if !process.env.ANTHROPIC_API_KEY → return { ok:false, reason:'ai_unavailable', keywords:keywordsFromHeuristic, phrases:phrasesFromHeuristic, services:servicesFromHeuristic, offerings:offeringsFromHeuristic } (all heuristic, flagged source_type later). Else ONE Haiku call returning strict JSON {keywords[],phrases[],services[],offerings[]} over pageSignalsForClaude(pages), 20s AbortController, safeParse with fence-strip, logTokenUsage({feature:'kotoiq_comprehensive_extraction', model:HAIKU_MODEL, agencyId, metadata:{client_id, pages:pages.length}}). On parse fail or empty → fall back to heuristics per category, return {ok:true} with whatever Haiku gave + heuristic fill.
    - Each returned item wrapped {value, source_type:'ai_inferred', confidence, source_url, captured_at} (StoredServiceRecord shape) when AI-derived; heuristic items source_type:'ai_inferred' too (still machine-inferred, flagged in UI).
    Tests (tests/kotoiq/comprehensiveExtractor.test.ts):
    - keywordsFromHeuristic/phrasesFromHeuristic/offeringsFromHeuristic on a fixture pages[] return non-empty deduped arrays.
    - extractComprehensive returns {ok:false, reason:'ai_unavailable'} when ANTHROPIC_API_KEY unset (delete env in test) AND still returns heuristic categories.
    - safeParse handles a fenced ```json block and a bare object; rejects garbage → heuristic fallback.
  </behavior>
  <action>
    Read node_modules/next/dist/docs/ only if touching framework patterns (this file is a plain lib — no Next APIs). Create src/lib/kotoiq/comprehensiveExtractor.ts. Mirror serviceInference.inferViaClaude EXACTLY for the Claude call (HAIKU_MODEL, AbortController 20s, safeParse, logTokenUsage). Import and reuse servicesFromHeuristic, normalizeServiceName, pageSignalsForClaude from serviceInference (export pageSignalsForClaude if not already — add `export` keyword). Keep the three NEW heuristics (keywords/phrases/offerings) pure and IO-free so they unit-test on fixtures like scoreServiceCityGrid's computeCellScore. CRITICAL: copy serviceInference.ts:216's graceful guard; NEVER the `!` assertion; NEVER bare `catch {}` — always return {ok:false, reason:'ai_unavailable', ...heuristics}.
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/comprehensiveExtractor.test.ts</automated>
  </verify>
  <done>Tests pass; extractComprehensive degrades gracefully without a key and returns all four heuristic categories; logTokenUsage called exactly once per Haiku invocation.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize saveConfirmedServices → saveConfirmedField(category, items)</name>
  <files>src/lib/kotoiq/serviceInference.ts</files>
  <action>
    Add `export async function saveConfirmedField(args: { agencyId, clientId, category: 'keywords'|'phrases'|'services'|'offerings', items: ConfirmedServiceInput[] })` that is a category-parameterized copy of saveConfirmedServices: validate category ∈ the four (return {ok:false, detail:'invalid category'} otherwise — V5 input validation), build the same dedup-sorted StoredServiceRecord[] with the same provenance logic (user_added / user_confirmed / ai_inferred), read existing fields jsonb via db.clientProfile.get, set fields[category] = records, upsert. Then REWRITE saveConfirmedServices to delegate: `return saveConfirmedField({ ...args, category: 'services', items: args.services })` (back-compat for the existing save_services action + score_grid reader — fields.services[] shape is byte-identical). Never throws.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/kotoiq/ -t "saveConfirmedField" 2>/dev/null || npx tsc --noEmit</automated>
  </verify>
  <done>saveConfirmedField writes fields[category]; saveConfirmedServices delegates with category='services'; tsc clean; fields.services[] shape unchanged so score_grid still reads it.</done>
</task>

<task type="auto">
  <name>Task 3: Wire extract_comprehensive + save_field actions into /api/kotoiq</name>
  <files>src/app/api/kotoiq/route.ts</files>
  <action>
    Append two handlers to the existing /api/kotoiq if-chain (no ALLOWED_ACTIONS constant exists — confirmed). Route files export ONLY handlers — all logic stays in lib.
    (1) `extract_comprehensive`: mirror infer_services (route.ts:6784) — load the latest captured_at kotoiq_site_baseline batch for the client (REUSE the same loader infer_services uses), map to BaselinePageInput[], call extractComprehensive({agencyId, clientId, pages}). Return the four flagged lists + an `ai_available` boolean (false when reason==='ai_unavailable') so the UI shows the "AI unavailable" banner. Does NOT auto-confirm.
    (2) `save_field`: accept {category, items}, validate category, call saveConfirmedField. Return {ok, saved}.
    Both ride the existing verifySession auth gate (V2/V4 — no new auth surface). Read node_modules/next/dist/docs/ before adding to a route if unsure about handler-export rules (route files export only handlers).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both actions appended to the if-chain; tsc clean; extract_comprehensive returns four lists + ai_available flag; save_field persists via saveConfirmedField.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/kotoiq | category param + items untrusted; validate category enum |
| server→Anthropic | Haiku call; key may be unfunded ($0) — must degrade, not throw |
| server→client domain | extractor reads only already-captured same-host baseline (no arbitrary fetch) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | save_field category param | mitigate | validate category ∈ {keywords,phrases,services,offerings} in saveConfirmedField + action |
| T-12-02 | Tampering | AI-inferred categories drive scoring | mitigate | every item source_type:'ai_inferred', flagged in UI; only user-confirmed feed downstream |
| T-12-03 | DoS (cost) | Haiku over all pages | mitigate | pageSignalsForClaude already slices to 60 pages / 8000 chars |
| T-12-04 | Info disclosure | cross-agency fields write | mitigate | getKotoIQDb auto-injects agency_id; clientProfile.upsert onConflict agency_id,client_id |
| T-12-05 | Repudiation | untracked Claude spend | mitigate | logTokenUsage on the single Haiku call |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/comprehensiveExtractor.test.ts` passes.
- `npx tsc --noEmit` clean.
- Manual (gated on a FUNDED ANTHROPIC_API_KEY — currently $0): call extract_comprehensive for a paired client with baseline rows → four non-empty lists. Without a funded key the action returns heuristic lists + ai_available:false. Build/ship does NOT depend on the key.
</verification>

<success_criteria>
- Four-category extraction works (Haiku when funded; heuristic + ai_available:false otherwise) with no throw and no silent catch.
- saveConfirmedField persists each category to fields[category]; saveConfirmedServices delegates; score_grid reads fields.services[] unchanged.
- tsc clean; pure-fn tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-01-SUMMARY.md`.
</output>
