---
phase: 12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - src/lib/kotoiq/synergyEngine.ts
  - src/app/api/kotoiq/route.ts
  - src/components/kotoiq/SynergySuggestions.jsx
  - tests/kotoiq/synergyEngine.test.ts
autonomous: true
requirements: [SCAN-06]
must_haves:
  truths:
    - "From confirmed services/offerings + industry/business context, a Sonnet engine returns synergistic services + complementary products with rationales"
    - "When ANTHROPIC_API_KEY is absent/unfunded, the engine returns {ok:false, reason:'ai_unavailable'} (no throw) and the UI shows a visible AI-unavailable banner"
    - "Suggestions render in a DISTINCT visual state from confirmed chips; Accept promotes a suggestion into the relevant category via save_field"
  artifacts:
    - path: "src/lib/kotoiq/synergyEngine.ts"
      provides: "Sonnet synergy engine mirroring localStrategistEngine shape, with graceful-degrade guard"
      exports: ["recommendSynergies"]
    - path: "src/components/kotoiq/SynergySuggestions.jsx"
      provides: "Accept-able suggestion chips, distinct visual state"
      min_lines: 80
    - path: "tests/kotoiq/synergyEngine.test.ts"
      provides: "Graceful-degrade + JSON-parse unit tests"
  key_links:
    - from: "src/app/api/kotoiq/route.ts (recommend_synergies)"
      to: "synergyEngine.recommendSynergies"
      via: "action handler"
      pattern: "recommend_synergies"
    - from: "src/components/kotoiq/SynergySuggestions.jsx (Accept)"
      to: "/api/kotoiq save_field"
      via: "promote suggestion → fields[category] as user_added"
      pattern: "save_field"
---

<objective>
Build the synergy engine (WS3): a Sonnet pass over the client's CONFIRMED
services/offerings + industry/business context that suggests complementary/synergistic
services and products as ACCEPT-able chips in a distinct visual state.

Purpose: Turns confirmed inputs into expansion recommendations the user can promote into
their category lists (feeding 12-05/12-06). Mirrors the proven `localStrategistEngine`
Sonnet+JSON+cost+logging shape — but COPIES the graceful-degrade guard from
`serviceInference`, NOT the `ANTHROPIC_API_KEY!` throw.

Output: `synergyEngine.ts` (new), `recommend_synergies` action, `SynergySuggestions.jsx`
(new), pure-fn unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-CONTEXT.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-RESEARCH.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-02-PLAN.md

# Shape to mirror + the throw to AVOID (read before coding):
@src/lib/kotoiq/localStrategistEngine.ts
@src/lib/kotoiq/serviceInference.ts

<interfaces>
<!-- localStrategistEngine.recommendLocalStrategy (:178) is the shape to MIRROR: -->
//   Sonnet model 'claude-sonnet-4-6-20250627', strict-JSON output, try/catch parse +
//   fence-strip, logTokenUsage({feature:'kotoiq_local_strategist', model:'claude-sonnet-4-6'}),
//   cost math $3 in / $15 out per 1M.
// AVOID localStrategistEngine.ts:188 → `new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY! })` (THROWS).
// COPY serviceInference.ts:216 → `if (!process.env.ANTHROPIC_API_KEY) return {ok:false, reason:'ai_unavailable'}`.

// Inputs to read: confirmed fields.services[] + fields.offerings[] (written by 12-01),
//   plus industry/business_name via pick() across clients dedicated cols + onboarding_answers,
//   OR kotoiq_client_profile. Reuse localStrategistEngine's business-context shaping fields:
//   { business_name, business_model, industry }.

// 12-01/12-02 give: save_field action { category, items } for the Accept promotion.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: synergyEngine.ts — Sonnet synergy with graceful-degrade guard</name>
  <files>src/lib/kotoiq/synergyEngine.ts, tests/kotoiq/synergyEngine.test.ts</files>
  <behavior>
    - recommendSynergies({ agencyId, clientId, services[], offerings[], industry?, businessName? }): if !process.env.ANTHROPIC_API_KEY → return { ok:false, reason:'ai_unavailable', synergistic_services:[], complementary_products:[] } (NO throw). Else Sonnet ('claude-sonnet-4-6-20250627') strict-JSON {synergistic_services:[{name, rationale}], complementary_products:[{name, rationale}]} seeded with confirmed services/offerings + business context. try/catch parse + fence-strip. logTokenUsage({feature:'kotoiq_synergy_recommendations', model:'claude-sonnet-4-6', agencyId, metadata:{client_id}}). On parse failure → {ok:false, reason:'parse_error', ...empty}. Never throws, never bare catch{}.
    - Keep the prompt-building + JSON-parse helpers pure where possible (buildSynergyPrompt(inputs), parseSynergyJson(raw)) so they unit-test without network.
    Tests:
    - recommendSynergies returns {ok:false, reason:'ai_unavailable'} when ANTHROPIC_API_KEY unset.
    - parseSynergyJson handles fenced + bare JSON, rejects garbage → empty arrays.
    - buildSynergyPrompt includes the confirmed service names + industry.
  </behavior>
  <action>
    Create src/lib/kotoiq/synergyEngine.ts. Copy the Sonnet+JSON+cost+logTokenUsage scaffolding from localStrategistEngine.recommendLocalStrategy but replace the apiKey line with serviceInference.ts:216's guard. Keep buildSynergyPrompt + parseSynergyJson as pure exported helpers for testing. Do not auto-persist — suggestions persist only on Accept (Task 3). No Next APIs (plain lib).
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/synergyEngine.test.ts</automated>
  </verify>
  <done>Tests pass; engine degrades to {ok:false, reason:'ai_unavailable'} without a key; logTokenUsage called once per Sonnet call; no throw / no bare catch.</done>
</task>

<task type="auto">
  <name>Task 2: recommend_synergies action in /api/kotoiq</name>
  <files>src/app/api/kotoiq/route.ts</files>
  <action>
    Append `recommend_synergies` to the if-chain: read confirmed fields.services[]/offerings[] for the client, resolve industry/business_name via pick() (clients dedicated cols + onboarding_answers) or kotoiq_client_profile, call recommendSynergies. Return { ok, reason, synergistic_services, complementary_products, ai_available: ok }. Rides the existing verifySession gate. Route exports only handlers.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Action wired; returns suggestions + ai_available flag; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: SynergySuggestions.jsx — accept-able suggestion chips</name>
  <files>src/components/kotoiq/SynergySuggestions.jsx</files>
  <action>
    Follow react-best-practices (https://react.dev/reference/react). Create a client component { agencyId, clientId, onAccepted } that fetches recommend_synergies on demand (button-triggered to control Sonnet spend, not on-mount), renders synergistic_services + complementary_products as suggestion chips in a DISTINCT visual state from confirmed chips (e.g. dashed/tip/violet FlagChip variant per DESIGN.md — clearly "suggested, not yet yours"). Each chip shows its one-line rationale (tooltip or subtext). An "Accept" action on a chip promotes it: fetch { action:'save_field', agency_id, client_id, category, items:[{ name, user_added:true, confidence:1.0 }] } where category = 'services' for synergistic_services, 'offerings' for complementary_products. After accept, mark the chip accepted and call onAccepted. When ai_available:false, render a visible "AI unavailable" notice (no silent swallow). Koto primitives only; no reskin.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "require('fs').accessSync('src/components/kotoiq/SynergySuggestions.jsx')"</automated>
  </verify>
  <done>Suggestion chips render in a distinct state with rationales; Accept promotes via save_field as user_added; AI-unavailable surfaced; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/kotoiq | accept payload → save_field re-validates category server-side |
| server→Anthropic | Sonnet; unfunded key must degrade, not throw |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | AI synergies look confirmed | mitigate | distinct "suggested" visual state; only Accept promotes (explicit gate) |
| T-12-10 | DoS (cost) | Sonnet on every render | mitigate | fetch is button-triggered, not on-mount |
| T-12-11 | Info disclosure | $0-credit error swallowed | mitigate | recommendSynergies returns reason:'ai_unavailable' → visible banner |
| T-12-12 | Repudiation | untracked Sonnet spend | mitigate | logTokenUsage on the Sonnet call |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/synergyEngine.test.ts` passes; `npx tsc --noEmit` clean.
- Manual (gated on FUNDED ANTHROPIC_API_KEY — currently $0): trigger synergies → distinct suggestion chips; Accept → item appears in the target category as user_added. Without a funded key: visible "AI unavailable" banner, no crash. Build/ship independent of the key.
</verification>

<success_criteria>
- Sonnet synergy engine with graceful-degrade (no throw, no bare catch) + logTokenUsage.
- Distinct suggestion chips; Accept promotes via save_field as user_added.
- Visible AI-unavailable signal.
</success_criteria>

<output>
After completion, create `.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-03-SUMMARY.md`.
</output>
