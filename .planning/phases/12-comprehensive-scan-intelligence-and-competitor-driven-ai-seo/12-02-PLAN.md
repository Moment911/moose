---
phase: 12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/kotoiq/CategoryChips.jsx
  - src/components/kotoiq/ServiceChips.jsx
autonomous: true
requirements: [SCAN-04, SCAN-05]
must_haves:
  truths:
    - "All four categories (keywords, phrases, services, offerings) render as editable chip groups"
    - "AI-inferred chips are visibly flagged (data-integrity badge); user can select some/all and delete some/all"
    - "User can manually add a custom item to any category (source_type user_added, confidence 1.0)"
    - "Confirming a category saves via save_field; existing ServiceChips behavior is unchanged"
  artifacts:
    - path: "src/components/kotoiq/CategoryChips.jsx"
      provides: "Category-parameterized chips component for all four categories"
      min_lines: 120
    - path: "src/components/kotoiq/ServiceChips.jsx"
      provides: "Thin wrapper rendering <CategoryChips category='services' .../> (back-compat)"
      contains: "CategoryChips"
  key_links:
    - from: "src/components/kotoiq/CategoryChips.jsx"
      to: "/api/kotoiq save_field"
      via: "fetch on Confirm"
      pattern: "save_field"
    - from: "src/components/kotoiq/CategoryChips.jsx"
      to: "/api/kotoiq extract_comprehensive"
      via: "hydration (prop-passed list preferred over per-category fetch)"
      pattern: "extract_comprehensive"
---

<objective>
Generalize `ServiceChips.jsx` into a category-parameterized `CategoryChips.jsx` (WS2)
that renders editable, AI-flagged chips for ANY of the four categories, with manual
entry (WS4) already covered by the existing add-chip path. Keep `ServiceChips` working
as a thin wrapper so `StepGaps.jsx:100` is untouched.

Purpose: One reusable chips component the guided steps (12-06) hydrate four times from a
single `extract_comprehensive` call. Reuses the proven provenance + save flow from
`ServiceChips`; no new persistence (rides 12-01's `save_field`).

Output: `CategoryChips.jsx` (new), `ServiceChips.jsx` reduced to a wrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-CONTEXT.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-RESEARCH.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-01-PLAN.md

# Reuse target (read in full before coding):
@src/components/kotoiq/ServiceChips.jsx
@DESIGN.md

<interfaces>
<!-- ServiceChips.jsx structure read this session: -->
// export default function ServiceChips({ agencyId, clientId, onConfirmed })   (:84)
// - on mount: fetch { action:'infer_services', agency_id, client_id } → seed chips (:102)
// - AiBadge() info/violet AI-inferred badge (:34)
// - ServiceChip({ name, kind, onRemove }) (:53) — kind ∈ ai_inferred|user_added|user_confirmed
// - add-chip path → user_added, confidence 1.0 (:129) — THIS IS WS4 manual entry, already built
// - Confirm CTA → fetch { action:'save_services', agency_id, client_id, services } (:158)
//   provenance mapped: user_added / user_confirmed / ai_inferred (:145-154)
// Built on DESIGN.md koto primitives: SectionHeader, EducationalNote, ActionCallout,
//   CtaButton, FlagChip, Skeleton, KotoKeyframes. Navy #201b51, pink #cb1c6b, cream #faf9f6.

// New actions available from 12-01:
//   extract_comprehensive → { keywords[], phrases[], services[], offerings[], ai_available }
//   save_field → accepts { action:'save_field', agency_id, client_id, category, items }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryChips.jsx (category-parameterized)</name>
  <files>src/components/kotoiq/CategoryChips.jsx</files>
  <action>
    READ https://react.dev/reference/react and node_modules/next/dist/docs/ patterns are not needed (client component, no Next APIs) — but DO follow react-best-practices (stable keys, no derived-state-in-render, effects only for external sync).
    Copy ServiceChips.jsx and parameterize:
    - Props: { agencyId, clientId, category ('keywords'|'phrases'|'services'|'offerings'), title, subtitle, icon, placeholder, seedItems (optional pre-fetched list), inferAction='extract_comprehensive', saveAction='save_field', onConfirmed }.
    - Hydration: if seedItems prop provided, use it (the parent fetches extract_comprehensive ONCE and passes d[category] down — avoids 4 round-trips). Else fetch { action: inferAction, agency_id, client_id } and read d[category].
    - When the source returned ai_available:false, render a VISIBLE "AI unavailable — showing heuristic suggestions" notice (EducationalNote or ErrorState variant) instead of swallowing it.
    - Keep ServiceChip / AiBadge / add-chip (user_added, confidence 1.0 = WS4) / select+delete behavior IDENTICAL.
    - Confirm CTA → fetch { action: saveAction, agency_id, client_id, category, items } with the same provenance mapping (user_added / user_confirmed / ai_inferred). Call onConfirmed(confirmedNames) after save.
    - Use only DESIGN.md koto primitives (no restyle — navy/cream, NOT the deferred blazly reskin).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "require('fs').accessSync('src/components/kotoiq/CategoryChips.jsx')"</automated>
  </verify>
  <done>CategoryChips renders any category; AI items flagged; manual add → user_added; Confirm posts save_field with category; ai_available:false surfaces a visible notice. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce ServiceChips.jsx to a CategoryChips wrapper</name>
  <files>src/components/kotoiq/ServiceChips.jsx</files>
  <action>
    Replace the body of ServiceChips so it returns
    `<CategoryChips category="services" inferAction="infer_services" saveAction="save_services" title="..." subtitle="..." {...props} />`
    preserving the existing props ({ agencyId, clientId, onConfirmed }) and the existing services-specific copy/title so StepGaps.jsx:100 renders identically. Keep using infer_services/save_services for the services category to avoid touching score_grid's read path (fields.services[] is identical either way; using save_services keeps the existing action wired). Do not delete CategoryChips features.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "CategoryChips" src/components/kotoiq/ServiceChips.jsx && echo OK</automated>
  </verify>
  <done>ServiceChips is a thin wrapper over CategoryChips; StepGaps still works; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/kotoiq | category + items from the chip UI; server-side save_field re-validates category |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | AI-inferred chips look authoritative | mitigate | AiBadge/FlagChip on every ai_inferred chip (data-integrity standard) |
| T-12-07 | Tampering | client sends invalid category | mitigate | save_field (12-01) validates category enum server-side; client is not trusted |
| T-12-08 | Info disclosure | $0-credit AI error hidden | accept→mitigate | render visible "AI unavailable" notice from ai_available flag, never silent |
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- Manual (browser, behind Supabase auth — Adam verifies): four CategoryChips groups render; AI badge visible; add a custom keyword → appears as user_added; Confirm persists. Service chips unchanged in StepGaps.
</verification>

<success_criteria>
- CategoryChips works for all four categories with AI flagging, select/delete, manual add, and save_field persistence.
- ServiceChips is a back-compat wrapper; no regression in StepGaps.
- Visible "AI unavailable" signal when ai_available:false.
</success_criteria>

<output>
After completion, create `.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-02-SUMMARY.md`.
</output>
