---
phase: 12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo
plan: 06
type: execute
wave: 4
depends_on: ["12-01", "12-04", "12-05"]
files_modified:
  - src/lib/kotoiq/fastRankStrategyEngine.ts
  - src/app/api/kotoiq/route.ts
  - src/components/kotoiq-wp/steps/StepStrategy.jsx
  - src/components/kotoiq-wp/steps/StepSiteToday.jsx
  - src/components/kotoiq-wp/steps/StepCompetitors.jsx
  - src/components/kotoiq-wp/steps/StepGaps.jsx
  - src/components/kotoiq-wp/GuidedSpine.jsx
  - tests/kotoiq/fastRankStrategy.test.ts
autonomous: false
requirements: [STRAT-01, STRAT-02, UI-01]
must_haves:
  truths:
    - "A new guided 'Your strategy' step runs the semantic Koto tool over confirmed inputs + competitor intel + opportunity list → a fast-rank AI-SEO/GEO/AEO plan"
    - "The strategy composition checks ANTHROPIC_API_KEY first and returns {ok:false, reason:'ai_unavailable'} instead of letting recommendLocalStrategy/buildPlan throw"
    - "Step 2 shows four CategoryChips groups; step 3 shows the 3-lens competitor view; step 4 shows the extensive list; the new strategy step renders clusters/hub/linking/AEO-GEO tactics/build order"
    - "The plan persists via planBuilderEngine + recommendLocalStrategy's existing kotoiq_page_suggestions"
  artifacts:
    - path: "src/lib/kotoiq/fastRankStrategyEngine.ts"
      provides: "Composition over recommendLocalStrategy + semanticAgents + buildPlan + hubBuilder with key-guard"
      exports: ["recommendFastRankStrategy"]
    - path: "src/components/kotoiq-wp/steps/StepStrategy.jsx"
      provides: "New guided strategy step UI"
      min_lines: 80
    - path: "tests/kotoiq/fastRankStrategy.test.ts"
      provides: "Strategy-guard unit test (ai_unavailable without key)"
  key_links:
    - from: "src/components/kotoiq-wp/GuidedSpine.jsx"
      to: "StepStrategy"
      via: "added to STEPS[] + step switch"
      pattern: "strategy"
    - from: "src/app/api/kotoiq/route.ts (recommend_strategy)"
      to: "fastRankStrategyEngine.recommendFastRankStrategy"
      via: "action handler"
      pattern: "recommend_strategy"
---

<objective>
Build the semantic fast-rank strategy engine (WS7) and wire the full guided UI: a new
`<StepStrategy/>` step plus enrichment of steps 2/3/4, composing
`recommendLocalStrategy` + `semanticAgents` + `buildPlan` + `hubBuilder` over confirmed
inputs (12-01) + competitor intel (12-04) + opportunity list (12-05) into a concrete
fast-rank AI-SEO/GEO/AEO plan — with a key-guard so the underlying engines that THROW on
a missing key (`localStrategistEngine:188`, `planBuilderEngine:196`) instead surface a
visible "AI unavailable" signal.

Purpose: The capstone — assembles all prior plans into the user-facing strategy step and
completes the guided spine. Ends in a human-verify checkpoint.

Output: `fastRankStrategyEngine.ts` (new), `recommend_strategy` action, `StepStrategy.jsx`
(new), enriched steps 2/3/4 + GuidedSpine STEPS, a strategy-guard unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-CONTEXT.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-RESEARCH.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-01-PLAN.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-04-PLAN.md
@.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-05-PLAN.md

# Engines to compose (read signatures before coding):
@src/lib/kotoiq/localStrategistEngine.ts
@src/lib/kotoiq/planBuilderEngine.ts
@src/lib/semanticAgents.ts
@src/components/kotoiq-wp/GuidedSpine.jsx
@src/components/kotoiq-wp/steps/StepShell.jsx
@DESIGN.md

<interfaces>
<!-- recommendLocalStrategy (localStrategistEngine.ts:178) → url_structure, topic_clusters[]
//   (kind:'service_x_city', schema_types, internal_link_targets, e_e_a_t_signals),
//   internal_linking_strategy{hub_and_spoke, cross_links[]}, schema_plan{site_wide, service_pages, city_pages},
//   aeo_strategy{target_entities, answer_format_pages, citation_strategy, structured_answers}, attack_plan[].
//   AUTO-PERSISTS clusters → kotoiq_page_suggestions (:248,257). THROWS on missing key (:188) → must guard.
// buildPlan(s, {client_id, agency_id, goal, context}) → PlanDraft persisted to kotoiq_plans/kotoiq_plan_steps. THROWS (:196) → must guard.
// semanticAgents.runQueryGapAnalyzer(...) accepts competitor_pages[] → {primary_angle, query_network[], competitor_gaps,...}.
// hubBuilder.buildHubPage (pure) → CollectionPage + BreadcrumbList + ItemList HTML+JSON-LD.

// GuidedSpine.jsx (read this session): STEPS array at :43 (6 entries: connected, site, competitors, gaps, plan, live).
//   Step switch by index. Spine owns runId, selectedState, selectedCities, confirmedServices; polls run_all_status.
//   Add a 7th 'strategy' entry (recommend inserting BETWEEN gaps and plan, or before live — keep the stepper labels plain-English).
//   StepShell = status pill + EducationalNote + one CTA. Koto primitives: Eyebrow, SectionHeader, EducationalNote,
//   ActionCallout, StatGrid, Stat, FlagChip, CtaButton, Skeleton, ErrorState, KotoKeyframes. Navy/cream — NO reskin.

// Available from prior plans:
//   12-01 extract_comprehensive → 4 lists; save_field. 12-02 CategoryChips. 12-04 competitor_intel. 12-05 opportunity_list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: fastRankStrategyEngine.ts — compose engines behind a key-guard</name>
  <files>src/lib/kotoiq/fastRankStrategyEngine.ts, tests/kotoiq/fastRankStrategy.test.ts</files>
  <behavior>
    - recommendFastRankStrategy({ agencyId, clientId, services[], offerings[], cities[], state, competitorIntel, opportunityList }): FIRST guard — if !process.env.ANTHROPIC_API_KEY → return { ok:false, reason:'ai_unavailable' } WITHOUT calling recommendLocalStrategy/buildPlan (both throw on missing key). Else: call recommendLocalStrategy seeded with confirmed services/offerings + cities + competitor intel + opportunity list (as notes/existing_pages/competitor context); optionally enrich top clusters via semanticAgents.runQueryGapAnalyzer(passing WS5 competitor pages as competitor_pages); persist the executable build order via buildPlan (kotoiq_plans) and rely on recommendLocalStrategy's existing kotoiq_page_suggestions persistence. Return { ok:true, strategy:{ topic_clusters, internal_linking_strategy, schema_plan, aeo_strategy, attack_plan, plan_id } }. Wrap the whole composition so a downstream throw becomes { ok:false, reason } — never bare catch{} (log the reason).
    - buildStrategyContext(inputs) pure helper to shape the recommendLocalStrategy input.
    Tests:
    - recommendFastRankStrategy returns { ok:false, reason:'ai_unavailable' } when ANTHROPIC_API_KEY unset (and does NOT invoke the throwing engines).
    - buildStrategyContext includes confirmed services + cities + competitor names.
  </behavior>
  <action>
    Create src/lib/kotoiq/fastRankStrategyEngine.ts COMPOSING the four existing engines (no re-implementation). The new code is: the key-guard, buildStrategyContext, and the orchestration. Because recommendLocalStrategy + buildPlan use the `ANTHROPIC_API_KEY!` throw, the guard MUST short-circuit before calling them and return reason:'ai_unavailable' (drives the visible UI banner). logTokenUsage is already inside both underlying engines. No Next APIs.
  </action>
  <verify>
    <automated>npx vitest run tests/kotoiq/fastRankStrategy.test.ts</automated>
  </verify>
  <done>Engine composes the four tools behind a key-guard; returns ai_unavailable without a key (no throw); persists via buildPlan + page_suggestions when funded; test passes.</done>
</task>

<task type="auto">
  <name>Task 2: recommend_strategy action in /api/kotoiq</name>
  <files>src/app/api/kotoiq/route.ts</files>
  <action>
    Append `recommend_strategy` to the if-chain: read confirmed fields.services[]/offerings[] + fields.competitor_intel, run/read the opportunity_list, validate {cities[], state}, call recommendFastRankStrategy. Return { ok, reason, strategy, ai_available: ok }. Rides verifySession. Route exports only handlers — read node_modules/next/dist/docs/ if unsure about handler-export rules.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Action wired; returns strategy + ai_available; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: StepStrategy.jsx + GuidedSpine 7th step + enrich steps 2/3/4</name>
  <files>src/components/kotoiq-wp/steps/StepStrategy.jsx, src/components/kotoiq-wp/GuidedSpine.jsx, src/components/kotoiq-wp/steps/StepSiteToday.jsx, src/components/kotoiq-wp/steps/StepCompetitors.jsx, src/components/kotoiq-wp/steps/StepGaps.jsx</files>
  <action>
    Follow react-best-practices (https://react.dev/reference/react) and AGENTS.md (read node_modules/next/dist/docs/ before any new framework pattern). Koto primitives only — NO blazly reskin (deferred).
    - NEW StepStrategy.jsx: client step on StepShell. Button-triggered fetch of recommend_strategy (control Sonnet spend). Render topic_clusters, pillar/hub plan, internal_linking_strategy, schema_plan, aeo_strategy (citation targets, answer-first pages, llms.txt), and the prioritized attack_plan/build order. When ai_available:false → visible "AI unavailable" banner (ErrorState/EducationalNote), no silent swallow.
    - GuidedSpine.jsx: add a 7th STEPS entry { key:'strategy', label:'Your strategy', sub:'Fast-rank plan' } (plain-English, no bare acronyms — matches the spine's convention) inserted between 'gaps' and 'plan' (or before 'live'); add the matching case to the step switch; thread the shared confirmed inputs + selectedState/selectedCities + the competitor_intel/opportunity_list results into StepStrategy via the spine's owned state.
    - StepSiteToday.jsx (step 2): render four CategoryChips groups (keywords/phrases/services/offerings) hydrated from ONE extract_comprehensive call (pass seedItems per category to avoid 4 round-trips).
    - StepCompetitors.jsx (step 3): call competitor_intel (12-04) and render the per-competitor × per-lens set with Blazly-style GEO/AEO visibility bars (per CONTEXT specifics) + DataSourceBadge provenance. Reuse the existing CityPicker. Mark any 'unavailable' lens explicitly.
    - StepGaps.jsx (step 4): swap the single ServiceChips for the four CategoryChips and render the EXTENSIVE list from opportunity_list (12-05) with the bucket grouping + competitor-derived count.
    Keep all changes additive to the existing spine; do not break run_all_status polling or existing cross-step state.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "require('fs').accessSync('src/components/kotoiq-wp/steps/StepStrategy.jsx')" && grep -q "strategy" src/components/kotoiq-wp/GuidedSpine.jsx && echo OK</automated>
  </verify>
  <done>7-step spine renders; step 2 four-category chips; step 3 three-lens competitor view; step 4 extensive list; new strategy step renders the plan or a visible AI-unavailable banner; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — full Phase 12 guided flow</name>
  <action>Human verifies the assembled flow per the steps below; no automated implementation in this task.</action>
  <what-built>The full Phase 12 guided flow: comprehensive four-category extraction (step 2), 3-lens competitor intel (step 3), extensive opportunity list (step 4), and the new fast-rank strategy step — assembled over the Phase 11 spine.</what-built>
  <how-to-verify>
    1. Run `next dev --no-turbo` (React19/Turbopack sidebar bug) and open the KotoIQ-WP Guided tab for a paired client.
    2. Step 2: confirm four CategoryChips groups (keywords/phrases/services/offerings) render with AI badges; add a manual keyword → appears as user_added; Confirm persists.
    3. Step 3: run competitor intel → per-competitor × per-lens view with provenance badges; any failed lens shows 'unavailable' (NOT empty-as-no-competitors).
    4. Step 4: extensive list is larger than the own-only grid (competitor-derived count > 0); buckets intact.
    5. Strategy step: with an UNFUNDED key, confirm a VISIBLE "AI unavailable" banner (no crash). With a FUNDED ANTHROPIC_API_KEY, confirm clusters/hub/linking/AEO-GEO tactics/build order render and the plan persists (kotoiq_plans + kotoiq_page_suggestions).
    6. Confirm navy/cream DESIGN.md styling throughout (no blazly reskin).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/kotoiq | strategy inputs untrusted → validate cities/state |
| server→Anthropic (Sonnet) | recommendLocalStrategy/buildPlan THROW on missing key → must guard |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-21 | DoS (availability) | recommendLocalStrategy/buildPlan throw on $0 key | mitigate | key-guard short-circuits before the throwing engines → reason:'ai_unavailable' |
| T-12-22 | Info disclosure | $0-credit error swallowed | mitigate | visible "AI unavailable" banner in StepStrategy from ai_available flag |
| T-12-23 | Tampering | AI strategy presented as verified fact | mitigate | strategy clearly AI-generated (flagged); competitor facts carry DataSourceBadge provenance from 12-04 |
| T-12-24 | DoS (cost) | Sonnet strategy on every render | mitigate | recommend_strategy fetch button-triggered, not on-mount |
| T-12-25 | Repudiation | untracked Sonnet spend | mitigate | logTokenUsage already inside recommendLocalStrategy + buildPlan |
</threat_model>

<verification>
- `npx vitest run tests/kotoiq/fastRankStrategy.test.ts` passes; `npx tsc --noEmit` clean.
- Human-verify checkpoint (above) covers the assembled flow.
- Runtime note: strategy output is gated on a FUNDED ANTHROPIC_API_KEY (currently $0). Build/ship + tsc are NOT gated — the key-guard returns ai_unavailable and the UI shows a banner. Verify real strategy output once a funded key is set.
</verification>

<success_criteria>
- New strategy step composes the four engines behind a key-guard (no throw, no bare catch); persists via buildPlan + page_suggestions.
- Steps 2/3/4 enriched (four-category chips, 3-lens competitor view, extensive list); 7-step spine intact.
- Visible AI-unavailable signal everywhere a Claude step can't run.
- DESIGN.md navy/cream preserved (no reskin).
</success_criteria>

<output>
After completion, create `.planning/phases/12-comprehensive-scan-intelligence-and-competitor-driven-ai-seo/12-06-SUMMARY.md`.
</output>
