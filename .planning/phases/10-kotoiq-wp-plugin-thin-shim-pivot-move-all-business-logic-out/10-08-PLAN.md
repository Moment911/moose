---
phase: 10-kotoiq-wp-plugin-thin-shim-pivot
plan: 08
type: execute
wave: 5
depends_on: [10-06]
files_modified:
  - src/lib/wp-shim/ports/sitemapPort.ts
  - src/lib/wp-shim/ports/sitemapPort.test.ts
  - src/lib/wp-shim/ports/sitemapServe.ts
  - src/lib/wp-shim/index.ts
  - src/app/api/kotoiq-shim-cron/sitemap-refresh/route.ts
  - wp-plugin-kotoiq-shim/includes/sitemap-server.php
  - wp-plugin-kotoiq-shim/kotoiq-shim.php
  - vercel.json
autonomous: true
requirements:
  - SHIM-SITEMAP-COMPOSER
must_haves:
  truths:
    - "Dashboard composes sitemap.xml (with image, video, FAQ sub-sitemaps) in TypeScript using existing sitemapCrawler.ts; pushes via file.write verb to wp-content/uploads/kotoiq/sitemap.xml"
    - "WP-core /wp-sitemap.xml remains active as fallback when the pushed file is missing or stale (per CONTEXT.md D-Sitemap-strategy USER-LOCKED — defense in depth)"
    - "Plugin adds a generic XML-server route that serves wp-content/uploads/kotoiq/sitemap.xml at /kotoiq-sitemap.xml; NO sitemap-composition logic in plugin source"
    - "Vercel Cron schedule rebuilds sitemap daily for every paired site; per-site override via API call"
    - "Plugin source contains ZERO references to sitemap heuristics, FAQ schema regex, image/video extraction, priority/changefreq rules — all that lives in sitemapCrawler.ts"
  artifacts:
    - path: "src/lib/wp-shim/ports/sitemapPort.ts"
      provides: "composeSitemap + pushSitemap + refreshAllSites helpers using existing sitemapCrawler.ts"
      exports: ["composeSitemap", "pushSitemap", "refreshSitemap", "refreshAllSites"]
    - path: "src/lib/wp-shim/ports/sitemapServe.ts"
      provides: "Dashboard-side wrapper for testing the served URL"
      exports: ["fetchServedSitemap"]
    - path: "src/app/api/kotoiq-shim-cron/sitemap-refresh/route.ts"
      provides: "Vercel Cron endpoint — invoked daily to rebuild + push for all paired sites"
    - path: "wp-plugin-kotoiq-shim/includes/sitemap-server.php"
      provides: "Generic /kotoiq-sitemap.xml rewrite rule that serves wp-content/uploads/kotoiq/sitemap.xml"
      contains: "kotoiq-sitemap.xml"
    - path: "vercel.json (updated)"
      provides: "Cron schedule for sitemap-refresh route"
      contains: "kotoiq-shim-cron/sitemap-refresh"
  key_links:
    - from: "sitemapPort.ts"
      to: "src/lib/sitemapCrawler.ts (existing — 420 LOC)"
      via: "Imports composeXml + relevant builders; sitemapPort orchestrates per-site fetch + compose + push"
      pattern: "Dashboard-side composition; plugin just serves the file"
    - from: "sitemap-server.php"
      to: "wp-content/uploads/kotoiq/sitemap.xml"
      via: "WP rewrite rule maps /kotoiq-sitemap.xml → readfile() of the static XML"
      pattern: "Static file serve; no PHP generation; falls back to WP core sitemap if missing"
---

<objective>
Port v3's sitemap composition (modules/seo-sitemap.php — 499 LOC including
6 sub-sitemap generators, image extraction regex, video extraction regex,
FAQ schema detection) to dashboard TypeScript, using the existing
src/lib/sitemapCrawler.ts (420 LOC). Push the composed XML to each paired
site via file.write to wp-content/uploads/kotoiq/sitemap.xml. The shim
plugin adds a generic XML-server that serves whatever file is there.

Per CONTEXT.md D-Sitemap-strategy (USER-LOCKED):
- Dashboard generates the sitemap XML and pushes via file.write
- WP-core /wp-sitemap.xml left active as fallback (defense in depth)
- Plugin no longer contains sitemap-composition logic

Per research §Module Port Plan — seo-sitemap: 6 sub-sitemap generators
(index, posts, images, video, faq, lastmod) all move to TypeScript. Plugin
keeps only a single rewrite rule + static-file serve handler.

Per CLAUDE.md memory: Anthropic API calls (none expected in sitemap
composition, but if any creep in — e.g., AI-generated FAQ schema —
must log via logTokenUsage with feature='shim_sitemap_*').

Output:
- sitemapPort.ts (TS composer + push)
- sitemap-server.php (PHP rewrite + serve)
- Vercel Cron route + schedule
- Tests for the composer
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-06-SUMMARY.md
@src/lib/sitemapCrawler.ts
@wp-plugin-kotoiq/includes/modules/seo-sitemap.php
@src/lib/wp-shim/verbs/index.ts
@src/lib/wp-shim/wpFetch.ts
@vercel.json

<interfaces>
<!-- The existing sitemapCrawler.ts (420 LOC) — read its actual exports before importing -->
<!-- Likely exports based on file name + research mention:                              -->
<!--   composeSitemapIndex(siteUrl, subsitemaps) → string                               -->
<!--   composeUrlSetSitemap(urls) → string                                              -->
<!--   composeImageSitemap(posts) → string                                              -->
<!--   composeVideoSitemap(posts) → string                                              -->
<!--   composeFaqSitemap(posts) → string                                                -->
<!--   extractImagesFromHtml(html) → ImageRef[]                                         -->
<!--   extractVideosFromHtml(html) → VideoRef[]                                         -->
<!--   detectFaqSchema(html) → FaqEntry[]                                               -->
<!-- If the actual API differs, adapt — this file is the canonical source.              -->

<!-- The pushed sitemap file path -->
SITEMAP_PUSH_PATH = 'wp-content/uploads/kotoiq/sitemap.xml'
SITEMAP_IMAGES_PATH = 'wp-content/uploads/kotoiq/sitemap-images.xml'
SITEMAP_VIDEOS_PATH = 'wp-content/uploads/kotoiq/sitemap-videos.xml'
SITEMAP_FAQ_PATH = 'wp-content/uploads/kotoiq/sitemap-faq.xml'
SITEMAP_INDEX_PATH = 'wp-content/uploads/kotoiq/sitemap.xml'  // index is the entry point

<!-- The serve URL -->
The pushed sitemap is served from {site_url}/kotoiq-sitemap.xml via the
rewrite rule in includes/sitemap-server.php. GSC + IndexNow get pointed at
this URL.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sitemapPort + composeSitemap + push helpers + cron endpoint</name>
  <files>
    src/lib/wp-shim/ports/sitemapPort.ts,
    src/lib/wp-shim/ports/sitemapPort.test.ts,
    src/lib/wp-shim/ports/sitemapServe.ts,
    src/app/api/kotoiq-shim-cron/sitemap-refresh/route.ts,
    vercel.json
  </files>
  <read_first>
    - src/lib/sitemapCrawler.ts (FULL FILE — confirm actual exports + signatures)
    - wp-plugin-kotoiq/includes/modules/seo-sitemap.php (v3 reference — kotoiq_sitemap_index/posts/images/video/faq — port shape, NOT line-by-line)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md (§Module Port Plan — seo-sitemap)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md (D-Sitemap-strategy)
    - src/lib/wp-shim/verbs/index.ts (fileWrite + querySelect wrappers)
    - vercel.json (existing schedule — additive only)
    - https://vercel.com/docs/cron-jobs (cron schedule cron-string format, route auth)
    - AGENTS.md (Next.js v16 — verify App Router export shape against node_modules/next/dist/docs before writing the route)
  </read_first>
  <behavior>
    composeSitemap(siteUrl, creds, opts?):
    - Fetches posts via wp/v2/posts (chunked, status=publish) — use wpFetch
    - Fetches pages via wp/v2/pages
    - For each: extract images + videos + FAQ entries via sitemapCrawler.ts helpers
    - Builds: index.xml + posts.xml + images.xml + videos.xml + faq.xml (5 files; FAQ optional if 0 entries)
    - Returns Record<string, string> mapping the 5 paths to XML strings

    pushSitemap(siteUrl, composedXmls):
    - For each {path, xml}: shimRpc fileWrite (already TS-side-guarded to wp-content/uploads/kotoiq/**)
    - Pings Google + Bing via dashboard's own fetch (NOT via shim) — but only if it's still useful (research note: Google deprecated sitemap-ping in 2023; ping Bing only)
    - Emits a dashboard-side log + writes a koto_wp_dual_run_log row if dual-run is active (Plan 10)

    refreshSitemap(supabase, agencyId, siteId):
    - Loads site creds
    - Calls composeSitemap → pushSitemap
    - Returns timing + per-file size stats
    - Updates koto_wp_sites.last_sitemap_pushed_at (new column added in this plan via inline migration — see action below)

    refreshAllSites(supabase):
    - SELECT all paired v4 sites
    - Sequentially refresh each (parallelism deferred; v4 fleet is small + we don't want to hammer shim)
    - Returns aggregate report

    Cron endpoint /api/kotoiq-shim-cron/sitemap-refresh:
    - Vercel Cron with daily schedule
    - Verifies cron secret (Vercel sets `Authorization: Bearer <CRON_SECRET>` env)
    - Calls refreshAllSites(supabaseAdmin)
    - Returns aggregate stats
  </behavior>
  <action>
    1. Read `src/lib/sitemapCrawler.ts` carefully — note the actual export names. The plan-text uses guesses; align to the actual API.

    2. Create `src/lib/wp-shim/ports/sitemapPort.ts`:
       - Import from sitemapCrawler.ts (adapt to actual API)
       - `interface ComposedSitemap { path: string; xml: string; entry_count: number; }`
       - `export async function composeSitemap(siteUrl: string, creds: AppCreds, opts?: {includeImages?: boolean; includeVideos?: boolean; includeFAQ?: boolean; postTypes?: string[]; maxPosts?: number}): Promise<ComposedSitemap[]>`:
         a. Default options: include all sub-sitemaps; postTypes=['post','page']
         b. For each postType, page through wp/v2/posts?type=...&per_page=100 (cap at 50 pages = 5000 posts)
         c. For each post, extract featured image URL + content embedded images via sitemapCrawler's helpers
         d. Aggregate URLs into the appropriate sub-sitemap structure
         e. Compose each XML string via sitemapCrawler.compose* functions
         f. Return array of `{path, xml, entry_count}`
       - `export async function pushSitemap(siteUrl: string, composed: ComposedSitemap[]): Promise<{ok: boolean; pushed: number; errors: any[]}>`:
         a. For each: `await fileWrite(siteUrl, {path: c.path, content_base64: Buffer.from(c.xml).toString('base64'), mode: 'overwrite'})`
         b. Aggregate results
       - `export async function refreshSitemap(supabase, agencyId: string, siteId: string): Promise<RefreshResult>`:
         a. Load site row + creds
         b. composeSitemap → pushSitemap
         c. Update koto_wp_sites.last_sitemap_pushed_at = now()
         d. Return {site_id, site_url, ok, files: composed.length, total_urls, error?}
       - `export async function refreshAllSites(supabase): Promise<{processed, errors: []}>`:
         a. SELECT id, agency_id, site_url FROM koto_wp_sites WHERE shim_version = 'v4' AND dual_run_state IN ('active', 'promoted')
         b. Sequential refreshSitemap per site
         c. Aggregate
       - **NOTE on koto_wp_sites schema:** the column `last_sitemap_pushed_at timestamptz` may need to be added. Since Plan 01's migration is already applied, this plan needs a SECOND migration. **Do NOT add another migration here** — instead, store the timestamp in a new option per site (via optionUpdate to `kotoiq_shim_last_sitemap_pushed_at` on the WP side) AND log a koto_wp_push_history row with template_id=null + status='sitemap_refresh' for dashboard-side audit. The koto_wp_push_history schema already supports this.

    3. Create `src/lib/wp-shim/ports/sitemapPort.test.ts`:
       - Mock wpFetch + shimRpc + sitemapCrawler functions
       - Tests:
         - composeSitemap returns 5 files for a fixture site with images/videos/FAQ
         - composeSitemap omits videos+faq when fixture has none
         - pushSitemap calls fileWrite once per file
         - refreshSitemap inserts a koto_wp_push_history row with status='sitemap_refresh'
         - refreshAllSites iterates only sites with shim_version='v4'
         - Pagination: 250 posts → 3 wpFetch calls (100+100+50)

    4. Create `src/lib/wp-shim/ports/sitemapServe.ts`:
       - `export async function fetchServedSitemap(siteUrl: string): Promise<{ok: boolean; xml?: string; last_modified?: string; status: number}>`
       - Plain fetch to `{siteUrl}/kotoiq-sitemap.xml` (no auth; sitemap is public)
       - Used by Plan 10/11 to verify the pushed sitemap is being served correctly

    5. Create `src/app/api/kotoiq-shim-cron/sitemap-refresh/route.ts`:
       - Use Next.js App Router conventions (per AGENTS.md — confirm signature from node_modules/next/dist/docs)
       - Export GET (Vercel Cron uses GET by default)
       - Verify cron auth: `headers.get('authorization')` matches `'Bearer ' + process.env.CRON_SECRET`
       - On unauth: return 401
       - On auth: call refreshAllSites(createServiceClient()) and return JSON aggregate
       - Cap to 5-minute execution (Vercel Hobby limit; Pro is 5 min default too without config — research-cited)
       - Logs each site's result via console.log (Vercel logs) for cron-monitoring

    6. Update `vercel.json`:
       - Read current file
       - Add to `crons` array (additive):
         ```json
         { "path": "/api/kotoiq-shim-cron/sitemap-refresh", "schedule": "0 4 * * *" }
         ```
         (daily at 04:00 UTC)
       - If `crons` array doesn't exist, add it
       - **DO NOT** overwrite any existing cron entries
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/wp-shim/ports/sitemapPort.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four files exist
    - `grep -c "import.*sitemapCrawler" src/lib/wp-shim/ports/sitemapPort.ts` returns at least 1 (reuses existing crawler — no algorithm duplication)
    - `grep -c "fileWrite" src/lib/wp-shim/ports/sitemapPort.ts` returns at least 1
    - `grep -c "wp-content/uploads/kotoiq" src/lib/wp-shim/ports/sitemapPort.ts` returns at least 1 (path matches plugin's allowed-dir)
    - `grep -c "kotoiq-shim-cron/sitemap-refresh" vercel.json` returns 1
    - `grep -cE "schedule.*0 4|0 4.*schedule" vercel.json` returns at least 1 (cron schedule present — matches daily 04:00 spec)
    - `grep -c "process.env.CRON_SECRET\\|CRON_SECRET" src/app/api/kotoiq-shim-cron/sitemap-refresh/route.ts` returns at least 1 (cron auth)
    - `npm run test -- --run src/lib/wp-shim/ports/sitemapPort.test.ts` exits 0
    - `npx tsc --noEmit --project tsconfig.json 2>&1 | grep "src/lib/wp-shim/ports/sitemapPort\\|src/app/api/kotoiq-shim-cron" | grep -c "error TS"` returns 0
  </acceptance_criteria>
  <done>
    Dashboard composes 5 sub-sitemaps (or fewer) in TypeScript via existing sitemapCrawler.ts. Pushes via fileWrite to wp-content/uploads/kotoiq/. Vercel Cron schedules daily refresh for all v4 paired sites. Test suite covers happy paths + edge cases.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PHP sitemap-server (generic XML serve with WP-core fallback) + index.ts re-export</name>
  <files>
    wp-plugin-kotoiq-shim/includes/sitemap-server.php,
    wp-plugin-kotoiq-shim/kotoiq-shim.php,
    src/lib/wp-shim/index.ts
  </files>
  <read_first>
    - wp-plugin-kotoiq/includes/modules/seo-sitemap.php (v3 reference for the rewrite rule pattern lines 41-95 — port shape, not heuristics)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md (D-Sitemap-strategy fallback story)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md (§Module Port Plan — seo-sitemap, §Pitfall 6 — Sitemap.xml served from disk goes stale)
    - https://developer.wordpress.org/news/2020/07/22/new-xml-sitemaps-functionality-in-wordpress-5-5/ (WP-core sitemap fallback)
  </read_first>
  <behavior>
    sitemap-server.php:
    - Register WP rewrite rule: /kotoiq-sitemap.xml → query_var koto_sitemap_serve=1
    - On template_redirect: check query_var, if set:
      * Check file exists at wp-content/uploads/kotoiq/sitemap.xml AND mtime < 25 hours old
      * If yes: serve the file (Content-Type: application/xml, ETag from sha1 of file contents, X-Robots-Tag: noindex on the sitemap itself, sane Cache-Control)
      * If no (file missing OR stale): redirect 302 to /wp-sitemap.xml (WP-core fallback)
    - Similar rules for /kotoiq-sitemap-images.xml, /kotoiq-sitemap-videos.xml, /kotoiq-sitemap-faq.xml
    - Plugin source contains NO XML generation, NO image extraction, NO video extraction, NO FAQ detection, NO priority/changefreq heuristics — just file serve + fallback redirect

    No verbs added in this file. The sitemap.xml file gets there via file.write (Plan 04 verb). This file is pure runtime, like content-rotation shortcode.

    On activation: must add the rewrite rule + flush rules.
    On deactivation: remove rewrite + flush.
  </behavior>
  <action>
    1. Create `wp-plugin-kotoiq-shim/includes/sitemap-server.php`:
       - Header comment — INTENTIONALLY GENERIC:
         ```
         /**
          * Generic static-file XML server. Serves a pushed XML at
          * /kotoiq-sitemap.xml; falls back to WP core's built-in
          * /wp-sitemap.xml if the pushed file is missing or stale.
          *
          * Sub-paths supported:
          *   /kotoiq-sitemap.xml         → wp-content/uploads/kotoiq/sitemap.xml
          *   /kotoiq-sitemap-images.xml  → wp-content/uploads/kotoiq/sitemap-images.xml
          *   /kotoiq-sitemap-videos.xml  → wp-content/uploads/kotoiq/sitemap-videos.xml
          *   /kotoiq-sitemap-faq.xml     → wp-content/uploads/kotoiq/sitemap-faq.xml
          *
          * The pushed XML is composed externally (not by this plugin); this
          * file is purely a static-file serve layer with a freshness gate.
          *
          * @package KotoIQ Shim
          */
          ```
       - On `init`:
         ```php
         add_rewrite_rule('^kotoiq-sitemap(?:-(images|videos|faq))?\\.xml$', 'index.php?koto_sitemap_serve=$matches[1]&koto_sitemap=1', 'top');
         ```
       - On `query_vars` filter: register `koto_sitemap_serve` + `koto_sitemap`
       - On `template_redirect`:
         ```php
         $is_sitemap = (int) get_query_var('koto_sitemap', 0);
         if (!$is_sitemap) return;
         $sub = get_query_var('koto_sitemap_serve', '');
         $sub = is_string($sub) ? $sub : '';
         $name = $sub ? "sitemap-{$sub}.xml" : 'sitemap.xml';
         $path = WP_CONTENT_DIR . '/uploads/kotoiq/' . $name;
         $stale_threshold = 25 * HOUR_IN_SECONDS;

         if (!file_exists($path) || (time() - filemtime($path)) > $stale_threshold) {
             // Fall back to WP-core sitemap; only viable for the main sitemap
             $fallback = $sub ? home_url('/wp-sitemap.xml') : home_url('/wp-sitemap.xml');
             wp_redirect($fallback, 302, 'KotoIQ Shim');
             exit;
         }

         $etag = hash_file('sha1', $path);
         status_header(200);
         header('Content-Type: application/xml; charset=UTF-8');
         header('X-Robots-Tag: noindex, follow');
         header('Cache-Control: public, max-age=3600');
         header('ETag: "' . $etag . '"');
         readfile($path);
         exit;
         ```
       - On `kotoiq_shim_activate` hook (called from kotoiq-shim.php's activation handler): `flush_rewrite_rules();` after the rewrite is added
       - On `kotoiq_shim_deactivate` hook (called from uninstall flow per Plan 02): `flush_rewrite_rules();`
       - **DO NOT** include sitemap CONTENT generation, ETag manipulation beyond sha1 of contents, image/video/FAQ schema regex anywhere in this file. The file's job is "serve XML at static path with freshness check + fallback."

    2. Update `wp-plugin-kotoiq-shim/kotoiq-shim.php`:
       - `require_once KOTOIQ_SHIM_DIR . 'includes/sitemap-server.php';`
       - In the activation hook, after the existing options setup, add: `do_action('kotoiq_shim_activate');` then `flush_rewrite_rules();`
       - In the deferred-deactivate handler (already exists per Plan 02), add `do_action('kotoiq_shim_deactivate');` before deactivate_plugins. (This is a small change — verify by reading the file first.)

    3. Update `src/lib/wp-shim/index.ts`:
       - Re-export everything from sitemapPort + sitemapServe:
         ```typescript
         export * from './ports/sitemapPort';
         export * from './ports/sitemapServe';
         ```

    4. Run PHP syntax check + LOC budget:
       - `find wp-plugin-kotoiq-shim -name '*.php' -exec php -l {} \;` → all clean
       - `node scripts/wp-plugin-loc-budget.cjs --plugin-dir wp-plugin-kotoiq-shim --budget 500 --strict` → exits 0 (sitemap-server.php is ~50 LOC of business logic)

    5. Verify IP-clean: the sitemap-server source must mention NO sitemap composition concepts. Acceptance criteria below checks this.
  </action>
  <verify>
    <automated>find wp-plugin-kotoiq-shim -name '*.php' -exec php -l {} \; 2>&1 | grep -c "No syntax errors"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wp-plugin-kotoiq-shim/includes/sitemap-server.php`
    - `grep -c "kotoiq-sitemap\\.xml\\|kotoiq-sitemap-" wp-plugin-kotoiq-shim/includes/sitemap-server.php` returns at least 1 (rewrite rule pattern present)
    - `grep -c "wp-sitemap.xml" wp-plugin-kotoiq-shim/includes/sitemap-server.php` returns at least 1 (WP-core fallback present)
    - `grep -c "readfile\\|filemtime" wp-plugin-kotoiq-shim/includes/sitemap-server.php` returns at least 1 (static-serve pattern)
    - `grep -c "stale\\|HOUR_IN_SECONDS\\|25" wp-plugin-kotoiq-shim/includes/sitemap-server.php` returns at least 1 (freshness threshold present)
    - **Critical IP-leak check** — sitemap-server.php must NOT include ANY composition concept: `grep -ciE "priority|changefreq|extract_image|extract_video|faq_schema|image_extension|video_url|lastmod_computed" wp-plugin-kotoiq-shim/includes/sitemap-server.php` returns 0
    - Plugin source overall still IP-clean: `grep -rciE "yoast|rank_math|focus_keyword|seo[_ ]?score|sitemap[_ ]?priority|priority.*changefreq|image_sitemap|video_sitemap|faq_sitemap|page[_ ]?factory|hyperlocal" wp-plugin-kotoiq-shim/ | grep -v ':0$'` returns no hits
    - LOC budget --strict: `node scripts/wp-plugin-loc-budget.cjs --plugin-dir wp-plugin-kotoiq-shim --budget 500 --strict` exits 0
    - `grep -c "sitemapPort\\|sitemapServe" src/lib/wp-shim/index.ts` returns at least 2
    - PHP syntax: all files clean
  </acceptance_criteria>
  <done>
    Plugin serves /kotoiq-sitemap.xml as a static file from wp-content/uploads/kotoiq/ with WP-core fallback when stale. ZERO sitemap composition logic in plugin source. Dashboard owns full composition algorithm via TypeScript. Plan 11 cutover playbook will exercise the full daily refresh + serve flow.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard cron → site shim | Daily push of XML; signed envelope via fileWrite verb. |
| Plugin → static file → web requester | Public read; serves whatever's at wp-content/uploads/kotoiq/sitemap.xml. |
| Vercel Cron → /api/kotoiq-shim-cron/sitemap-refresh | Cron secret in env; rejects unauth requests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08-01 | Information Disclosure | Sitemap XML contains URLs that should be private (drafts, private posts) | mitigate | composeSitemap filters wp/v2/posts to status=publish only. Test fixture asserts only-published is included. |
| T-10-08-02 | Tampering | An attacker pushes a malicious XML via stolen cron secret | mitigate | Cron secret is in Vercel env; standard rotation policy. Sitemap content is signed by file.write verb which requires the Ed25519 envelope — so even with CRON_SECRET an attacker can't push to WP. |
| T-10-08-03 | Information Disclosure | Stale sitemap reveals deleted posts that no longer exist | mitigate | 25-hour freshness gate; sites with broken cron get WP-core fallback (which is always current). |
| T-10-08-04 | Denial of Service | Cron runs against 100 sites sequentially → exceeds Vercel function timeout | accept (per research §Performance) | At v1 scale (~10 sites) this completes in minutes. Beyond ~30 sites: split into parallel batches via Inngest/QStash in M2. |
| T-10-08-05 | Tampering | Plugin source reveals composition algorithm via sitemap-server.php | mitigate | sitemap-server.php contains ONLY file-serve + fallback. Acceptance grep enforces no composition strings. |
| T-10-08-06 | Information Disclosure | The served sitemap is indexed by Google as a regular page | mitigate | X-Robots-Tag: noindex on the sitemap response. WP-core sitemap also sets this. |
</threat_model>

<verification>
- composeSitemap returns 1-5 files based on content
- pushSitemap calls fileWrite for each composed file
- Vercel Cron schedule installed (vercel.json grep)
- /api/kotoiq-shim-cron/sitemap-refresh requires CRON_SECRET
- Plugin serves /kotoiq-sitemap.xml from disk + falls back to wp-core
- ZERO composition concepts in plugin source (grep)
- LOC budget still under 500
</verification>

<success_criteria>
- Dashboard can rebuild + push sitemap.xml for any paired site in one async call
- Vercel Cron rebuilds + pushes daily for all v4 paired sites
- Plugin serves the pushed file or falls back to WP core
- v3 plugin's 6 sub-sitemap functions are now zero — all logic moved to dashboard
- Plans 09-12 unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-08-SUMMARY.md` recording:
- Confirmation that src/lib/sitemapCrawler.ts exports match what was assumed; note any divergence + adaptation
- Daily cron schedule in vercel.json (exact cron string)
- Test count for sitemapPort.test.ts
- Confirmation IP-clean grep returns 0 across plugin source
- Final LOC for sitemap-server.php
</output>
