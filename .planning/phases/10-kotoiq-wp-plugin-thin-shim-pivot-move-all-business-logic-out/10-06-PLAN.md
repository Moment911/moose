---
phase: 10-kotoiq-wp-plugin-thin-shim-pivot
plan: 06
type: execute
wave: 4
depends_on: [10-04, 10-05]
files_modified:
  - wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php
  - wp-plugin-kotoiq-shim/includes/rpc/verb-table.php
  - wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php
  - wp-plugin-kotoiq-shim/kotoiq-shim.php
  - src/lib/wp-shim/verbs/index.ts
  - src/lib/wp-shim/verbs/verbs.test.ts
autonomous: true
requirements:
  - SHIM-ELEMENTOR-AND-ROTATION
must_haves:
  truths:
    - "elementor.save verb invokes Elementor's `Document::save()` API (NOT direct postmeta write) — preserves CSS regen, revisions, attribute validation, version pinning"
    - "elementor.clone verb reproduces v3's clone behavior but accepts a `meta_prefix_allowlist` arg from the dashboard instead of hardcoding `rank_math_` and `_koto_` prefixes"
    - "All 27 verbs now have real handlers — verb-table.php has zero stubs"
    - "koto_rotate shortcode exists as a GENERIC variant-picker: parses generic attributes (cache, section, pin), splits content on `|||KOTO_VARIANT|||`, caches selection per (post_id, section) via transients, picks via array_rand"
    - "Shortcode source comment is intentionally minimal — does NOT mention SEO, hyperlocal pages, rotation strategy, or any KotoIQ-specific use case"
    - "Side-by-side coexistence with v3 plugin: if `shortcode_exists('koto_rotate')` returns true (v3 already loaded), this shim does NOT re-register (mirrors v3's own defensive check)"
    - "kotoiq_force_css_regen + kotoiq_get_service_user_id helpers ported with generic names — `kotoiq_shim_force_css_regen`, `kotoiq_shim_get_service_user_id`"
  artifacts:
    - path: "wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php"
      provides: "elementor.save + elementor.clone — the two host-bound verbs"
      contains: "Document::save"
      min_lines: 100
    - path: "wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php"
      provides: "[koto_rotate] generic variant-picker"
      contains: "shortcode_exists"
    - path: "src/lib/wp-shim/verbs/index.ts (extended)"
      provides: "elementorSave + elementorClone wrappers"
      exports: ["elementorSave", "elementorClone"]
  key_links:
    - from: "src/lib/wp-shim/verbs/elementorSave"
      to: "verbs-elementor.php kotoiq_shim_verb_elementor_save"
      via: "shimRpc(site, 'elementor.save', {post_id|'new', elementor_data: Array, page_settings?, status?, title?, slug?, post_type?, idempotency_key})"
      pattern: "Replaces v3 PUT /builder/elementor/{id}"
    - from: "Dashboard template-push flow (Plan 09)"
      to: "elementor.save + meta.update + taxonomy.assign"
      via: "Composes the variable-substituted JSON tree dashboard-side, then sends through elementor.save for the Elementor write"
      pattern: "Option B push flow per CONTEXT.md D-Page-design-model"
---

<objective>
Implement the two host-bound verbs that MUST stay in the plugin
(`elementor.save`, `elementor.clone`) and the generic `[koto_rotate]`
shortcode that replaces v3's content-rotation module.

Per CONTEXT.md D-Page-design-model (USER-LOCKED): Option B page-design model
— the dashboard captures Elementor JSON from a sandbox site and pushes
filled-in copies to N sites via elementor.save. NO Koto-side canvas in
Phase 10.

Per research §Module Port Plan — elementor-builder (MED IP, mostly kept):
the verb signature is generic (post_id + JSON), but its inner work
(`Document::save()` + CSS regen + revision capping) is Elementor-specific.
This is accepted leakage: the verb name reveals "we can save Elementor
data" — that's WordPress integration, not IP.

Per research §Module Port Plan — content-rotation: the shortcode itself
is generic shortcode logic that any A/B testing plugin would have. Per
D-Snippets+Access reasoning: keep as generic primitive. The decision of
WHICH variants populate the shortcode is dashboard-driven (Plan 09's push
flow composes the `[koto_rotate]A|||B|||C[/koto_rotate]` content).

After this plan: ALL 27 verbs implemented. The shim is contract-complete.
Plans 07-09 port the dashboard business logic to TypeScript on top of
these verbs. Plans 10-12 handle cutover.

Output:
- elementor.save + elementor.clone PHP handlers
- koto_rotate shortcode (ported with generic comment/strings)
- TypeScript wrappers
- verb-table.php fully populated (27 / 27 real handlers)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md
@wp-plugin-kotoiq/includes/modules/elementor-builder.php
@wp-plugin-kotoiq/includes/modules/content-rotation.php
@wp-plugin-kotoiq-shim/includes/rpc/verb-table.php
@wp-plugin-kotoiq-shim/kotoiq-shim.php
@src/lib/wp-shim/verbs/index.ts

<interfaces>
<!-- elementor.save verb -->
elementor.save args (mirrors v3 PUT /builder/elementor/{id}):
  post_id          : int | 'new'                       // 'new' creates a new post
  elementor_data   : Array (NOT string — array of element trees)
  page_settings    : Array (optional)
  status           : 'draft'|'publish'|'private' (optional, default preserve)
  title            : string (required when post_id='new')
  slug             : string (optional)
  post_type        : 'page'|'post'|... (optional, default 'page' when 'new')
  idempotency_key  : string (optional; prior write with same key skipped)

elementor.save response:
  ok                  : true
  post_id             : int
  url                 : string
  status              : string
  elementor_version   : string|null
  css_regenerated     : bool
  idempotent          : bool        // true when skipped due to matching idempotency_key
  element_count       : int

<!-- elementor.clone verb -->
elementor.clone args (mirrors v3 POST /builder/clone, generalized):
  source_post_id          : int
  title                   : string (optional, default appends ' (Clone)')
  slug                    : string (optional)
  status                  : 'draft'|'publish'|'private' (default 'draft')
  elementor_data          : Array (optional — overrides the cloned data)
  page_settings           : Array (optional — overrides cloned settings)
  body_html               : string (optional — overwrites post_content with [koto_rotate] shortcodes etc.)
  post_meta               : Record<string, string> (optional)
  meta_prefix_allowlist   : string[] (REQUIRED if post_meta is passed; lists allowed key prefixes — dashboard decides which, NOT hardcoded)
  idempotency_key         : string (optional)

elementor.clone response:
  ok                     : true
  post_id                : int
  source_id              : int
  url                    : string
  status                 : string

<!-- koto_rotate shortcode (PHP — same as v3 shape) -->
[koto_rotate cache="7d" section="intro"]A|||KOTO_VARIANT|||B|||KOTO_VARIANT|||C[/koto_rotate]
  cache:    7d / 24h / 1h / 0 / none (default 7d)
  section:  cache-key sub-tag (default 'default')
  pin:      force variant index 1-N (testing)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: elementor.save + elementor.clone PHP handlers + verb-table finalization</name>
  <files>
    wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php,
    wp-plugin-kotoiq-shim/includes/rpc/verb-table.php,
    wp-plugin-kotoiq-shim/kotoiq-shim.php
  </files>
  <read_first>
    - wp-plugin-kotoiq/includes/modules/elementor-builder.php (FULL FILE — port logic shape, generalize names)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md (§Module Port Plan — elementor-builder, §Pitfall 5 — Document::save input format)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md (D-Page-design-model — Option B push flow)
    - wp-plugin-kotoiq-shim/includes/rpc/verbs-meta.php (Plan 04 — meta.update used elsewhere; this file does NOT replicate generic meta logic)
  </read_first>
  <behavior>
    elementor.save:
    - Validates inputs: post_id integer OR literal 'new'; elementor_data MUST be array (not string — per Pitfall 5); idempotency_key length 1-64 if provided
    - On post_id='new': inserts new post via wp_insert_post with author=koto_service user
    - On post_id=int: loads existing post; rejects if not found
    - Idempotency check: if idempotency_key matches existing meta `koto_idempotency_key`, returns idempotent: true without doing the save
    - Edit-lock check: if another user is editing in WP admin (5-min lock), return 409 edit_locked
    - Loads Elementor Document via `\Elementor\Plugin::$instance->documents->get($post_id)`; creates one if missing (sets _elementor_edit_mode + _elementor_template_type)
    - Calls `$document->save(['elements' => $elementor_data, 'settings' => $page_settings])`
    - Triggers kotoiq_shim_force_css_regen($post_id) (helper ported)
    - Sets meta: koto_kotoiq=1 + koto_idempotency_key (if provided)
    - Caps revisions to 3 via wp_revisions_to_keep filter (per v3 pattern, Pitfall #5 in research)
    - Updates post_status if explicitly passed
    - Returns response shape above
    - Emits events.log entry `elementor.saved` with post_id + idempotency_key + element_count

    elementor.clone:
    - args.source_post_id must exist
    - Inserts new post via wp_insert_post with author=koto_service
    - Copies _elementor_data, _elementor_version, _elementor_edit_mode, _elementor_template_type, _elementor_page_settings from source
    - If args.elementor_data passed, overwrites _elementor_data (json_encode + wp_slash for double-escaping in WP DB)
    - Sets koto_kotoiq=1 + koto_idempotency_key
    - **Critical change from v3:** args.post_meta + args.meta_prefix_allowlist — the dashboard sends an explicit list of allowed prefixes (e.g., ['rank_math_', '_koto_', '_yoast_wpseo_']). Plugin loops post_meta entries and writes ONLY those matching an allowlisted prefix. **v3 hardcoded the prefixes; v4 receives them from the dashboard.** This is the IP-protection win — anyone reading the v4 source learns "we accept meta keys matching dashboard-allowed prefixes," not "we write rank_math_*" specifically.
    - If args.body_html passed, runs wp_update_post on the new post with sanitized post_content
    - Triggers kotoiq_shim_force_css_regen
    - Returns response shape

    Helpers (private):
    - kotoiq_shim_force_css_regen($post_id) — identical body to v3 but namespaced
    - kotoiq_shim_get_service_user_id() — looks up 'koto_service' user (created in Plan 02 pairing); falls back to current user ID 1
    - kotoiq_shim_count_elements + kotoiq_shim_extract_widget_types — direct port of v3 helpers, used in elementor.save response (these are pure JSON-tree walking, no IP)
  </behavior>
  <action>
    1. Create `wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php`:
       - Add early guard at the top of each verb: if `!defined('ELEMENTOR_VERSION') || !class_exists('\\Elementor\\Plugin')` → `WP_Error('elementor_not_active', 'Elementor plugin is not active on this site', ['status' => 503])`
       - **`kotoiq_shim_verb_elementor_save($args)`**:
         a. Validate post_id is int OR 'new'; validate elementor_data is array
         b. Validate idempotency_key matches `/^[A-Za-z0-9_-]{1,64}$/` if present
         c. If post_id === 'new':
            - Require args.title (non-empty string)
            - wp_insert_post with `post_type` (default 'page'), `post_title`, `post_name` (slug), `post_status` (default 'draft'), `post_author = kotoiq_shim_get_service_user_id()`
            - On wp_error: return wrapping WP_Error
         d. Load $post = get_post((int)$post_id); reject 404 if null
         e. Edit-lock check (mirror v3 lines 298-313)
         f. Idempotency check: if existing `koto_idempotency_key` meta matches → return `{ok: true, idempotent: true, post_id, url, message}`
         g. Document load + create (mirror v3 lines 327-340)
         h. `$save_data = ['elements' => $args['elementor_data']];` plus settings if provided
         i. `$document->save($save_data);`
         j. `kotoiq_shim_force_css_regen($post_id);`
         k. Set meta: koto_kotoiq + koto_idempotency_key
         l. Add revision cap filter (mirror v3 lines 348-351)
         m. wp_update_post status if changed
         n. Emit event: `kotoiq_shim_emit_event('elementor.saved', ['post_id' => $post_id, 'idempotency_key' => $args['idempotency_key'] ?? null]);`
         o. Return success shape
         p. Wrap entire body in try/catch — catch Throwable, return WP_Error('save_failed', ...)
       - **`kotoiq_shim_verb_elementor_clone($args)`**:
         a. Validate source_post_id is int > 0; source post exists
         b. Validate meta_prefix_allowlist: if args.post_meta is non-empty array, meta_prefix_allowlist MUST be a non-empty array of strings each matching `/^[a-zA-Z_][a-zA-Z0-9_]*_$/` (must end with underscore — they're prefixes). Reject otherwise.
         c. Insert new post with author=koto_service
         d. Loop the 5 Elementor metas (mirror v3) — copy from source to new
         e. If args.elementor_data: overwrite _elementor_data via `update_post_meta($new_id, '_elementor_data', wp_slash(wp_json_encode($args['elementor_data'])));`
         f. Set koto_kotoiq=1 + koto_idempotency_key
         g. **Generic prefix loop:**
            ```php
            if (!empty($args['post_meta']) && is_array($args['post_meta'])) {
                $allowed_prefixes = $args['meta_prefix_allowlist'] ?? [];
                foreach ($args['post_meta'] as $meta_key => $meta_value) {
                    foreach ($allowed_prefixes as $prefix) {
                        if (strpos($meta_key, $prefix) === 0) {
                            update_post_meta($new_id, $meta_key, sanitize_text_field((string)$meta_value));
                            break;
                        }
                    }
                }
            }
            ```
            **NO hardcoded `rank_math_` / `_koto_` in this file.** The prefix list comes from the dashboard. This is the IP win — generic v3 had specific names baked in.
         h. If args.body_html: `wp_update_post(['ID' => $new_id, 'post_content' => wp_kses_post($args['body_html'])]);`
         i. kotoiq_shim_force_css_regen
         j. Emit event: `elementor.cloned` with source_id + new_id
         k. Return success
       - **Helpers** at bottom of file (mirror v3 functions, generic names):
         - `kotoiq_shim_force_css_regen($post_id)` — same body as v3 lines 433-444
         - `kotoiq_shim_get_service_user_id()` — same body as v3 lines 450-462; user_login is 'koto_service' (per D-Pairing-user — same user the Plan 02 pairing creates)
         - `kotoiq_shim_count_elements($elements)` + `kotoiq_shim_extract_widget_types($elements)` — same bodies, renamed

    2. Update `wp-plugin-kotoiq-shim/includes/rpc/verb-table.php`:
       - Point `elementor.save` → `kotoiq_shim_verb_elementor_save`
       - Point `elementor.clone` → `kotoiq_shim_verb_elementor_clone`
       - Verify ZERO stubs remain — file should have no references to `kotoiq_shim_verb_not_yet_implemented`

    3. Update `wp-plugin-kotoiq-shim/kotoiq-shim.php`:
       - Add `require_once KOTOIQ_SHIM_DIR . 'includes/rpc/verbs-elementor.php';`

    4. PHP syntax check + LOC budget
  </action>
  <verify>
    <automated>find wp-plugin-kotoiq-shim -name '*.php' -exec php -l {} \; 2>&1 | grep -c "No syntax errors"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php`
    - `find wp-plugin-kotoiq-shim -name '*.php' -exec php -l {} \;` reports "No syntax errors" for all files
    - `grep -c "Document::save\\|->save(" wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php` returns at least 1
    - `grep -c "kotoiq_shim_force_css_regen" wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php` returns at least 2 (declaration + at least one call)
    - **The IP-protection win — verify NO hardcoded SEO-plugin prefixes:** `grep -c "rank_math_\\|_yoast_\\|wpseo" wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php` returns 0 (PRESENT in v3 source — must be ABSENT from v4)
    - `grep -c "meta_prefix_allowlist" wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php` returns at least 1 (generic prefix-list param exists)
    - `grep -c "koto_service" wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php` returns at least 1 (service user reference — matches Plan 02 pairing)
    - `grep -c "kotoiq_shim_verb_not_yet_implemented" wp-plugin-kotoiq-shim/includes/rpc/verb-table.php` returns 0 (ZERO stubs — all 27 verbs real)
    - All 27 verbs present: `node -e "const {SHIM_VERBS} = require('./src/lib/wp-shim/verbList'); const tbl = require('fs').readFileSync('wp-plugin-kotoiq-shim/includes/rpc/verb-table.php','utf8'); const missing = SHIM_VERBS.filter(v => !tbl.includes(\`'${v}'\`)); console.log(missing); process.exit(missing.length === 0 ? 0 : 1);"`
    - LOC budget: `node scripts/wp-plugin-loc-budget.cjs --plugin-dir wp-plugin-kotoiq-shim --budget 500 --strict` exits 0 (NOW running with --strict because all verbs are real and the budget gate kicks in)
    - Plugin source IP-clean across new file: `grep -ciE "yoast|rank_math|focus_keyword|seo[_ ]?score|sitemap[_ ]?priority|page[_ ]?factory|hyperlocal" wp-plugin-kotoiq-shim/includes/rpc/verbs-elementor.php` returns 0
  </acceptance_criteria>
  <done>
    elementor.save and elementor.clone implemented as the two host-bound verbs. Both generic — clone accepts dashboard-supplied meta_prefix_allowlist (no v3 hardcoded SEO-plugin name leak). All 27 verbs now have real handlers. LOC budget --strict gate passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: koto_rotate shortcode + TypeScript wrappers for elementor verbs</name>
  <files>
    wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php,
    wp-plugin-kotoiq-shim/kotoiq-shim.php,
    src/lib/wp-shim/verbs/index.ts,
    src/lib/wp-shim/verbs/verbs.test.ts
  </files>
  <read_first>
    - wp-plugin-kotoiq/includes/modules/content-rotation.php (v3 source — port body, strip module-loader specifics)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md (§Module Port Plan — content-rotation, §Performance — content-rotation shortcode)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md (§specifics — Content rotation in pushed templates)
    - src/lib/wp-shim/verbs/index.ts (Plans 04+05 wrappers; this plan appends elementor wrappers)
  </read_first>
  <behavior>
    koto_rotate shortcode:
    - Identical functional behavior to v3 (same attrs: cache, section, pin; same separator |||KOTO_VARIANT|||; same caching via transients; same array_rand selection)
    - Does NOT use koto_register_module / koto_is_module_enabled (those are v3 module-loader functions; v4 has no module concept)
    - Does NOT print or comment any KotoIQ-specific information
    - The comment header reads as generic shortcode plumbing — could be from any "rotate variants" plugin
    - Side-by-side guard: `if (!shortcode_exists('koto_rotate'))` — first plugin to register wins; if v3 already ran, v4 doesn't double-register (matches v3's own defensive check at content-rotation.php:35)

    TypeScript wrappers:
    - elementorSave + elementorClone with full type signatures matching PHP response shapes
    - elementorClone runtime guard: if post_meta passed but meta_prefix_allowlist is missing/empty → throw before RPC
    - elementorSave runtime guard: elementor_data must be Array (TS type signature)
    - Tests: happy path + edge cases (post_id 'new' vs int, missing title for 'new', missing allowlist for clone)
  </behavior>
  <action>
    1. Create `wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php`:
       - Header comment — INTENTIONALLY GENERIC:
         ```
         /**
          * [koto_rotate] — generic variant-picker shortcode. Splits its body on
          * a delimiter, picks one variant at render time, caches the selection
          * via WP transients. Useful for any A/B content presentation.
          *
          * Attributes:
          *   cache   — duration ("7d" / "24h" / "1h" / "0"); default 7d.
          *   section — sub-key disambiguation when one post has multiple rotators
          *   pin     — force a specific 1-indexed variant (bypasses cache)
          *
          * @package KotoIQ Shim
          */
          ```
       - Side-by-side guard: `if (!shortcode_exists('koto_rotate'))` → register
       - Function `kotoiq_shim_rotate_shortcode($atts, $content = '')`:
         - shortcode_atts for cache/section/pin
         - empty content → return ''
         - explode on '|||KOTO_VARIANT|||', trim + filter empties + array_values
         - empty array → return ''
         - count===1 → return do_shortcode of the single variant
         - if pin set: clamp to 0..N-1, return that variant
         - cache key: "koto_rotate_{$post_id}_{$section}" (sanitize_key the section)
         - if get_transient hit → return do_shortcode(variants[index])
         - else: array_rand → set_transient (if duration > 0) → return do_shortcode(variants[index])
       - Helper `kotoiq_shim_rotate_parse_cache_duration($str)`:
         - port v3 lines 85-95 exactly (1d/24h/30m/0/none)
       - **No mention of KotoIQ, no mention of SEO, no mention of how this is used. The shortcode looks like generic A/B plumbing.**

    2. Update `wp-plugin-kotoiq-shim/kotoiq-shim.php`:
       - require_once KOTOIQ_SHIM_DIR . 'shortcodes/koto-rotate.php';

    3. Update `src/lib/wp-shim/verbs/index.ts` (append):
       ```typescript
       // Elementor types
       export interface ElementorSaveArgs {
         post_id: number | 'new';
         elementor_data: Array<unknown>;  // Elementor element tree — array of element nodes
         page_settings?: Record<string, unknown>;
         status?: 'draft' | 'publish' | 'private';
         title?: string;          // required if post_id === 'new'
         slug?: string;
         post_type?: 'page' | 'post' | string;
         idempotency_key?: string;
       }
       export interface ElementorSaveResponse {
         ok: true;
         post_id: number;
         url: string;
         status: string;
         elementor_version: string | null;
         css_regenerated: boolean;
         idempotent?: boolean;
         element_count: number;
       }
       export async function elementorSave(
         siteUrl: string,
         args: ElementorSaveArgs,
       ): Promise<ShimRpcResponse<ElementorSaveResponse>> {
         // Runtime guards (defense in depth)
         if (args.post_id === 'new' && !args.title) {
           throw new Error('elementorSave: title is required when post_id === "new"');
         }
         if (!Array.isArray(args.elementor_data)) {
           throw new Error('elementorSave: elementor_data must be an array (not a JSON string)');
         }
         if (args.idempotency_key && !/^[A-Za-z0-9_-]{1,64}$/.test(args.idempotency_key)) {
           throw new Error('elementorSave: idempotency_key must match /^[A-Za-z0-9_-]{1,64}$/');
         }
         return shimRpc<ElementorSaveResponse>(siteUrl, 'elementor.save', args);
       }
       export interface ElementorCloneArgs {
         source_post_id: number;
         title?: string;
         slug?: string;
         status?: 'draft' | 'publish' | 'private';
         elementor_data?: Array<unknown>;
         page_settings?: Record<string, unknown>;
         body_html?: string;
         post_meta?: Record<string, string>;
         meta_prefix_allowlist?: string[];   // REQUIRED if post_meta is non-empty
         idempotency_key?: string;
       }
       export interface ElementorCloneResponse {
         ok: true;
         post_id: number;
         source_id: number;
         url: string;
         status: string;
       }
       export async function elementorClone(
         siteUrl: string,
         args: ElementorCloneArgs,
       ): Promise<ShimRpcResponse<ElementorCloneResponse>> {
         if (args.post_meta && Object.keys(args.post_meta).length > 0) {
           if (!args.meta_prefix_allowlist || args.meta_prefix_allowlist.length === 0) {
             throw new Error('elementorClone: meta_prefix_allowlist is required when post_meta is non-empty');
           }
           // Validate every key in post_meta matches at least one prefix (defense-in-depth)
           const allowed = args.meta_prefix_allowlist;
           for (const k of Object.keys(args.post_meta)) {
             if (!allowed.some(p => k.startsWith(p))) {
               throw new Error(`elementorClone: meta key "${k}" does not match any allowed prefix`);
             }
           }
         }
         return shimRpc<ElementorCloneResponse>(siteUrl, 'elementor.clone', args);
       }
       ```

    4. Update `src/lib/wp-shim/verbs/verbs.test.ts` with new test cases:
       - elementorSave happy path
       - elementorSave throws when post_id='new' + missing title
       - elementorSave throws when elementor_data is a string
       - elementorSave throws when idempotency_key has invalid chars
       - elementorClone happy path
       - elementorClone throws when post_meta has entries but meta_prefix_allowlist is empty
       - elementorClone throws when a post_meta key doesn't match any allowed prefix

    5. Run `npm run test -- --run src/lib/wp-shim/verbs/`
  </action>
  <verify>
    <automated>npm run test -- --run src/lib/wp-shim/verbs/ && find wp-plugin-kotoiq-shim -name '*.php' -exec php -l {} \; 2>&1 | grep -c "No syntax errors"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php`
    - `grep -c "shortcode_exists('koto_rotate')" wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php` returns 1 (side-by-side guard)
    - `grep -c "add_shortcode('koto_rotate'" wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php` returns 1
    - `grep -c "|||KOTO_VARIANT|||" wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php` returns at least 1 (separator preserved — matches dashboard composer output)
    - **No IP-leak in shortcode source** — `grep -ciE "seo|hyperlocal|rotation_strategy|page_factory|kotoiq_" wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php` returns 0 EXCEPT for `kotoiq_shim_rotate_*` function name prefix (which is package naming, not strategy revelation). The acceptance refinement: `grep -ciE "seo|hyperlocal|rotation_strategy|page_factory" wp-plugin-kotoiq-shim/shortcodes/koto-rotate.php` returns 0.
    - `grep -cE "^export async function elementorSave|^export async function elementorClone" src/lib/wp-shim/verbs/index.ts` returns 2
    - `grep -c "meta_prefix_allowlist" src/lib/wp-shim/verbs/index.ts` returns at least 2 (type definition + runtime guard)
    - `npm run test -- --run src/lib/wp-shim/verbs/` exits 0
    - `npx tsc --noEmit --project tsconfig.json 2>&1 | grep "src/lib/wp-shim" | grep -c "error TS"` returns 0
    - LOC budget --strict still passes: `node scripts/wp-plugin-loc-budget.cjs --plugin-dir wp-plugin-kotoiq-shim --budget 500 --strict` exits 0
    - Plugin source overall IP-clean: `grep -rciE "yoast|rank_math|focus_keyword|seo[_ ]?score|sitemap[_ ]?priority|page[_ ]?factory|hyperlocal" wp-plugin-kotoiq-shim/ | grep -v ':0$'` produces no output (zero hits across all files)
  </acceptance_criteria>
  <done>
    koto_rotate shortcode lives in shortcodes/ folder with generic comment + side-by-side guard. elementorSave + elementorClone TS wrappers in place with runtime guards. All 27 verbs implemented and tested. Plugin under 500 LOC --strict. IP-clean grep passes. Plan 06 closes the shim contract — Plans 07-09 (dashboard ports) can now build on top.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| elementor.save → Document::save() | The verb dispatcher trusts dispatched args after signed-envelope auth. Document::save() is Elementor's own API — its security is Elementor's responsibility. |
| elementor.clone → meta_prefix_allowlist | The dashboard chooses which prefixes to allow. A buggy dashboard could grant write access to arbitrary post meta — including SEO plugin meta with security implications. |
| koto_rotate → post_content variants | Shortcode renders content stored in post_content. Already trusted: whoever wrote the post wrote those variants. Standard WP shortcode trust model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06-01 | Tampering | elementor.save passed a string instead of array (Pitfall 5) | mitigate | PHP handler validates is_array; TS wrapper runtime-checks Array.isArray. Documented response: "Successful 200 but empty page" failure mode prevented. |
| T-10-06-02 | Elevation of Privilege | elementor.clone with meta_prefix_allowlist=['user_'] could write to wp_usermeta-shadow keys | mitigate | Prefix-list is dashboard-authored; dashboard is responsible. Plugin doesn't restrict further — it's a generic primitive. Defense: Plan 11 audit reviews dashboard-side allowlist composition before cutover. |
| T-10-06-03 | Repudiation | Elementor write happens with no audit | mitigate | elementor.saved + elementor.cloned events emitted; dashboard can query via events.log_tail for forensics. |
| T-10-06-04 | Denial of Service | elementor.save with a giant elementor_data (e.g., 10MB JSON) blocks PHP | accept | WP body limit (8MB default) and PHP memory_limit cap this naturally. Plan 11 documents the operational limit. |
| T-10-06-05 | Tampering | Shortcode pin attribute allows arbitrary variant selection | accept | Shortcode attrs come from post_content; whoever can edit post_content can already write any content. Pin is for testing — not a security primitive. |
| T-10-06-06 | Information Disclosure | koto_rotate cache transient table reveals which posts have which sections rotating | accept | Transient names are visible to anyone with DB access. They reveal "post N has a rotation section called X." That's a v3 leak too — content-rotation cache keys had same shape. Acceptable: it's about which posts use the rotation feature, not what KotoIQ thinks rotation should achieve. |
| T-10-06-07 | Information Disclosure | elementor.save / elementor.clone source code reveals "we write Elementor data via Document::save" | accept | Documented as unavoidable: per research §Module Port Plan — elementor-builder, "the verb name is generic, the operation is host-specific. The hostile client reading the code learns 'we save Elementor pages through their official API.' That's WordPress integration, not IP." |
| T-10-06-08 | Tampering | idempotency_key collision across two different writes | mitigate | Validation pattern `/^[A-Za-z0-9_-]{1,64}$/` enforces shape; dashboard guarantees uniqueness via composing UUID + run_id. Documented in Plan 09 push flow. |
</threat_model>

<verification>
- All PHP files pass syntax check
- 27/27 verbs in verb-table.php have real handlers (zero stubs)
- LOC budget --strict passes (<500)
- IP-clean grep passes (no rank_math_, _yoast_, focus_keyword, page_factory, hyperlocal in any file)
- TypeScript typecheck clean
- All tests green
- elementor.save validates array (Pitfall 5)
- elementor.clone enforces meta_prefix_allowlist as required when post_meta is non-empty
</verification>

<success_criteria>
- The shim plugin is contract-complete: all 27 verbs implemented
- Plugin LOC under 500 (business logic) with --strict enforcement
- Plugin source contains no IP-leaking strings
- A dashboard caller can run: `await elementorSave(site, {post_id: 'new', title: 'Hello', elementor_data: [...], idempotency_key: 'abc'})` and get back a working page
- Dashboard ports in Plans 07-09 can now begin
- Cutover ops in Plans 10-12 can begin
</success_criteria>

<output>
After completion, create `.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-06-SUMMARY.md` recording:
- Final LOC count (business-logic only) + total file count
- Confirmation of 27/27 real verb handlers
- Result of the IP-leak regression grep (paste output: should be empty for every plugin file)
- Confirmation that meta_prefix_allowlist replaces v3's hardcoded prefix list
- Diff summary: which files changed vs Plan 05 SUMMARY's final state
</output>
