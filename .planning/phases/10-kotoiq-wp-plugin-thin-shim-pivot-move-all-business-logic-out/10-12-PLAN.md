---
phase: 10-kotoiq-wp-plugin-thin-shim-pivot
plan: 12
type: execute
wave: 8
depends_on: [10-11]
files_modified:
  - scripts/cutover/sunset-v3.cjs
  - scripts/cutover/sunset-v3.test.cjs
  - scripts/cutover/cleanup-legacy-options.cjs
  - src/app/api/wp/route.ts
  - src/app/api/kotoiq-manifest/route.ts
  - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/SUNSET-PLAYBOOK.md
autonomous: false
requirements:
  - SHIM-V3-SUNSET
must_haves:
  truths:
    - "sunset-v3.cjs is gated by the same kind of guardrails as kill-switch (--confirm + --reason + typed I UNDERSTAND); refuses unless every site in fleet is dual_run_state='promoted' for at least 60 days OR --override-day-60 is passed with documented reason"
    - "On execution: fires shimRpc to v3's /wp-json/kotoiq/v1/destruct with deactivate=true on every promoted site → v3 plugin deactivates; v3 plugin folder remains on disk but inactive (per CONTEXT.md D-Cutover — 'do NOT use v3.x's uninstall.php')"
    - "Updates legacy `/api/kotoiq-manifest` and `/api/wpsc-manifest` (v3 channels) to return a sunset notice (200 with deprecated=true field) so any v3 site that somehow still self-update-polls sees a clear 'no upgrade available' signal"
    - "Per CONTEXT.md D-TypeScript-port-equivalence: shadow-mode logs (koto_wp_dual_run_log) are retained for 6 months post-promotion before optional truncation"
    - "Per CONTEXT.md D-Backward-compat: 'Assume no external callers depend on /wp-json/kotoiq/v1/* or /wp-json/wpsimplecode/v1/*. Deprecate cleanly on day 60.' — this plan implements that deprecation"
    - "src/app/api/wp/route.ts is annotated with @deprecated comments + ALLOWED_ACTIONS pruned to ONLY the actions the dashboard still uses for sunset operations (destruct, info-only reads); all other actions return 410 Gone"
    - "ROADMAP.md updated to mark Phase 10 complete; any follow-up M2 work (per-site keypairs, search-replace performance tuning) is moved to a deferred-list"
  artifacts:
    - path: "scripts/cutover/sunset-v3.cjs"
      provides: "Fleet-wide v3 deactivation script"
    - path: "scripts/cutover/cleanup-legacy-options.cjs"
      provides: "Optional follow-up to clear `wpsc_*` options on promoted sites via the v4 option.delete verb (only after sunset completes)"
    - path: "src/app/api/wp/route.ts (modified)"
      provides: "ALLOWED_ACTIONS pruned to minimal set; deprecation header added"
    - path: "src/app/api/kotoiq-manifest/route.ts (modified)"
      provides: "Returns deprecated=true + sunset_date + redirect to /api/kotoiq-shim-manifest"
    - path: ".planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/SUNSET-PLAYBOOK.md"
      provides: "Concrete runbook for the day-60 sunset event"
  key_links:
    - from: "sunset-v3.cjs"
      to: "shimRpc /destruct + v3 legacy Bearer endpoint /wp-json/kotoiq/v1/destruct"
      via: "Both paths attempted — v4 shim's destruct closes itself; v3 destruct deactivates v3 plugin"
      pattern: "Per-site sequential; aggregate report"
    - from: "Phase 10 closure"
      to: "ROADMAP.md + .planning/STATE.md updates"
      via: "Marks Phase 10 complete; logs follow-up items"
      pattern: "Plan 12 Task 2 + the final SUMMARY"
---

<objective>
Sunset the v3 plugin per CONTEXT.md D-Cutover-side-by-side (USER-LOCKED):
60-day overlap window. After 60 days of v4-promoted operation on a site:
- Fire v3's /destruct with deactivate=true → v3 plugin deactivates
- v3 plugin folder remains on disk but inactive (per CONTEXT.md: 'Do NOT
  use v3.x's uninstall.php — that would wipe wpsc_* options, including
  the shared secret which we're still keeping for emergency rollback')
- Legacy manifest endpoints return sunset notice (gentle deprecation)
- Dashboard's src/app/api/wp/route.ts pruned to minimal set of allowed
  actions (everything is going through src/lib/wp-shim now)
- ROADMAP.md marked complete + STATE.md updated

Per D-Backward-compat: 'Assume no external callers depend on /wp-json/kotoiq/v1/*'.
Documentation reads that this is a clean cut.

This plan has one human-action checkpoint: confirming the sunset criteria
are actually met across the fleet before running sunset-v3.cjs. After
confirmation + script execution, Phase 10 is complete.

Output:
- 2 CLI scripts (sunset-v3, cleanup-legacy-options)
- 2 dashboard endpoint updates (api/wp pruning + manifest deprecation)
- SUNSET-PLAYBOOK.md
- ROADMAP + STATE updates
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md
@.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-11-SUMMARY.md
@src/app/api/wp/route.ts
@src/app/api/kotoiq-manifest/route.ts
@src/lib/wp-shim/index.ts
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
Sunset gates (sunset-v3.cjs checks before firing):
  - ALL koto_wp_sites WHERE shim_version='v4' MUST be dual_run_state='promoted'
  - ALL promoted sites MUST have v4_promoted_at <= NOW - 60 days
  - koto_wp_dual_run_log: zero major_diff entries fleet-wide in last 30 days
  - --override-day-60 bypasses the 60-day check (requires --reason)

Sunset operation per site:
  1. shimRpc(siteUrl, '/destruct', {deactivate: true})  // v4's own destruct — NO. The shim destruct deactivates v4. We want v3 deactivated.
  2. Instead: fetch v3 endpoint /wp-json/kotoiq/v1/destruct with legacy Bearer auth + body {deactivate: true}
     - This calls v3's pairing.php kotoiq_destruct → schedules v3 deactivation
  3. Update koto_wp_sites SET shim_version='v4_only' (new value), wpsc_api_key=NULL  (the Bearer is no longer needed)
  4. Insert koto_wp_shim_pairings event='v3_sunset_destructed' notes={reason, deactivated_at}

Legacy manifest deprecation:
  /api/kotoiq-manifest + /api/wpsc-manifest (existing routes) get a small change:
  - Return 200 with body `{deprecated: true, sunset_date: '2026-XX-XX', successor_manifest: '/api/kotoiq-shim-manifest'}`
  - No version + download_url fields (so any v3 site still polling sees nothing to upgrade to)
  - Existing route file location: read src/app/api/kotoiq-manifest/route.ts before modifying

ALLOWED_ACTIONS pruning in src/app/api/wp/route.ts:
  Before: ~80 actions covering all v3 dashboard operations
  After: A documented minimal set:
    - 'meta' (read v3 plugin meta for fleet status checks during sunset window)
    - 'health_ping' (basic site reachability)
    - 'destruct' (used by sunset-v3.cjs)
  Every other action returns 410 Gone with body explaining "Use shimRpc + src/lib/wp-shim/* instead"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: sunset-v3.cjs + cleanup-legacy-options.cjs + tests</name>
  <files>
    scripts/cutover/sunset-v3.cjs,
    scripts/cutover/sunset-v3.test.cjs,
    scripts/cutover/cleanup-legacy-options.cjs
  </files>
  <read_first>
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-CONTEXT.md (D-Cutover-side-by-side full body — the "do NOT use v3.x's uninstall.php" rule)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-RESEARCH.md (§Back-Compat Strategy — Sunset phase steps 4-5, Cleanup phase)
    - wp-plugin-kotoiq/includes/pairing.php (v3 destruct endpoint shape — kotoiq_destruct accepts {deactivate})
    - scripts/cutover/kill-switch.cjs (Plan 11 — guardrail pattern: --confirm + --reason + I UNDERSTAND prompt; mirror exactly)
    - src/lib/wp-shim/credentialsVault.ts (loadSiteCredentials — used to read legacy bearer if needed)
    - supabase/migrations/20260626_kotoiq_shim.sql (koto_wp_sites.wpsc_api_key — legacy column from earlier phases)
  </read_first>
  <behavior>
    sunset-v3.cjs:
    - args: --confirm --reason='...' [--override-day-60] [--filter=site-id|agency-id|all]
    - Loads sites matching filter where shim_version='v4'
    - Per-site gate checks:
      - dual_run_state === 'promoted' (or --override-day-60 + --override-promoted)
      - v4_promoted_at <= NOW - 60 days (or --override-day-60)
      - In the last 30 days, zero koto_wp_dual_run_log rows with diff_status='major_diff' for this site
    - On gate failures: print per-site diagnostic; do NOT proceed unless --override flag was passed AND --reason was non-empty
    - Print fleet-wide change-set: "About to deactivate v3 on N sites in agency A across B agencies"
    - Confirmation prompt: type 'I UNDERSTAND' (matching kill-switch.cjs pattern)
    - For each site:
      1. Read koto_wp_sites.wpsc_api_key (legacy Bearer)
      2. If non-null: POST to {site_url}/wp-json/kotoiq/v1/destruct with Authorization: Bearer + body {deactivate: true}
      3. If null (already cleared): skip the v3 destruct (already deactivated or never paired)
      4. Update koto_wp_sites SET shim_version='v4_only', wpsc_api_key=NULL, wpsc_version=NULL
      5. Insert koto_wp_shim_pairings event='v3_sunset_destructed' with notes={reason, attempted_v3_destruct, destruct_result}
    - Aggregate report at end: success_count, skip_count, error_count + per-site details
    - On error per-site: do NOT abort the whole run; continue to next site + collect for report

    cleanup-legacy-options.cjs:
    - args: --confirm --reason='...' [--filter=site-id|all]
    - OPTIONAL follow-up — runs AFTER sunset-v3.cjs has succeeded
    - For each shim_version='v4_only' site:
      - shimRpc option.delete for the legacy v3 options (wpsc_snippets, wpsc_access_policy, kotoiq_seo_redirects, kotoiq_seo_404_log, koto_modules_enabled, kotoiq_pairing_ready, kotoiq_dashboard_url) — but ONLY after confirming the data has been migrated to v4-side options (kotoiq_shim_snippets, kotoiq_shim_access_policy, kotoiq_shim_redirects, kotoiq_shim_404_log, kotoiq_shim_features_enabled)
      - Tool DOES NOT migrate data here — that's the responsibility of Plan 11's cutover phase. cleanup only deletes already-migrated options.
    - Per-site: print before/after option counts
    - DOES NOT touch wpsc_api_key (already nulled by sunset-v3.cjs)

    sunset-v3.test.cjs:
    - Tests:
      - Gate failure: 1 site not promoted → script refuses without --override-day-60
      - Gate failure: 1 site promoted < 60 days ago → refuses
      - Gate failure: site has major_diff in last 30 days → refuses
      - All gates pass → executes per-site loop
      - Per-site v3 destruct fails (network/auth) → continues to next site, error recorded
      - Per-site update fails (DB) → continues, error recorded
      - --override-day-60 + --reason: bypasses 60-day gate; STILL enforces 'promoted' gate unless --override-promoted also passed
      - Audit row inserted on every site (success or skip)
      - --confirm + --reason both required (refuses without)
  </behavior>
  <action>
    1. Create `scripts/cutover/sunset-v3.cjs`:
       - Use Node + @supabase/supabase-js + node:fetch
       - Use node:util.parseArgs for CLI args
       - Load .env (per scripts/cutover/kill-switch.cjs pattern)
       - Define gate-check functions per the behavior list
       - Implement the per-site destruct loop with try/catch
       - Match the I UNDERSTAND confirmation pattern from kill-switch.cjs exactly
       - The v3 destruct call:
         ```javascript
         const res = await fetch(`${site.site_url}/wp-json/kotoiq/v1/destruct`, {
           method: 'POST',
           headers: {
             'Authorization': `Bearer ${site.wpsc_api_key}`,
             'Content-Type': 'application/json',
           },
           body: JSON.stringify({ deactivate: true }),
         });
         ```
       - **Do NOT** delete the koto_wp_sites row (it stays as historical record); only update + null the legacy bearer column
       - Print final summary table

    2. Create `scripts/cutover/cleanup-legacy-options.cjs`:
       - Loads v4_only sites
       - For each: calls shimRpc option.delete for each legacy option key in a hardcoded list
       - Sequential per-site (avoid hammering shim)
       - Reports per-site option-count delta

    3. Create `scripts/cutover/sunset-v3.test.cjs`:
       - Use Node's built-in test runner (node:test) — no Vitest needed for plain .cjs scripts
       - Mock supabase client + global fetch
       - Cover the test cases listed in behavior section
       - Run via: `node --test scripts/cutover/sunset-v3.test.cjs`

    4. Verify scripts parse + tests pass:
       - `node --check scripts/cutover/sunset-v3.cjs`
       - `node --check scripts/cutover/cleanup-legacy-options.cjs`
       - `node --test scripts/cutover/sunset-v3.test.cjs`
  </action>
  <verify>
    <automated>node --test scripts/cutover/sunset-v3.test.cjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/cutover/sunset-v3.cjs && test -f scripts/cutover/cleanup-legacy-options.cjs && test -f scripts/cutover/sunset-v3.test.cjs`
    - `node --check scripts/cutover/sunset-v3.cjs` succeeds
    - `node --check scripts/cutover/cleanup-legacy-options.cjs` succeeds
    - `grep -c "kotoiq/v1/destruct\\|kotoiq_destruct" scripts/cutover/sunset-v3.cjs` returns at least 1 (v3 endpoint targeted)
    - `grep -c "deactivate.*true\\|deactivate: true" scripts/cutover/sunset-v3.cjs` returns at least 1
    - `grep -c "shim_version.*v4_only\\|'v4_only'" scripts/cutover/sunset-v3.cjs` returns at least 1 (state transition)
    - `grep -c "wpsc_api_key.*null\\|wpsc_api_key: null" scripts/cutover/sunset-v3.cjs` returns at least 1 (legacy bearer cleared)
    - `grep -c "v4_promoted_at\\|60.*day\\|60 \\* 24" scripts/cutover/sunset-v3.cjs` returns at least 1 (60-day gate)
    - `grep -c "I UNDERSTAND" scripts/cutover/sunset-v3.cjs` returns at least 1 (typed confirmation)
    - `grep -c "v3_sunset_destructed" scripts/cutover/sunset-v3.cjs` returns at least 1 (audit event name)
    - **CRITICAL:** `grep -c "uninstall.php\\|wpsc_snippets.*delete\\|wpsc_access_policy.*delete" scripts/cutover/sunset-v3.cjs` returns 0 — per CONTEXT.md, sunset MUST NOT delete v3 options (that's cleanup-legacy-options.cjs's job, and even that is OPTIONAL + run separately)
    - `node --test scripts/cutover/sunset-v3.test.cjs` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    sunset-v3.cjs deactivates v3 plugin on every promoted site after 60 days. Legacy Bearer cleared from koto_wp_sites. Cleanup-legacy-options is a separate optional follow-up. All guardrails match kill-switch pattern. Tests cover gate failures + per-site error handling.
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard API pruning + manifest deprecation + SUNSET-PLAYBOOK + ROADMAP update</name>
  <files>
    src/app/api/wp/route.ts,
    src/app/api/kotoiq-manifest/route.ts,
    .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/SUNSET-PLAYBOOK.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - src/app/api/wp/route.ts (FULL FILE — find the ALLOWED_ACTIONS array + dispatcher; this plan prunes it)
    - src/app/api/kotoiq-manifest/route.ts (current shape — confirm it's a Next App Router route)
    - .planning/ROADMAP.md (full file — find Phase 10 entry to update)
    - .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/CUTOVER-PLAYBOOK.md (Plan 11 — section 7 referenced this plan)
    - .planning/STATE.md (for the final phase-complete update)
    - AGENTS.md (Next.js v16 conventions — route signatures may have changed)
  </read_first>
  <behavior>
    src/app/api/wp/route.ts:
    - Add a top-of-file deprecation comment block
    - Prune ALLOWED_ACTIONS to: ['meta', 'health_ping', 'destruct'] (sunset-only operations)
    - For any other action: return 410 Gone with body explaining "This endpoint is deprecated. Use shimRpc verbs (see src/lib/wp-shim/) for v4 operations."
    - The dispatcher logic + all helper functions remain — they're called by the 3 retained actions OR ignored
    - Add a `@deprecated` JSDoc on the route export

    src/app/api/kotoiq-manifest/route.ts:
    - Change GET response to:
      ```json
      {
        "deprecated": true,
        "sunset_date": "<YYYY-MM-DD: 60 days after Plan 11 pilot pair>",
        "successor_manifest": "/api/kotoiq-shim-manifest",
        "message": "The KotoIQ v3 self-update channel has been sunset. v3 plugins do not receive further updates. Migrate to kotoiq-shim v4."
      }
      ```
    - HTTP status remains 200 (so v3 sites' self-update path doesn't error catastrophically)
    - Remove the version + download_url + sha256 fields entirely (no upgrade target offered)
    - DO NOT delete the route file — keep it so v3 polls return a clean signal indefinitely

    SUNSET-PLAYBOOK.md:
    - Day-60 runbook
    - Concrete commands per step

    ROADMAP.md:
    - Mark Phase 10 [x]
    - Update progress count
    - Add deferred items section for M2 follow-ups
  </behavior>
  <action>
    1. Update `src/app/api/wp/route.ts`:
       - Read the existing file
       - Locate the ALLOWED_ACTIONS array (or equivalent)
       - Replace with the minimal set: `['meta', 'health_ping', 'destruct']`
       - Add at top of file (after existing imports):
         ```typescript
         /**
          * @deprecated Phase 10 thin-shim pivot completed. This endpoint is retained
          * only for sunset operations (meta + health_ping + destruct). All other
          * dashboard ↔ WP communication MUST go through src/lib/wp-shim/* and the
          * v4 shim verb dispatcher at /wp-json/kotoiq-shim/v1/rpc.
          *
          * See:
          *   .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/CUTOVER-PLAYBOOK.md
          *   src/lib/wp-shim/index.ts
          */
         ```
       - In the dispatcher: if action not in the pruned ALLOWED_ACTIONS → return 410 Gone with body:
         ```json
         {
           "error": "deprecated",
           "message": "This action is deprecated. Use src/lib/wp-shim/* instead.",
           "successor": "/wp-json/kotoiq-shim/v1/rpc"
         }
         ```
       - The 3 retained actions remain functional via their existing handler functions; do NOT delete handler bodies (they may be referenced from the destruct path during sunset-v3.cjs)

    2. Update `src/app/api/kotoiq-manifest/route.ts`:
       - Replace the export GET handler body with the deprecation response shape
       - Keep the file + route registered

    3. Create `.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/SUNSET-PLAYBOOK.md`:
       - **Section 1: Sunset readiness check**
         - [ ] All sites in fleet are dual_run_state='promoted'
         - [ ] All v4_promoted_at timestamps are >= 60 days ago
         - [ ] koto_wp_dual_run_log has zero major_diff in last 30 days fleet-wide
         - [ ] No open kill_switch_fired audit events in last 60 days
       - **Section 2: Communications (optional)**
         - Notify any external integrators (per D-Backward-compat: assumed none, but document the fallback)
       - **Section 3: Execute sunset**
         - `node scripts/cutover/sunset-v3.cjs --confirm --reason='Day-60 sunset per CONTEXT.md D-Cutover-side-by-side'`
         - Expected output: deactivation per-site report
         - Verify in Supabase: `SELECT shim_version, COUNT(*) FROM koto_wp_sites GROUP BY shim_version;` → expect only 'v4_only' (plus any non-paired sites)
       - **Section 4: Verify legacy manifest deprecation**
         - `curl -s https://hellokoto.com/api/kotoiq-manifest | jq` → expect `{deprecated: true, ...}`
       - **Section 5: Verify dashboard API pruning**
         - `curl -X POST https://hellokoto.com/api/wp -d '{"action":"some_old_action"}' -H 'Content-Type: application/json'` → expect 410 Gone with deprecation body
       - **Section 6: (Optional, 90+ days post-sunset) cleanup**
         - `node scripts/cutover/cleanup-legacy-options.cjs --confirm --reason='Final cleanup of wpsc_* options'`
         - Removes legacy v3 options from the WP database (data has been migrated; option keys are orphaned)
       - **Section 7: Close-out**
         - Mark Phase 10 complete in ROADMAP.md (this plan does it inline; verify)
         - Move follow-up items to deferred list (M2 candidates):
           - Per-site keypair rotation (per D-Keypair-scope deferred)
           - searchReplace performance benchmarking (per research §Risks)
           - Multisite support (per CONTEXT.md §deferred)
           - WP.org distribution (per D-Plugin-distribution: never; do not undo this)

    4. Update `.planning/ROADMAP.md`:
       - Find Phase 10 entry
       - Change `- [ ] **Phase 10: ...` to `- [x] **Phase 10: ...`
       - Update plan list to mark all 12 plans complete with brief outcome notes
       - Update the Progress table at the bottom: `10 | 12/12 | Complete | <date>`
       - Add a "Deferred to M2" section under Phase 10 with the list from SUNSET-PLAYBOOK Section 7

    5. The STATE.md update happens automatically when the orchestrator processes the Plan 12 SUMMARY (gsd-tools logs phase completion). DO NOT directly edit STATE.md from this task.

    6. Validate:
       - `npx tsc --noEmit --project tsconfig.json 2>&1 | grep "src/app/api/wp\\|src/app/api/kotoiq-manifest" | grep -c "error TS"` returns 0
       - `cat .planning/ROADMAP.md | grep -c "\\[x\\] \\*\\*Phase 10"` returns at least 1
  </action>
  <verify>
    <automated>grep -c "deprecated" src/app/api/kotoiq-manifest/route.ts && grep -c "410\\|Gone" src/app/api/wp/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@deprecated" src/app/api/wp/route.ts` returns at least 1
    - `grep -c "410\\|Gone" src/app/api/wp/route.ts` returns at least 1 (deprecation HTTP status)
    - `grep -c "kotoiq-shim/v1/rpc\\|/api/wp-shim" src/app/api/wp/route.ts` returns at least 1 (successor pointer)
    - `grep -c "deprecated.*true\\|deprecated: true" src/app/api/kotoiq-manifest/route.ts` returns at least 1
    - `grep -c "successor_manifest.*kotoiq-shim-manifest" src/app/api/kotoiq-manifest/route.ts` returns at least 1
    - `test -f .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/SUNSET-PLAYBOOK.md`
    - `grep -cE "Section [1-7]" .planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/SUNSET-PLAYBOOK.md` returns at least 7
    - `grep -c "\\[x\\] \\*\\*Phase 10" .planning/ROADMAP.md` returns at least 1 (Phase 10 marked complete)
    - `grep -c "Deferred to M2\\|deferred-list\\|M2" .planning/ROADMAP.md` returns at least 1 (follow-up items documented)
    - `npx tsc --noEmit --project tsconfig.json 2>&1 | grep "src/app/api/wp\\|src/app/api/kotoiq-manifest" | grep -c "error TS"` returns 0
  </acceptance_criteria>
  <done>
    src/app/api/wp/route.ts pruned with deprecation pointer. Legacy manifest endpoint returns sunset notice. SUNSET-PLAYBOOK runbook complete. ROADMAP.md marks Phase 10 [x] with deferred-to-M2 items captured. Phase 10 ready for closure.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HUMAN — Day-60 sunset gauntlet</name>
  <what-built>
    Plan 12 Tasks 1+2 produced the sunset tools + deprecation endpoints +
    runbook. This checkpoint is the final cutover gauntlet — the operator
    confirms day-60 criteria are met + executes the sunset script.
  </what-built>
  <how-to-verify>
    **NOTE:** This checkpoint is gated by REAL CALENDAR TIME — at least
    60 days must have passed since the first site was promoted (Plan 11
    checkpoint). If this plan is being verified earlier (e.g., for
    integration testing), use --override-day-60 with a documented reason.

    **Step A — Verify sunset readiness:**
    ```sql
    -- Run in Supabase SQL Editor against prod:
    SELECT
      COUNT(*) FILTER (WHERE shim_version = 'v4' AND dual_run_state = 'promoted') AS promoted,
      COUNT(*) FILTER (WHERE shim_version = 'v4' AND dual_run_state != 'promoted') AS not_yet_promoted,
      COUNT(*) FILTER (WHERE shim_version = 'v4' AND v4_promoted_at <= NOW() - INTERVAL '60 days') AS promoted_60d_plus,
      COUNT(*) AS total
    FROM koto_wp_sites
    WHERE shim_version IN ('v3', 'v4');

    SELECT
      COUNT(*) AS major_diff_30d,
      MAX(called_at) AS latest_major_diff
    FROM koto_wp_dual_run_log
    WHERE diff_status = 'major_diff'
      AND called_at >= NOW() - INTERVAL '30 days';
    ```
    Expected: promoted_60d_plus equals total AND major_diff_30d equals 0.

    **Step B — Execute sunset:**
    ```bash
    node scripts/cutover/sunset-v3.cjs --confirm --reason='Day-60 sunset per CONTEXT.md D-Cutover-side-by-side' --filter=all
    # → Prompts "I UNDERSTAND" → type it
    # → Sequential per-site destruct
    # → Aggregate report
    ```

    **Step C — Verify dashboard pruning:**
    ```bash
    curl -X POST https://hellokoto.com/api/wp \
      -H 'Content-Type: application/json' \
      -d '{"action":"get_elementor_data","site_id":"<id>","agency_id":"<id>"}'
    # Expected: 410 Gone with deprecation body
    ```

    **Step D — Verify legacy manifest:**
    ```bash
    curl -s https://hellokoto.com/api/kotoiq-manifest | jq
    # Expected: {"deprecated": true, "sunset_date": ..., "successor_manifest": "/api/kotoiq-shim-manifest"}
    ```

    **Step E — Verify Supabase state:**
    ```sql
    SELECT shim_version, COUNT(*) FROM koto_wp_sites GROUP BY shim_version;
    -- Expected: only 'v4_only' (and possibly some unpaired sites with NULL)
    ```

    **Step F — Verify ROADMAP closure:**
    Check that `.planning/ROADMAP.md` shows Phase 10 marked complete + 12/12 plans complete.

    **Step G — Confirm to Claude with:**
    - Number of sites successfully deactivated
    - Number of skip/error cases (+ diagnostic for each error)
    - Confirmation of 410 response from /api/wp for old actions
    - Confirmation of deprecated:true from /api/kotoiq-manifest
    - Confirmation ROADMAP.md updated

    Expected response shape: `sunset-complete — n_deactivated=<N>; skipped=<S>; errors=<E>; api_410=yes; manifest_deprecated=yes; roadmap_marked=yes`
  </how-to-verify>
  <resume-signal>
    Type "sunset-complete" with the 6-field status from Step G. If --override-day-60 was used (e.g., for integration testing), note that explicitly: "sunset-complete (override) — ..."
  </resume-signal>
  <files>
    Supabase koto_wp_sites updates (shim_version='v4_only' for promoted sites) + koto_wp_shim_pairings audit rows + WP-side v3 plugin deactivations + .planning/ROADMAP.md Phase 10 [x] marking (which Task 2 already applied; verification re-confirms here)
  </files>
  <action>
    HUMAN ACTION REQUIRED — at calendar day 60 post-pilot (or with --override-day-60 + reason for integration test), operator verifies sunset readiness via the SQL queries in <how-to-verify> Step A, then runs sunset-v3.cjs with the full guardrail flags. Operator then verifies the dashboard pruning (410 response) + manifest deprecation (deprecated:true) + Supabase state (shim_version='v4_only'). Operator reports the 6-field status via the resume signal.
  </action>
  <verify>
    Operator confirms via the resume signal payload (n_deactivated + skipped + errors + api_410=yes + manifest_deprecated=yes + roadmap_marked=yes). Claude validates the response and closes Phase 10.
  </verify>
  <done>
    100% of paired sites are shim_version='v4_only'; /api/wp returns 410 for old actions; /api/kotoiq-manifest returns deprecated=true; ROADMAP.md shows Phase 10 [x] with all 12 plans checked.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sunset-v3.cjs → v3 /destruct endpoint | Uses legacy Bearer from koto_wp_sites.wpsc_api_key. Compromised dashboard could deactivate the fleet. Same as kill-switch.cjs blast radius. |
| sunset-v3.cjs → koto_wp_sites update | Service-role write; clears wpsc_api_key as part of state transition. |
| Deprecated /api/wp route → 410 | Standard HTTP deprecation; no data exposure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-12-01 | Tampering | sunset-v3.cjs run with override but criteria not actually met | mitigate | --override-day-60 requires --reason; audit row records the reason verbatim. Forensic-reviewable. |
| T-10-12-02 | Denial of Service | Mass v3 destruct breaks sites that secretly still depend on v3 modules | accept (per D-Backward-compat assumption) | If a site secretly depends on v3 module endpoints despite being 'promoted', that's a process gap from the cutover phase. Roll back via kill-switch.cjs --mode=rollback. |
| T-10-12-03 | Information Disclosure | Legacy manifest endpoint exposes successor URL | accept | The /api/kotoiq-shim-manifest endpoint is already public; no new info exposure. |
| T-10-12-04 | Repudiation | Sunset operations not logged | mitigate | Each per-site sunset inserts a koto_wp_shim_pairings event='v3_sunset_destructed' with reason + timestamp + destruct_result. |
| T-10-12-05 | Tampering | An attacker invokes /api/wp with a retained action ('destruct') to deactivate v3 unauthorized | mitigate | verifySession + agencyId check in /api/wp's existing dispatcher (Phase 7+ pattern). Plus: destruct on v3 requires the legacy Bearer which is per-site-stored — not just session-level. |
| T-10-12-06 | Spoofing | Cleanup-legacy-options.cjs deletes options before data is fully migrated | mitigate | Documented as OPTIONAL + separate from sunset-v3.cjs. Operator runs it only after confirming data migration. Plan 11's per-site promotion already migrates the options into kotoiq_shim_* keys. |
| T-10-12-07 | Information Disclosure | wpsc_api_key cleared from koto_wp_sites but the v3 plugin still has it in wp_options.wpsc_api_key on the WP site | accept | The v3 plugin is deactivated; wp_options reads stay. Per CONTEXT.md: 'Do NOT use v3.x's uninstall.php — that would wipe wpsc_* options, including the shared secret which we're still keeping for emergency rollback'. The legacy Bearer remains as emergency rollback. Accepted residual risk. |
</threat_model>

<verification>
- sunset-v3.cjs gated by 60-day check + zero major_diff
- Per-site destruct works against v3 /destruct endpoint via legacy Bearer
- /api/wp returns 410 for all non-sunset actions
- /api/kotoiq-manifest returns deprecated=true
- ROADMAP.md marks Phase 10 [x]
- All 12 plans complete
- Pilot site + all promoted sites successfully deactivated
- SUMMARY.md captures the close-out state
</verification>

<success_criteria>
- 100% of fleet is shim_version='v4_only' (no v3 plugin active)
- /api/wp + /api/kotoiq-manifest emit deprecation signals
- Phase 10 closed in ROADMAP.md
- The Phase 10 critical success criterion: a senior PHP developer with WP filesystem access reads the entire `wp-plugin-kotoiq-shim/` plugin source and CANNOT reconstruct KotoIQ's value proposition. The shim is generic plumbing; the value lives in the dashboard.
- Follow-up M2 items documented in ROADMAP "Deferred to M2" section
</success_criteria>

<output>
After completion, create `.planning/phases/10-kotoiq-wp-plugin-thin-shim-pivot-move-all-business-logic-out/10-12-SUMMARY.md` recording:
- Final fleet state (count by shim_version)
- Sunset run results: deactivated / skipped / errors
- Confirmation of /api/wp 410 + /api/kotoiq-manifest deprecation
- Confirmation of ROADMAP.md Phase 10 [x] + plan checklist all checked
- A short note on whether any --override flags were used + the reasons
- The list of M2 deferred items (final reference for the next milestone planner)
</output>
