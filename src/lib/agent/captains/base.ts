import 'server-only'

// Re-export the Captain interface from types — this file exists as the
// canonical import path referenced in AGENT_ARCHITECTURE.md Section 7.2.
export type { Captain } from '../types'
