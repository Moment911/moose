"use client";
// ReviewPage.jsx - placeholder, credentials moved to environment variables

import { R, T, BLK, GRY, W, GRN, AMB, FH, FB } from '../lib/theme'

export default function ReviewPage() {
  return <div>Review Page</div>
}
